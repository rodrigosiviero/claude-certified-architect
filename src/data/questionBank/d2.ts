/**
 * Question Bank — D2 Tool Design & Integration (12 questions)
 */
import type { PoolQuestion } from './d1';

const d2New: PoolQuestion[] = [
  {
    poolId: 134, domain: 'd2', domainLabel: 'Effective Tool Interfaces',
    examTask: '2.1 Effective Tool Interfaces',
    scenario: 'A customer support agent has get_customer and lookup_order tools. Both have minimal descriptions: "Retrieves customer information" and "Retrieves order details." Both accept identifier strings. The agent calls get_customer 34% of the time when users provide order numbers, because it can\'t determine which tool handles which input format.',
    question: 'What tool design change would most effectively improve tool selection accuracy?',
    options: [
      'A) Add few-shot examples to the system prompt showing correct tool selection for 8 different query patterns involving customer IDs and order numbers.',
      'B) Expand each tool\'s description to include accepted input formats (e.g., "Accepts customer IDs starting with CUS- or email addresses"), example queries it handles, and explicit boundaries explaining when to use it versus the other tool.',
      'C) Implement a routing classifier that parses user input before each agent turn and pre-selects the appropriate tool based on detected patterns.',
      'D) Consolidate both tools into a single lookup_entity tool that accepts any identifier and internally routes to the correct backend.',
    ],
    correct: 1,
    explanation: 'Tool descriptions are the primary mechanism models use for selection. When descriptions include input formats, examples, and boundaries, the model can reliably differentiate between similar tools. A adds token overhead without fixing the root cause. C adds infrastructure when a simpler fix exists. D removes tool granularity and may obscure errors. [Task 2.1 — Effective Tool Interfaces: detailed descriptions are the first-line fix for tool selection problems.]',
  },
  {
    poolId: 135, domain: 'd2', domainLabel: 'Structured Error Responses',
    examTask: '2.2 Structured Error Responses',
    scenario: 'A payment processing agent\'s charge_credit_card tool returns a plain-text error: "Transaction failed." When this occurs, the agent retries the same transaction 3 times in 4 seconds, triggering the payment gateway\'s fraud detection and temporarily blocking the merchant account.',
    question: 'What error response design would enable the agent to handle payment failures more intelligently?',
    options: [
      'A) Return a structured error object with fields: error_code (e.g., INSUFFICIENT_FUNDS, FRAUD_SUSPECTED, NETWORK_TIMEOUT), retryable (boolean), retry_after_seconds (number), and suggested_action (e.g., "try_alternative_card", "escalate_to_human").',
      'B) Add a system prompt instruction: "If a payment fails, do not retry immediately. Wait 30 seconds and try once more before escalating."',
      'C) Implement automatic exponential backoff in the orchestration layer for all tool failures, with a maximum of 3 retries regardless of error type.',
      'D) Replace the plain-text error with a detailed natural language explanation including the full gateway response so the agent can reason about the cause.',
    ],
    correct: 0,
    explanation: 'A structured error response with error_code, retryable, retry_after_seconds, and suggested_action enables the agent to make semantically correct decisions — retrying only transient errors with appropriate delays, and escalating permanent failures. B is a hardcoded rule that doesn\'t adapt to different error types. C retries all errors uniformly, including non-retryable ones like INSUFFICIENT_FUNDS. D uses verbose natural language when structured data is more reliable for programmatic decisions. [Task 2.2 — Structured Error Responses: structured errors enable intelligent agent behavior.]',
  },
  {
    poolId: 136, domain: 'd2', domainLabel: 'Tool Distribution & tool_choice',
    examTask: '2.3 Tool Distribution & tool_choice',
    scenario: 'A banking assistant has 15 tools spanning account management, loan processing, and investment services. Using tool_choice: "auto", the agent selects incorrect tools 18% of the time (e.g., calling get_investment_portfolio when the user asks about loan rates). Each misselection wastes an average of 1,200 tokens and adds 2 seconds of latency.',
    question: 'What approach would most effectively reduce tool selection errors without limiting the agent\'s capabilities?',
    options: [
      'A) Split the 15 tools into 3 domain-specific toolsets and use a lightweight intent classifier to select the appropriate toolset before each agent turn, setting tool_choice to the matching domain\'s tools.',
      'B) Reduce the tool catalog to 8 essential tools by removing rarely-used functions, decreasing selection complexity.',
      'C) Set tool_choice to the specific tool you expect the agent to use based on keyword matching from the user\'s input.',
      'D) Add detailed descriptions to all 15 tools including 3-5 usage examples each, helping the model differentiate between similar tools.',
    ],
    correct: 0,
    explanation: 'Domain-based toolset selection with an intent classifier directly addresses the root cause: presenting 15 tools creates selection ambiguity. Grouping by domain reduces the choice set to 5 tools per turn while preserving full capability. B removes functionality. C overrides the model\'s judgment and can\'t handle multi-tool scenarios. D is valuable but may be insufficient when tools are genuinely similar across domains. [Task 2.3 — Tool Distribution: managing tool visibility per context improves selection accuracy.]',
  },
  {
    poolId: 137, domain: 'd2', domainLabel: 'MCP Server Integration',
    examTask: '2.4 MCP Server Integration',
    scenario: 'Your agent connects to 3 MCP servers: filesystem (4 tools), database (6 tools), and analytics (5 tools). When all 3 servers are connected, initial tool discovery and schema loading takes 4.2 seconds. During a typical session, the agent only uses tools from 1-2 servers per conversation, but all 15 tools remain loaded in context.',
    question: 'What optimization would reduce both startup latency and per-request context overhead?',
    options: [
      'A) Connect all MCP servers at startup but unload tool schemas after the first user message by analyzing which tools are relevant to the query.',
      'B) Implement lazy MCP server connection: connect servers on-demand when the agent determines tools from that server are needed, caching connections for the session duration.',
      'C) Pre-connect all servers but replace full tool schemas with abbreviated descriptions (name + 10-word summary) during tool discovery, loading full schemas only when a tool is selected.',
      'D) Consolidate the 3 MCP servers into a single unified server that exposes all 15 tools through one connection.',
    ],
    correct: 1,
    explanation: 'Lazy on-demand connection reduces startup latency (connecting only needed servers) and context overhead (only loading relevant tool schemas). B directly addresses both concerns. A connects all servers upfront (keeping startup latency) then removes schemas dynamically (adding complexity). C keeps the startup cost but reduces per-request context — partial improvement. D requires server-side changes and loses the modularity benefits of MCP. [Task 2.4 — MCP Server Integration: connection management impacts both latency and context efficiency.]',
  },
  {
    poolId: 138, domain: 'd2', domainLabel: 'Built-in Tool Selection',
    examTask: '2.5 Built-in Tool Selection',
    scenario: 'A research agent has access to both a web_search tool and a document_search tool (for internal knowledge base). When users ask about company policies, the agent uses web_search 40% of the time, returning generic internet results instead of authoritative internal documentation.',
    question: 'What is the most effective approach to ensure internal policy queries use the correct tool?',
    options: [
      'A) Implement a query classifier that detects internal policy keywords and overrides tool_choice to force document_search for those queries.',
      'B) Add explicit boundary descriptions to each tool: web_search ("Use for general knowledge, current events, public information, market data — NOT for internal policies") and document_search ("Use for company policies, internal procedures, HR guidelines, compliance documents — ALWAYS prefer this for internal questions").',
      'C) Remove web_search entirely and have all queries go through document_search first, falling back to web_search only when document_search returns no results.',
      'D) Add a system prompt instruction: "For internal policy questions, always use document_search, not web_search."',
    ],
    correct: 1,
    explanation: 'Explicit boundary descriptions in tool definitions are the most reliable approach — they\'re always present in the model\'s context when it makes tool selection decisions, unlike system prompt instructions which may be deprioritized. A requires building and maintaining a classifier. C changes the tool access pattern, adding latency for all queries. D relies on instruction following which is probabilistic. [Task 2.5 — Built-in Tool Selection: tool boundary descriptions are more reliable than prompt-based routing.]',
  },
  {
    poolId: 139, domain: 'd2', domainLabel: 'Tool Search for Large Catalogs',
    examTask: '2.6 Tool Search for Large Catalogs',
    scenario: 'An enterprise agent has 85 tools across departments (HR, Finance, IT, Legal, Facilities). With all tool schemas loaded, the system prompt and tool definitions consume 52,000 tokens — leaving insufficient context for complex multi-step tasks. The agent also shows degraded tool selection accuracy (72% vs. 94% with fewer tools).',
    question: 'What architecture would maintain tool accessibility while improving selection accuracy and reducing context consumption?',
    options: [
      'A) Implement a two-stage tool discovery system: a lightweight tool registry with name + 20-word descriptions for all 85 tools, plus on-demand loading of full schemas for the 5-8 tools selected by the agent for the current task.',
      'B) Reduce the tool catalog by consolidating similar tools across departments, targeting 30 tools total.',
      'C) Split the agent into 5 department-specific agents, each with 15-20 tools, and route user queries to the appropriate agent.',
      'D) Load all 85 tools but use a higher-tier model with a larger context window to maintain selection accuracy.',
    ],
    correct: 0,
    explanation: 'Two-stage discovery (lightweight registry + on-demand schema loading) keeps all 85 tools accessible while reducing the per-request context from 52K to ~8K tokens. The agent first identifies relevant tools from the registry, then loads only their full schemas. B reduces capability. C fragments the user experience and adds routing complexity. D addresses symptoms rather than the root cause and increases cost. [Task 2.6 — Tool Search: managing large tool catalogs requires staged discovery to balance accessibility with context efficiency.]',
  },
  {
    poolId: 140, domain: 'd2', domainLabel: 'Effective Tool Interfaces',
    examTask: '2.1 Effective Tool Interfaces',
    scenario: 'A scheduling agent has a book_meeting tool that takes participant_ids, start_time, duration, and room_id. The tool returns either "Meeting booked" or "Conflict detected." When there\'s a conflict, the agent asks the user for a new time without knowing what the conflict is or suggesting alternatives.',
    question: 'What tool interface change would enable the agent to handle scheduling conflicts more helpfully?',
    options: [
      'A) Return a structured response with available_time_slots for all participants, the conflicting meeting details (with permission to share), and suggested alternative times ranked by participant availability overlap.',
      'B) Add a separate check_availability tool that the agent should call before attempting to book, to detect conflicts proactively.',
      'C) Increase the system prompt instructions to guide the agent to ask participants for 3 preferred time slots before booking.',
      'D) Modify the book_meeting tool to automatically resolve conflicts by finding the next available slot and booking it.',
    ],
    correct: 0,
    explanation: 'A structured conflict response with available slots and suggested alternatives enables the agent to offer actionable recommendations immediately. B adds an extra tool call for every booking attempt. C shifts burden to users. D removes user agency by automatically choosing times without confirmation. [Task 2.1 — Effective Tool Interfaces: tool return values should provide actionable context for error recovery.]',
  },
  {
    poolId: 141, domain: 'd2', domainLabel: 'Structured Error Responses',
    examTask: '2.2 Structured Error Responses',
    scenario: 'A logistics agent\'s create_shipment tool returns different error formats depending on the failure mode: sometimes a string ("Address not found"), sometimes a JSON object with an error field, and sometimes an HTTP status code with no body. The agent handles each format inconsistently — sometimes retrying, sometimes escalating, sometimes crashing.',
    question: 'What standardization approach would ensure consistent error handling across all failure modes?',
    options: [
      'A) Wrap the tool in an adapter that catches all error responses and normalizes them to a consistent format: {success: boolean, error_type: string, message: string, details: object, retryable: boolean}.',
      'B) Add system prompt instructions describing all possible error formats and how the agent should handle each one.',
      'C) Add try/catch around every tool call in the agentic loop to prevent crashes, logging errors for later analysis.',
      'D) Replace the create_shipment tool with a new implementation that has better internal error handling.',
    ],
    correct: 0,
    explanation: 'A normalizing adapter provides a consistent interface regardless of the underlying error format, enabling reliable agent handling. B requires the model to parse and distinguish between multiple formats — error-prone. C prevents crashes but doesn\'t enable intelligent error handling. D may not be feasible if the tool is a third-party service. [Task 2.2 — Structured Error Responses: consistent error formats are foundational for reliable agent behavior.]',
  },
  {
    poolId: 142, domain: 'd2', domainLabel: 'Tool Distribution & tool_choice',
    examTask: '2.3 Tool Distribution & tool_choice',
    scenario: 'A customer service agent must always verify identity before accessing account tools. Currently, you rely on the system prompt to enforce this sequence, but 7% of conversations show the agent accessing account data without completing verification.',
    question: 'What tool_choice configuration would deterministically enforce the verification-first workflow?',
    options: [
      'A) Set tool_choice to {type: "tool", name: "verify_identity"} for the first turn, then switch to "auto" only after verify_identity returns a confirmed identity.',
      'B) Add verify_identity as a required parameter for all account tools, forcing the model to call it first.',
      'C) Set tool_choice to "required" for the first turn, ensuring the agent calls at least one tool before responding.',
      'D) Configure the system to reject any response that includes account data if the conversation history doesn\'t contain a verify_identity tool call.',
    ],
    correct: 0,
    explanation: 'Using tool_choice to force verify_identity on the first turn, then switching to "auto" after verification, provides deterministic enforcement. The agent physically cannot call account tools until tool_choice is relaxed. B is not a real API feature — tool_choice doesn\'t work as a parameter. C forces any tool, not specifically verify_identity. D is post-hoc detection rather than prevention. [Task 2.3 — Tool Distribution: tool_choice can enforce mandatory tool sequences deterministically.]',
  },
  {
    poolId: 143, domain: 'd2', domainLabel: 'MCP Server Integration',
    examTask: '2.4 MCP Server Integration',
    scenario: 'Your agent uses MCP to connect to a database server with tools like query_database, list_tables, and describe_schema. During peak usage, the database server\'s MCP connection drops intermittently. When this happens, the agent receives no error — it sends a tool call and gets no response, causing the agentic loop to hang indefinitely.',
    question: 'What MCP integration pattern would prevent indefinite hangs during server disconnections?',
    options: [
      'A) Implement a heartbeat mechanism that pings the MCP server every 30 seconds and triggers a reconnection if no response is received within 5 seconds.',
      'B) Add a timeout wrapper around every MCP tool call: if no response arrives within a configurable threshold (e.g., 15 seconds), return a structured timeout error to the agent, enabling it to retry or use an alternative approach.',
      'C) Configure the MCP client to automatically reconnect when a connection drop is detected, retrying the failed tool call transparently.',
      'D) Cache the last known response from each MCP tool and return the cached value when the server is unavailable.',
    ],
    correct: 1,
    explanation: 'A timeout wrapper ensures the agentic loop never hangs indefinitely — it receives a structured error and can decide how to proceed (retry, use alternative, inform user). A heartbeat helps detect disconnections but doesn\'t prevent in-progress calls from hanging. C retries transparently but may retry indefinitely without a timeout. D returns stale data that may be incorrect. [Task 2.4 — MCP Server Integration: timeout handling is essential for reliable external service communication.]',
  },
  {
    poolId: 144, domain: 'd2', domainLabel: 'Tool Search for Large Catalogs',
    examTask: '2.6 Tool Search for Large Catalogs',
    scenario: 'Your enterprise agent dynamically loads tool schemas based on the user\'s department. The tool registry stores 120 tools with metadata (name, department, description, tags). When the agent queries for relevant tools, it receives 15-20 matches but occasionally misses the optimal tool because the search relies on exact keyword matching against tool names and descriptions.',
    question: 'What tool search improvement would increase the likelihood of surfacing the optimal tool?',
    options: [
      'A) Increase the number of tags per tool to cover more potential search terms.',
      'B) Implement semantic search over tool descriptions using embeddings, returning tools whose descriptions match the query\'s intent rather than just keyword overlap.',
      'C) Return all 120 tools and let the model select the best one from the full catalog.',
      'D) Require tool authors to include a comprehensive "when to use" section in every tool description.',
    ],
    correct: 1,
    explanation: 'Semantic search over embeddings matches intent rather than keywords, dramatically improving tool discovery when users describe needs differently from how tools are named. A adds metadata overhead without addressing the fundamental limitation of exact matching. C returns too many tools for reliable selection. D is a good practice but doesn\'t solve the keyword matching limitation. [Task 2.6 — Tool Search: semantic matching outperforms keyword search for large tool catalogs.]',
  },
  {
    poolId: 145, domain: 'd2', domainLabel: 'Built-in Tool Selection',
    examTask: '2.5 Built-in Tool Selection',
    scenario: 'A coding assistant has a file_read tool and a grep_search tool. When the user asks "Where is the database connection configured?", the agent calls file_read on 12 different config files sequentially (taking 45 seconds) instead of using grep_search to locate the relevant file first (which would take 3 seconds).',
    question: 'What change would direct the agent toward the more efficient search-first approach?',
    options: [
      'A) Remove file_read from the tool catalog and provide only grep_search, forcing the agent to search before reading.',
      'B) Update grep_search\'s description to emphasize it as the preferred starting point for location tasks: "Use this FIRST when you need to find where something is defined or configured — returns file paths and line numbers. More efficient than reading files sequentially."',
      'C) Implement a system that intercepts sequential file_read calls and automatically switches to grep_search after 3 consecutive reads.',
      'D) Add few-shot examples showing grep_search followed by targeted file_read as the standard pattern for "where is" queries.',
    ],
    correct: 1,
    explanation: 'Updating the tool description to emphasize it as the first step for location tasks directly influences the model\'s tool selection at the point of decision. The model sees this guidance every time it considers which tool to use. A removes legitimate functionality. C adds hidden behavior that may surprise users. D is helpful but descriptions are more reliable than few-shot for tool selection because they\'re always present in context. [Task 2.5 — Built-in Tool Selection: tool descriptions are the most effective lever for influencing selection patterns.]',
  },
];

export default d2New;
