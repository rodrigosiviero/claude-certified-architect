import type { ScenarioExamData } from './types';

const exam: ScenarioExamData = {
  id: 5,
  title: 'Document Processing Pipeline',
  description: 'Design and optimize automated document processing workflows with extraction, validation, classification, and quality assurance.',
  icon: '📄',
  questions: [
    { id: 1, examScenario: 5, domain: 'd1', domainLabel: 'Agentic Loops', examTask: '1.1 Agentic Loops', scenario: 'A document processing agent extracts data from invoices using an agentic loop: load_document → extract_fields → validate → output. For 8% of invoices, the extraction step produces incomplete results, but the agent proceeds to validation and output with missing fields rather than re-extracting.', question: 'What loop modification would ensure complete extraction before proceeding?', options: ['A) Add a system prompt instruction: "Ensure all fields are extracted before proceeding to validation."', 'B) Add a completeness check between extraction and validation: count extracted fields against the required field list. If completeness < 95%, loop back to extraction with a targeted prompt listing the missing fields.', 'C) Run the extraction step 3 times and take the most complete result.', 'D) Increase the extraction step\'s max_tokens to allow more detailed extraction.'], correct: 1, explanation: 'A completeness check with targeted re-extraction directly addresses incomplete results by identifying specific gaps and providing the agent with actionable feedback. A relies on instruction following. C wastes tokens on redundant extractions. D doesn\'t address the root cause — the model may still miss fields with more output tokens. [Task 1.1 — Agentic Loops: completeness gates between stages prevent propagating incomplete results downstream.]' },
    { id: 2, examScenario: 5, domain: 'd1', domainLabel: 'Multi-Agent Orchestration', examTask: '1.2 Multi-Agent Orchestration', scenario: 'Your document pipeline has ExtractionAgent, ClassificationAgent, and ValidationAgent. Currently they run sequentially. Analysis shows extraction takes 3s, classification takes 1s, and validation takes 4s. Classification and validation both need extraction results but don\'t depend on each other.', question: 'What orchestration change would minimize total processing time?', options: ['A) Run all three agents in parallel.', 'B) Run extraction first, then run classification and validation in parallel since they both depend on extraction but not on each other. Total: 3s + max(1s, 4s) = 7s instead of 8s.', 'C) Merge classification and validation into a single agent.', 'D) Run classification before extraction to determine document type.'], correct: 1, explanation: 'Parallelizing classification and validation after extraction reduces total time from 8s to 7s. They\'re independent — both need extraction results but don\'t need each other\'s output. A violates dependencies (classification needs extraction). C loses specialization. D inverts the dependency. [Task 1.2 — Multi-Agent Orchestration: parallelize independent downstream stages after their common dependency completes.]' },
    { id: 3, examScenario: 5, domain: 'd2', domainLabel: 'Effective Tool Interfaces', examTask: '2.1 Effective Tool Interfaces', scenario: 'Your extraction agent has extract_text and extract_table tools. Both accept a document parameter. The agent uses extract_text for 40% of documents that contain tables, producing unstructured output that\'s harder to validate.', question: 'What tool interface change would improve tool selection?', options: ['A) Remove extract_text and force all documents through extract_table.', 'B) Update tool descriptions to include explicit boundaries: extract_text ("For prose content, paragraphs, and narrative text — NOT for tabular data"), extract_table ("For any content with rows and columns, financial figures, pricing tables, specifications — ALWAYS prefer this when the document contains structured data").', 'C) Add a pre-processing step that detects tables and routes to the correct tool.', 'D) Combine both tools into extract_content that automatically detects the content type.'], correct: 1, explanation: 'Explicit boundary descriptions in tool definitions enable reliable selection at the point of decision. The model sees "NOT for tabular data" and "ALWAYS prefer this for structured data" when choosing. A removes legitimate functionality. C adds infrastructure when a simpler fix exists. D may produce inconsistent output formats. [Task 2.1 — Tool Interfaces: explicit boundary descriptions in tool definitions are the most effective tool selection aid.]' },
    { id: 4, examScenario: 5, domain: 'd4', domainLabel: 'Structured Output', examTask: '4.3 Structured Output (tool_use + JSON Schema)', scenario: 'Your extraction agent must output structured invoice data: vendor, date, line_items (array), subtotal, tax, total. Free-text output requires a parsing step that fails 15% of the time due to format variations.', question: 'What is the most reliable approach to guarantee structured output?', options: ['A) Add JSON formatting instructions to the system prompt with the schema.', 'B) Define the invoice schema as a tool with required fields, types, and descriptions, then use tool_choice to force the model to output via this tool — guaranteeing parseable structured output.', 'C) Use response_format: {type: "json_object"} and include the schema.', 'D) Post-process with a second Claude call that extracts structured data from free text.'], correct: 1, explanation: 'tool_use with JSON Schema and tool_choice provides the strongest guarantee — the model is constrained to produce valid JSON matching the schema. A and C rely on instruction following for format compliance. D adds latency and cost for what should be a single-pass operation. [Task 4.3 — Structured Output: tool_use with JSON Schema is the most reliable mechanism for guaranteed structured output.]' },
    { id: 5, examScenario: 5, domain: 'd4', domainLabel: 'Validation & Retry', examTask: '4.4 Validation, Retry & Feedback Loops', scenario: 'Your document validation catches extraction errors 12% of the time — wrong data types, missing required fields, values outside expected ranges. Currently you regenerate the entire extraction on failure.', question: 'What retry approach would be most efficient?', options: ['A) Retry the full extraction with the same prompt — it will likely succeed on the second attempt.', 'B) Include the specific validation error in the retry: "The \'tax_amount\' field is negative (-$5.20) but should be positive. The \'invoice_date\' format should be YYYY-MM-DD, not DD/MM/YYYY. Fix these specific issues while keeping the correct fields."', 'C) Add the full schema to the system prompt to prevent validation errors.', 'D) Validate the document format before extraction.'], correct: 1, explanation: 'Including specific validation errors enables targeted fixes — correcting only what\'s wrong while preserving correct content. A relies on blind retries. C is good prevention but doesn\'t help when errors occur. D validates the input format, not the output. [Task 4.4 — Validation & Retry: specific error context in retries enables targeted corrections.]' },
    { id: 6, examScenario: 5, domain: 'd4', domainLabel: 'Batch Processing', examTask: '4.5 Batch Processing Strategies', scenario: 'You process 200 documents daily using real-time Claude calls at $0.01 each ($2.00/day). Documents arrive throughout the day but processed results are only needed by 8 AM the next morning for the accounting team.', question: 'What is the optimal processing strategy?', options: ['A) Process documents in real-time as they arrive for minimal latency.', 'B) Accumulate documents throughout the day and submit as a Message Batch at midnight. Results arrive before 8 AM at 50% cost savings ($1.00/day).', 'C) Process documents in batches of 50 every 4 hours.', 'D) Process immediately but switch to a cheaper model.'], correct: 1, explanation: 'Since results are only needed by 8 AM, overnight batch processing maximizes Message Batches\' 50% savings while meeting the deadline. A doesn\'t optimize for the actual requirement. C partially batches but doesn\'t maximize savings. D may reduce quality. [Task 4.5 — Batch Processing: overnight batch submission aligned with delivery deadlines maximizes cost savings.]' },
    { id: 7, examScenario: 5, domain: 'd5', domainLabel: 'Context Management', examTask: '5.1 Conversation Context Management', scenario: 'Your agent processes multi-page contracts (50+ pages). After extracting data from page 30, the context is 20K tokens. When processing page 40, the agent forgets key definitions from page 5 (e.g., "Party A" refers to the client, not the vendor).', question: 'What context management approach would maintain critical definitions throughout?', options: ['A) Re-read page 5 before processing each subsequent page.', 'B) Extract key definitions and cross-references into a structured "contract context" object that\'s injected at the start of each page processing step, ensuring critical terms are always visible.', 'C) Process the entire contract in a single pass to maintain full context.', 'D) Have the agent ask for clarification when it encounters ambiguous terms.'], correct: 1, explanation: 'A structured contract context object ensures critical definitions are always visible regardless of how deep into the document the agent is processing. A wastes tokens on repeated reads. C may hit context limits with 50+ pages. D shifts burden to the user and isn\'t feasible for automated processing. [Task 5.1 — Context Management: extracting and persisting key definitions prevents information loss across long document processing.]' },
    { id: 8, examScenario: 5, domain: 'd5', domainLabel: 'Error Propagation', examTask: '5.3 Error Propagation Strategies', scenario: 'A document classification agent encounters an unknown document type. It returns a generic "Classification failed" error. The downstream routing agent doesn\'t know whether to retry classification, route to a human, or skip the document.', question: 'What error design would enable appropriate downstream handling?', options: ['A) Return structured error context: {error_type: "UNKNOWN_DOCUMENT_TYPE", confidence_scores: {invoice: 0.25, contract: 0.22, report: 0.20}, suggested_actions: ["route_to_human", "retry_with_ocr", "flag_for_review"]}.', 'B) Default to "invoice" classification since it\'s the most common type.', 'C) Retry classification 3 times with increasing detail.', 'D) Return the raw model output for the downstream agent to interpret.'], correct: 0, explanation: 'Structured error context with confidence scores and suggested actions enables the downstream agent to make an informed decision — route to human review given the low, evenly distributed confidence. B misclassifies documents. C retries may not help if the document type is genuinely unknown. D shifts interpretation burden without providing structure. [Task 5.3 — Error Propagation: structured errors with confidence context enable intelligent downstream decisions.]' },
    { id: 9, examScenario: 5, domain: 'd5', domainLabel: 'Human Review & Confidence', examTask: '5.5 Human Review & Confidence Calibration', scenario: 'Your extraction agent processes financial documents with a self-reported confidence score. When it reports 85%+ confidence, accuracy is 90%. For documents with confidence 60-85%, accuracy is 70%. A wrong extraction could cause a financial reporting error.', question: 'What review policy balances throughput with accuracy?', options: ['A) Require human review for all documents to ensure accuracy.', 'B) Allow autonomous processing for documents with 85%+ confidence (90% accuracy is acceptable), and require human review for documents below 85% confidence. Apply spot-checking to 5% of high-confidence results for ongoing calibration.', 'C) Lower the autonomous threshold to 70% confidence to increase throughput.', 'D) Process all documents autonomously and flag errors in post-processing.'], correct: 1, explanation: 'Tiered review based on calibrated confidence balances throughput (autonomous for high-accuracy tier) with safety (human review for lower-accuracy tier). Spot-checking maintains calibration over time. A is overly cautious. C accepts 70% accuracy for financial data — too risky. D catches errors after they occur rather than preventing them. [Task 5.5 — Human Review: tiered review with ongoing calibration balances throughput and accuracy for financial data.]' },
    { id: 10, examScenario: 5, domain: 'd2', domainLabel: 'Tool Distribution & tool_choice', examTask: '2.3 Tool Distribution & tool_choice', scenario: 'Your document pipeline must classify documents before extraction (different document types use different extraction schemas). The agent sometimes skips classification and goes directly to extraction, using the wrong schema.', question: 'What tool_choice configuration would enforce classification-first?', options: ['A) Add a system prompt instruction: "Always classify the document before extracting data."', 'B) Set tool_choice to {type: "tool", name: "classify_document"} for the first turn, then switch to "auto" after classification returns a document type that determines which extraction tool becomes available.', 'C) Set tool_choice to "required" to ensure at least one tool is called.', 'D) Combine classification and extraction into a single tool.'], correct: 1, explanation: 'Using tool_choice to force classify_document on the first turn, then switching to "auto" with type-specific extraction tools, provides deterministic enforcement. A relies on instruction following. C forces any tool, not specifically classification. D loses the architectural benefit of separate concerns. [Task 2.3 — Tool Distribution: tool_choice enforces mandatory first-step classification deterministically.]' },
    { id: 11, examScenario: 5, domain: 'd1', domainLabel: 'Task Decomposition', examTask: '1.6 Task Decomposition Strategies', scenario: 'You need to process a 200-page legal contract. You decompose it into 20 subtasks of 10 pages each. The final synthesis misses cross-references between sections (e.g., Section 3 references definitions in Section 15). 35% of answers are incomplete due to missed dependencies.', question: 'What decomposition would handle cross-section dependencies?', options: ['A) Process the entire contract in a single call to maintain full context.', 'B) Use a two-pass approach: first pass extracts cross-references and builds a dependency graph, second pass assigns subtasks informed by the graph so related sections are analyzed together.', 'C) Decompose into overlapping chunks (pages 1-15, 11-25, etc.) with 5-page overlap.', 'D) Increase chunk size to 30 pages per subtask.'], correct: 1, explanation: 'A two-pass approach with dependency graph explicitly handles cross-section references. The graph ensures related sections are co-analyzed. A may hit context limits with 200 pages. C adds overlap but doesn\'t guarantee sections 3 and 15 are covered together. D is a probabilistic improvement that doesn\'t address the fundamental decomposition problem. [Task 1.6 — Task Decomposition: dependency-aware decomposition prevents information loss across subtask boundaries.]' },
    { id: 12, examScenario: 5, domain: 'd1', domainLabel: 'Workflow Enforcement', examTask: '1.4 Workflow Enforcement & Structured Handoffs', scenario: 'Your pipeline requires: OCR → extraction → validation → formatting. The agent occasionally skips OCR and passes raw image data to extraction, producing garbage output.', question: 'What enforcement mechanism would ensure OCR always runs?', options: ['A) Add explicit instructions: "Always run OCR on scanned documents before extraction."', 'B) Implement a pipeline where the extraction tool only accepts input from the OCR tool — the orchestration layer ensures OCR output is passed to extraction, making it impossible to skip.', 'C) Add a pre-processing check that detects image input and routes to OCR.', 'D) Have the extraction tool handle both OCR and extraction internally.'], correct: 1, explanation: 'Pipeline enforcement where extraction only receives OCR output makes skipping impossible — extraction physically cannot receive raw image data. A relies on instruction following. C adds detection logic but doesn\'t prevent the skip. D combines concerns and loses the benefits of specialized processing. [Task 1.4 — Workflow Enforcement: pipeline-enforced data flow prevents step-skipping deterministically.]' },
    { id: 13, examScenario: 5, domain: 'd2', domainLabel: 'MCP Server Integration', examTask: '2.4 MCP Server Integration', scenario: 'Your document agent connects to an MCP-based OCR server. The OCR server occasionally returns garbled text for poor-quality scans. Currently the agent passes garbled text directly to extraction, producing wrong results.', question: 'What integration pattern would handle low-quality OCR output?', options: ['A) Add an OCR quality score to the tool response (0-1). When quality is below 0.5, flag the document for manual review rather than proceeding with extraction.', 'B) Increase the OCR processing quality setting for all documents.', 'C) Run OCR twice and compare results, using the more consistent output.', 'D) Have the extraction agent attempt to process regardless of quality.'], correct: 0, explanation: 'A quality score enables data-driven decisions — flag low-quality results for human review rather than producing unreliable extractions. B may slow processing for all documents. C doubles OCR cost. D produces unreliable results. [Task 2.4 — MCP Server Integration: quality metrics in tool responses enable data-driven processing decisions.]' },
    { id: 14, examScenario: 5, domain: 'd5', domainLabel: 'Provenance & Uncertainty', examTask: '5.6 Information Provenance & Uncertainty', scenario: 'Your extraction agent occasionally adds data that isn\'t in the source document — for example, inferring a vendor address from the vendor name when the address isn\'t printed on the invoice. This happens in 10% of extractions.', question: 'What approach would prevent inferred data from appearing in extractions?', options: ['A) Add a system prompt instruction: "Only extract data that is explicitly present in the document."', 'B) Require the extraction tool to output source coordinates (page, region) for every extracted field, enabling verification that each data point comes from the actual document. Fields without source coordinates are flagged as unverified.', 'C) Add a post-extraction verification step that compares extracted data against the original document.', 'D) Use a lower temperature to reduce creative additions.'], correct: 1, explanation: 'Source coordinates for every field enable automated verification that extracted data actually appears in the document. This prevents inference structurally — every data point must be traceable to a specific location. A relies on instruction following. C adds cost and latency. D doesn\'t prevent inference when the model lacks the correct information. [Task 5.6 — Provenance: requiring source coordinates for extracted data prevents inference structurally.]' },
    { id: 15, examScenario: 5, domain: 'd4', domainLabel: 'Prompts with Explicit Criteria', examTask: '4.1 Prompts with Explicit Criteria', scenario: 'Your classification agent categorizes documents into 15 types. Accuracy is 75%. The prompt says: "Classify this document into the most appropriate category." Many errors come from confusion between similar types (e.g., "purchase_order" vs "invoice").', question: 'What prompt change would most effectively reduce classification errors?', options: ['A) Add few-shot examples for each of the 15 categories.', 'B) Replace vague instructions with explicit criteria for each category, including defining features, required elements, and distinguishing characteristics for commonly confused pairs.', 'C) Increase the model temperature to consider more perspectives.', 'D) Use a simpler 5-category classification first, then sub-classify.'], correct: 1, explanation: 'Explicit criteria with distinguishing characteristics for confused pairs directly addresses the root cause. The model needs clear boundaries between similar categories, not more examples of each in isolation. A adds examples without addressing the boundary ambiguity. C is counterproductive for classification. D adds pipeline complexity without addressing the boundary problem. [Task 4.1 — Explicit Criteria: distinguishing characteristics for commonly confused pairs improve classification more than generic examples.]' },
    { id: 16, examScenario: 5, domain: 'd3', domainLabel: 'Iterative Refinement', examTask: '3.5 Iterative Refinement', scenario: 'Your extraction agent achieves 88% accuracy on standard documents but only 62% on handwritten ones. You want to improve handwritten document accuracy without affecting standard document performance.', question: 'What refinement approach would be most appropriate?', options: ['A) Improve the system prompt to better handle handwriting.', 'B) Create a specialized extraction prompt for handwritten documents (triggered by classification), iteratively refining it against a test set of handwritten documents without changing the standard document prompt.', 'C) Add more examples of handwritten documents to the general prompt.', 'D) Process all documents with a handwriting-aware prompt.'], correct: 1, explanation: 'A specialized prompt for handwritten documents, iteratively refined against a test set, targets the specific weakness without affecting the 88% accuracy on standard documents. A is too vague. C dilutes the prompt for standard documents. D may reduce standard document accuracy. [Task 3.5 — Iterative Refinement: targeted refinement of specialized prompts addresses specific weaknesses without affecting general performance.]' },
    { id: 17, examScenario: 5, domain: 'd4', domainLabel: 'Few-Shot Prompting', examTask: '4.2 Few-Shot Prompting', scenario: 'Zero-shot extraction achieves 72% accuracy. Adding 3 examples per document type (45 total, 4,000 tokens) improves to 84%. Adding 3 more per type (90 total, 8,000 tokens) only improves to 85%.', question: 'What would be the most token-efficient way to push accuracy higher?', options: ['A) Add 5 more examples per type (165 total).', 'B) Analyze errors to identify the 3 most common extraction failure patterns and add 2 targeted examples specifically addressing each pattern — 6 new examples instead of 45.', 'C) Replace all examples with chain-of-thought extraction reasoning.', 'D) Use a larger model to process the 90 examples more effectively.'], correct: 1, explanation: 'Targeted examples addressing specific failure patterns are far more efficient than volume-based approaches. 6 targeted examples outperform 45 generic additions. A adds massive token overhead for 1% gain. C replaces working examples with an unproven approach. D increases cost without addressing the specific failure patterns. [Task 4.2 — Few-Shot Prompting: targeted examples addressing specific failure patterns outperform volume-based approaches.]' },
    { id: 18, examScenario: 5, domain: 'd1', domainLabel: 'Subagent Invocation', examTask: '1.3 Subagent Invocation & Context Isolation', scenario: 'Your document pipeline spawns a subagent for each document in a batch. The parent passes the full pipeline configuration (2,000 tokens) to each subagent. A batch of 100 documents generates 200,000 tokens of repeated configuration context.', question: 'How can you reduce context overhead while maintaining consistent extraction?', options: ['A) Pass the full configuration to every subagent for consistency.', 'B) Condense the pipeline configuration into a 300-token summary for subagents, and have the parent agent perform a final consistency review against the full configuration.', 'C) Process all documents in a single agent call.', 'D) Remove the configuration and let each subagent determine extraction rules independently.'], correct: 1, explanation: 'Condensed configuration for subagents + full-configuration parent review reduces per-subagent overhead by 85% while maintaining quality through the parent\'s consistency check. A wastes 200K tokens on repeated context. C may hit context limits. D produces inconsistent extractions. [Task 1.3 — Subagent Invocation: condensed context for subagents with full-context parent review balances quality and efficiency.]' },
    { id: 19, examScenario: 5, domain: 'd1', domainLabel: 'Session State', examTask: '1.7 Session State, Persistence & Resumption', scenario: 'Your document processing session is interrupted after processing 75 of 100 documents. The progress is lost — on resumption, it starts from document 1.', question: 'What session management would prevent this?', options: ['A) Store all 75 processed documents in a database.', 'B) Implement checkpoint persistence: after every 10 documents, save progress (processed document IDs, results, remaining queue). On resumption, restore the checkpoint and continue from document 76.', 'C) Require manual saves before any potential interruption.', 'D) Process all documents in a single uninterrupted session.'], correct: 1, explanation: 'Periodic checkpoint persistence enables resumption from the last checkpoint, avoiding redundant processing of completed documents. A stores results but doesn\'t enable automatic resumption. C is unrealistic. D assumes perfect reliability, which isn\'t achievable. [Task 1.7 — Session State: periodic checkpoints enable efficient resumption of batch processing after interruptions.]' },
    { id: 20, examScenario: 5, domain: 'd2', domainLabel: 'Structured Error Responses', examTask: '2.2 Structured Error Responses', scenario: 'Your formatting tool returns "Error" for various issues: unsupported format, missing required fields, and data overflow. The agent can\'t determine which fix to apply without understanding the specific error.', question: 'What error design would enable targeted error handling?', options: ['A) Return structured errors: {error_type: "UNSUPPORTED_FORMAT" | "MISSING_FIELDS" | "DATA_OVERFLOW", affected_fields: [...], max_length: number, suggested_fix: "..."} for each failure mode.', 'B) Add more descriptive error messages in natural language.', 'C) Have the agent retry with a different format when any error occurs.', 'D) Log all errors and handle them in a post-processing step.'], correct: 0, explanation: 'Structured errors with error_type and recovery context enable the agent to take the appropriate action for each failure — switch format, add missing fields, or truncate data. B requires natural language parsing. C applies a one-size-fits-all approach. D delays handling until post-processing. [Task 2.2 — Structured Error Responses: typed errors with recovery context enable targeted fixes for each failure mode.]' },
  ],
};

export default exam;
