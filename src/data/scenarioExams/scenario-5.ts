import type { ScenarioExamData } from './types';

const exam: ScenarioExamData = {
  id: 5,
  title: 'Document Processing Pipeline',
  description: 'Design and optimize an agent pipeline for document ingestion, extraction, classification, and quality assurance at scale.',
  icon: '📄',
  questions: [
    // ─── D1 ──────────────────────────────────────────────────────────
    {
      id: 1, examScenario: 5, domain: 'd1', domainLabel: 'Agentic Loops',
      scenario: 'Your document processing agent uses an agentic loop: extract fields, validate, flag issues, retry. For 90% of documents, extraction succeeds in 1-2 iterations. For the remaining 10% (poor quality scans, handwritten forms), the agent loops 6+ times without improving accuracy — each retry produces the same errors.',
      question: 'What is the most effective way to handle documents that consistently fail extraction?',
      options: [
        'A) Set max_iterations to 10 and keep retrying.',
        'B) After 2 failed iterations with no improvement in extraction accuracy, classify the document as "requires manual processing" and route it to a human queue with the specific extraction failures documented, rather than wasting additional API calls on diminishing returns.',
        'C) Increase the image resolution for each retry.',
        'D) Remove the retry loop and accept single-pass extraction results.',
      ],
      correct: 1,
      explanation: 'Diminishing returns detection — recognizing that repeated attempts produce the same errors — is a key agentic loop pattern. Routing to human processing after 2 failed attempts preserves API costs and processing time while ensuring quality. Option A wastes resources. Option C doesn\'t address the underlying model limitation. Option D accepts poor quality for the 10% that could benefit from one retry.',
    },
    {
      id: 2, examScenario: 5, domain: 'd1', domainLabel: 'Multi-Agent Orchestration',
      scenario: 'Your document pipeline has four sequential agents: IngestAgent (reads and normalizes documents), ClassifyAgent (categorizes by document type), ExtractAgent (pulls structured data based on type), and ValidateAgent (checks extraction accuracy). Documents that fail validation go back to ExtractAgent. Analysis shows 25% of validation failures are actually classification errors — the wrong extraction template was applied.',
      question: 'Where should failed documents be routed based on this analysis?',
      options: [
        'A) Always route back to ExtractAgent since that\'s where the extraction happens.',
        'B) Implement intelligent error routing: when validation detects that extracted fields don\'t match the expected schema for the classified type, route back to ClassifyAgent for re-classification rather than ExtractAgent, addressing the actual root cause.',
        'C) Route all failures to IngestAgent to start the pipeline over.',
        'D) Merge ClassifyAgent and ExtractAgent into a single agent.',
      ],
      correct: 1,
      explanation: 'Intelligent error routing based on failure type addresses root causes rather than symptoms. When 25% of failures are classification errors, routing those back to the classifier is more effective than retrying extraction with the wrong template. Option A addresses the symptom, not the cause. Option C is unnecessarily destructive. Option D loses the benefits of specialized agents.',
    },
    {
      id: 3, examScenario: 5, domain: 'd1', domainLabel: 'Tool Use Flow',
      scenario: 'Your document agent receives a response containing both a text block ("I\'ll extract the invoice data now") and a tool_use block calling extract_invoice_fields. Your implementation processes the tool_use but discards the text block.',
      question: 'What is the correct way to handle mixed content (text + tool_use) responses?',
      options: [
        'A) Only process the tool_use block — text blocks in tool-use responses are irrelevant.',
        'B) Process both: display the text block to the user for transparency and execute the tool_use block. The text provides context about what the agent is doing while the tool executes.',
        'C) Return an error — responses should contain either text or tool_use, not both.',
        'D) Queue the text block and display it only after the tool completes.',
      ],
      correct: 1,
      explanation: 'Mixed content blocks (text + tool_use) are valid API behavior. The text block provides immediate user feedback while the tool executes, improving perceived responsiveness. Discarding it removes transparency. Option A loses useful user-facing context. Option C treats valid behavior as an error. Option D delays feedback unnecessarily.',
    },
    {
      id: 4, examScenario: 5, domain: 'd1', domainLabel: 'Agentic Loops',
      scenario: 'Your document processing agent handles 5,000 documents/day. Each document goes through: read → extract → validate. The validate step catches errors and sends documents back for re-extraction. Currently, 15% of documents cycle through 3+ times. You add a deduplication check that detects when Claude extracts the same (incorrect) values twice in a row.',
      question: 'What should happen when identical failed extractions are detected?',
      options: [
        'A) Continue retrying — eventually the extraction will succeed.',
        'B) Break the loop: route the document to a human review queue with the extraction attempts and failure analysis documented, since repeated identical failures indicate the model cannot improve without new information or approach.',
        'C) Increase temperature on the next retry to encourage different extraction results.',
        'D) Reduce the validation strictness to allow the extraction to pass.',
      ],
      correct: 1,
      explanation: 'Identical failed extractions indicate the model has reached its capability limit for that document. Continuing to retry wastes resources without improvement. Human routing with documented attempts is the efficient response. Option A wastes API calls. Option C adds randomness that is unlikely to improve structured data extraction. Option D sacrifices quality.',
    },
    // ─── D2 ──────────────────────────────────────────────────────────
    {
      id: 5, examScenario: 5, domain: 'd2', domainLabel: 'System Prompt Design',
      scenario: 'Your extraction agent handles invoices from 200+ vendors, each with unique layouts. The system prompt describes extraction rules in general terms: "Extract vendor name, invoice number, date, line items, and total amount." For vendor ABC Corp (structured template), accuracy is 98%. For vendor XYZ Ltd (handwritten, irregular layout), accuracy drops to 65%.',
      question: 'What system prompt approach would most improve accuracy for challenging vendors?',
      options: [
        'A) Add more detailed generic extraction rules.',
        'B) Implement vendor-specific extraction profiles: when the vendor is identified, inject vendor-specific extraction instructions (e.g., "For XYZ Ltd invoices: the invoice number is handwritten in the top-right corner, dates are in DD/MM/YYYY format, line items may span multiple lines") that address the specific challenges of each vendor\'s format.',
        'C) Add few-shot examples for each of the 200 vendors.',
        'D) Use a single, extremely detailed extraction rule set for all vendors.',
      ],
      correct: 1,
      explanation: 'Vendor-specific extraction profiles address the root cause — each vendor\'s format has unique challenges that generic rules can\'t capture. Dynamic prompt injection based on vendor identification provides targeted guidance. Option A doesn\'t address vendor-specific challenges. Option C would create an enormous prompt. Option D would be too long and create cross-vendor confusion.',
    },
    {
      id: 6, examScenario: 5, domain: 'd2', domainLabel: 'Context Window Management',
      scenario: 'Your document analysis agent processes 50-page contracts. The agent reads the full document (45,000 tokens) plus system prompt (3,000 tokens) and extracts key terms. For documents over 40 pages, extraction quality degrades — the agent misses clauses in the middle sections while accurately extracting from the beginning and end.',
      question: 'What context management approach would ensure complete extraction from long documents?',
      options: [
        'A) Read only the first and last 10 pages, where most key clauses are typically located.',
        'B) Split the document into 10-page chunks, process each chunk independently with focused extraction instructions, then merge results in a final synthesis pass that identifies cross-referencing clauses and removes duplicates.',
        'C) Increase the model\'s context window to handle longer documents.',
        'D) Summarize each section before extraction to reduce token consumption.',
      ],
      correct: 1,
      explanation: 'Chunked processing ensures each section receives full attention, eliminating the middle-section blind spot. A final synthesis pass handles cross-references and deduplication. This is the standard pattern for documents exceeding effective context utilization. Option A misses middle sections entirely. Option C delays the problem at higher cost. Option D loses the detail needed for accurate extraction.',
    },
    {
      id: 7, examScenario: 5, domain: 'd2', domainLabel: 'Prompt Chaining',
      scenario: 'Your document pipeline chains: OCR → StructuredExtraction → DataEnrichment → Validation. The OCR stage produces text with occasional errors (e.g., "Inv0ice" instead of "Invoice"). The StructuredExtraction stage then fails to categorize these OCR-corrupted terms correctly.',
      question: 'What prompt chaining approach would most effectively handle OCR errors?',
      options: [
        'A) Improve the OCR system to eliminate errors entirely.',
        'B) Add an intermediate normalization stage or enhance the StructuredExtraction prompt with explicit OCR error handling: "Input text may contain OCR errors (e.g., \'0\' for \'O\', \'5\' for \'S\', broken words). Apply fuzzy matching for field labels and correct obvious OCR artifacts before categorization."',
        'C) Have the Validation stage catch and correct OCR errors.',
        'D) Discard documents with OCR errors.',
      ],
      correct: 1,
      explanation: 'Explicit OCR error handling at the extraction stage prevents downstream cascading failures. Instructing the model to apply fuzzy matching for corrupted terms is a pragmatic approach that acknowledges OCR limitations. Option A is ideal but unrealistic. Option C catches errors too late in the pipeline. Option D wastes processing resources on documents that could be handled with fuzzy matching.',
    },
    // ─── D3 ──────────────────────────────────────────────────────────
    {
      id: 8, examScenario: 5, domain: 'd3', domainLabel: 'Tool Selection & Design',
      scenario: 'Your document agent has three tools: extract_text (OCR), extract_table (table detection), and extract_handwriting (handwriting recognition). Claude sometimes calls extract_text on clearly tabular data, producing poorly structured results. The tools\' descriptions are: "Extracts text from documents," "Extracts tables from documents," and "Extracts handwriting from documents."',
      question: 'What tool design improvement would most improve selection accuracy?',
      options: [
        'A) Merge all three tools into a single extract_content tool.',
        'B) Expand descriptions with explicit use cases and boundaries: extract_text → "Extracts continuous text from paragraphs, headers, and prose content. Best for letters, contracts, and narrative documents. NOT for structured data in rows/columns." extract_table → "Extracts tabular data with rows and columns. Use when content is arranged in a grid structure with repeated patterns. Returns structured row/column data." extract_handwriting → "Extracts handwritten content from scanned documents. Use when document contains handwritten annotations, signatures with text, or filled-in forms."',
        'C) Add a routing classifier to pre-select the appropriate tool.',
        'D) Always call all three tools and merge results.',
      ],
      correct: 1,
      explanation: 'Detailed descriptions with positive and negative examples directly address the selection confusion. Claude uses tool descriptions as its primary selection mechanism — when they clearly state when to use (and not use) each tool, selection accuracy improves. Option A creates a poorly scoped tool. Option C over-engineers what better descriptions solve. Option D wastes compute and creates merging complexity.',
    },
    {
      id: 9, examScenario: 5, domain: 'd3', domainLabel: 'Tool Error Handling',
      scenario: 'Your OCR tool returns text with a confidence score per page. Pages with confidence below 0.6 contain garbled text. Your agent processes all pages equally, and the garbled text causes hallucinated extraction results — the model fills in what it thinks the text should say.',
      question: 'How should you handle low-confidence OCR results?',
      options: [
        'A) Process all pages regardless of confidence.',
        'B) When OCR confidence is below 0.6, return a structured result indicating "low confidence — text may be inaccurate" to the extraction agent, with the raw text available but flagged. Instruct the agent to mark extracted fields from low-confidence pages as "unverified" rather than attempting to interpret potentially garbled text.',
        'C) Retry OCR 5 times on low-confidence pages.',
        'D) Discard low-confidence pages entirely.',
      ],
      correct: 1,
      explanation: 'Flagging low-confidence results with explicit instructions prevents the model from confidently hallucinating interpretations of garbled text. Marking fields as "unverified" is honest and enables downstream quality control. Option A allows garbage-in-garbage-out. Option C wastes time on pages where the image quality is the bottleneck, not the OCR engine. Option D loses potentially valuable content.',
    },
    // ─── D4 ──────────────────────────────────────────────────────────
    {
      id: 10, examScenario: 5, domain: 'd4', domainLabel: 'Structured Output Patterns',
      scenario: 'Your extraction agent outputs JSON with fields: vendor, total, date, line_items. You need to handle documents where some fields are absent (no line items on summary invoices) and some have uncertain values (total amount partially obscured). Currently, the agent either omits fields entirely or hallucinates values.',
      question: 'What schema design approach best handles variable completeness?',
      options: [
        'A) Make all fields required and have the agent always fill them.',
        'B) Design the schema with optional fields, a confidence score per field, and a provenance annotation: {value: "$1,234.56", confidence: 0.72, source: "page_3_total_box", status: "extracted" | "inferred" | "missing"}. This enables downstream systems to make informed decisions about data quality.',
        'C) Use a flat text format instead of JSON.',
        'D) Add a validation step that rejects any extraction with missing fields.',
      ],
      correct: 1,
      explanation: 'A rich schema with confidence scores and provenance annotations provides downstream systems with the information to make quality-aware decisions. The status field distinguishes between explicitly found values, inferred values, and missing data — preventing false confidence in extracted data. Option A forces hallucination. Option C loses structure. Option D creates infinite retry loops for documents that genuinely lack certain fields.',
    },
    {
      id: 11, examScenario: 5, domain: 'd4', domainLabel: 'Validation & Retry Strategies',
      scenario: 'Your extraction agent processes 1,000 invoices/day. Validation catches 8% with incorrect totals (line items don\'t sum to the stated total). Retry with error feedback fixes 60% of these. The remaining 40% persist because Claude consistently misreads a specific vendor\'s price format (commas as decimal separators in European format: "1.234,56" instead of "1,234.56").',
      question: 'What strategy would most effectively resolve this persistent format misinterpretation?',
      options: [
        'A) Increase retries to 5 attempts.',
        'B) Fix the root cause: add explicit number format handling instructions to the system prompt for this vendor\'s locale ("European number format: periods as thousands separators, commas as decimal separators"), or implement a preprocessing step that normalizes number formats before extraction.',
        'C) Accept the 3.2% residual error rate.',
        'D) Add a post-processing step that validates arithmetic and corrects totals automatically.',
      ],
      correct: 1,
      explanation: 'When retries consistently fail on a specific pattern (European number format), the fix must address the root cause — the model\'s misinterpretation of the format. Explicit locale-aware instructions or preprocessing normalization are targeted fixes. Option A retries a fundamentally flawed interpretation. Option C accepts preventable errors. Option D fixes the symptom (wrong total) but doesn\'t fix the cause (wrong line item prices).',
    },
    {
      id: 12, examScenario: 5, domain: 'd4', domainLabel: 'Batch Processing',
      scenario: 'Your document processing pipeline handles 10,000 documents/day. Each document requires 3 Claude API calls (extract, validate, enrich) at $0.005 per call, totaling $150/day. You want to reduce costs using the Message Batches API where possible.',
      question: 'For which stage(s) is the Message Batches API appropriate?',
      options: [
        'A) All three stages — batch everything.',
        'B) The initial extraction stage can be batched (documents are processed asynchronously and results queued). Validation and enrichment may require real-time processing depending on whether downstream systems need immediate results. Use batch for extraction, evaluate real-time needs for other stages.',
        'C) Only the validation stage.',
        'D) The Message Batches API is not suitable for document processing.',
      ],
      correct: 1,
      explanation: 'The extraction stage is inherently batchable — documents can be processed asynchronously and queued for downstream handling. The key evaluation for other stages is whether downstream systems require real-time results. Option A ignores potential real-time requirements for validation and enrichment. Option C targets the wrong stage. Option D is overly restrictive — extraction is a clear batch candidate.',
    },
    {
      id: 13, examScenario: 5, domain: 'd4', domainLabel: 'Multi-Instance Review',
      scenario: 'Your document extraction uses dual-instance review: two Claude calls extract data from each document, and discrepancies are flagged for human review. This catches 15% more errors than single-pass but doubles the extraction cost.',
      question: 'What optimization would maintain error detection while reducing cost?',
      options: [
        'A) Remove dual-instance review and accept higher error rates.',
        'B) Apply risk-based dual review: use single-pass extraction for high-confidence document types (structured templates with 95%+ accuracy) and reserve dual-instance review for high-risk or low-accuracy document types. This maintains quality where needed while reducing cost on reliable document types.',
        'C) Use a cheaper model for the second review instance.',
        'D) Run dual review on only 10% of documents as a random sample.',
      ],
      correct: 1,
      explanation: 'Risk-based review allocation focuses quality assurance resources where they add the most value. Documents with historically high accuracy don\'t need dual review, while challenging document types benefit from the extra verification. Option A sacrifices quality. Option C may reduce review effectiveness. Option D leaves 90% of errors undetected.',
    },
    // ─── D5 ──────────────────────────────────────────────────────────
    {
      id: 14, examScenario: 5, domain: 'd5', domainLabel: 'Context Management',
      scenario: 'Your contract analysis agent processes 50-page legal documents. During extraction, it correctly identifies a non-compete clause on page 12. Later, when analyzing termination conditions on page 45, it proposes a termination handling that conflicts with the non-compete clause it already identified — the earlier clause is no longer in its attention window.',
      question: 'What context management pattern would prevent cross-clause contradictions?',
      options: [
        'A) Process each page independently to avoid cross-page influence.',
        'B) Implement a clause registry: as the agent identifies significant clauses, extract them into a structured summary (clause type, page, key terms, constraints) that is injected as persistent context in subsequent extraction turns, ensuring all clauses remain accessible throughout the analysis.',
        'C) Read the entire document twice — once for identification, once for analysis.',
        'D) Limit document length to 20 pages.',
      ],
      correct: 1,
      explanation: 'A clause registry with persistent injection ensures all identified clauses remain accessible regardless of the agent\'s current position in the document. This is the structured knowledge approach — extracting critical information from the stream and making it persistently available. Option A prevents the very cross-referencing that\'s needed. Option C doubles cost without solving the attention problem. Option D artificially limits capability.',
    },
    {
      id: 15, examScenario: 5, domain: 'd5', domainLabel: 'Escalation & Ambiguity',
      scenario: 'Your document agent processes financial statements. It encounters a balance sheet where the total assets don\'t equal total liabilities plus equity — a fundamental accounting inconsistency. The agent\'s confidence in its extraction is 0.82 (high), but the data is internally inconsistent.',
      question: 'What should the agent do when high-confidence extraction produces internally inconsistent data?',
      options: [
        'A) Accept the extraction as correct — the confidence is high.',
        'B) Flag the inconsistency for human review regardless of extraction confidence. Internal consistency checks provide a deterministic validation layer that overrides extraction confidence — if the numbers don\'t balance, either the extraction or the source document has an error that requires human verification.',
        'C) Adjust the numbers to make them balance.',
        'D) Discard the entire document.',
      ],
      correct: 1,
      explanation: 'Internal consistency checks are deterministic validators that operate independently of model confidence. High confidence in extraction doesn\'t guarantee correctness — the model may be confidently wrong. When fundamental rules (assets = liabilities + equity) are violated, human verification is warranted. Option A trusts confidence over logic. Option C falsifies data. Option D is unnecessarily destructive.',
    },
    {
      id: 16, examScenario: 5, domain: 'd5', domainLabel: 'Error Propagation',
      scenario: 'Your document pipeline: IngestAgent → ExtractAgent → ValidateAgent → ExportAgent. The ExtractAgent encounters a document in an unsupported language (Chinese). It returns an empty extraction result marked as successful. ValidateAgent receives empty data and validates it as "all fields empty — passed." ExportAgent exports an empty record to the database.',
      question: 'What error propagation pattern would have prevented this silent failure chain?',
      options: [
        'A) Have ExportAgent validate that records have data before exporting.',
        'B) The ExtractAgent should return a structured error: {is_error: true, error_type: "unsupported_language", language: "zh-CN", resolution: "Route to Chinese language processing pipeline"} — propagating the failure immediately rather than masking it as success.',
        'C) Add a language detection step before extraction.',
        'D) Have ValidateAgent reject records with all empty fields.',
      ],
      correct: 1,
      explanation: 'The root cause is ExtractAgent masking a failure as success. Proper error propagation means failures are surfaced with context at the earliest point. The structured error enables intelligent routing to a language-specific pipeline. Options A and D are downstream mitigations that shouldn\'t be needed if the error is handled at the source. Option C is a good complement but the error handling pattern must still be correct.',
    },
    {
      id: 17, examScenario: 5, domain: 'd5', domainLabel: 'Provenance & Attribution',
      scenario: 'Your legal document analysis agent extracts key contract terms and stores them in a database. A lawyer questions a specific extracted term: "This says the termination notice period is 90 days, but I believe it\'s 30 days." The database record shows the extracted value but not which page, paragraph, or sentence it came from.',
      question: 'What provenance information should be stored with each extracted field?',
      options: [
        'A) Just the extracted value — lawyers can read the original document.',
        'B) For each extracted field, store: the value, the source location (page number, paragraph), the verbatim text from the document, and the confidence score. This enables immediate verification without re-reading the entire document.',
        'C) Store the page number only.',
        'D) Store a link to the full extraction log.',
      ],
      correct: 1,
      explanation: 'Complete provenance — value, source location, verbatim text, and confidence — enables immediate field-level verification. The lawyer can check the verbatim text against the extracted value in seconds rather than re-reading the document. Option A forces manual re-reading. Option C is insufficient — the page alone doesn\'t identify the specific passage. Option D requires searching through logs.',
    },
    {
      id: 18, examScenario: 5, domain: 'd5', domainLabel: 'Human Review & Confidence',
      scenario: 'Your medical record extraction agent processes patient charts with confidence scores. Fields with confidence > 0.85 are auto-verified; below 0.85 are flagged for nurse review. Analysis shows the agent is overconfident on complex medication dosages — 12% of high-confidence extractions have dosage errors that nurses catch during review of other fields.',
      question: 'What does this reveal about confidence-based routing for medical data?',
      options: [
        'A) The confidence threshold should be raised to 0.95.',
        'B) Confidence scores alone are insufficient for high-stakes extraction. Implement domain-specific validation rules (e.g., dosage range checks for each medication, drug interaction checks) that apply regardless of confidence score. These deterministic checks catch errors that confidence scoring misses.',
        'C) Have nurses review all extractions regardless of confidence.',
        'D) Use a different model that produces more accurate confidence scores.',
      ],
      correct: 1,
      explanation: 'For high-stakes domains like medical data, confidence scores are a useful but insufficient quality gate. Deterministic validation rules (dosage ranges, drug interactions) catch errors that even high-confidence extractions miss. This is the principle of combining probabilistic (confidence) and deterministic (rule-based) validation. Option A may help but doesn\'t address the fundamental limitation of confidence scoring. Option C eliminates the efficiency benefit. Option D doesn\'t guarantee better calibration.',
    },
    {
      id: 19, examScenario: 5, domain: 'd5', domainLabel: 'Context Management',
      scenario: 'Your document pipeline processes invoices in batches of 100. Each invoice is an independent Claude API call. The system prompt is 2,800 tokens and is identical for every call. Over 100 invoices, the system prompt consumes 280,000 tokens of context — all conveying the same information.',
      question: 'Is this system prompt cost justified, and what alternative exists?',
      options: [
        'A) 2,800 tokens per call is minimal. No optimization needed.',
        'B) The cost is justified since each call is independent and the system prompt is necessary for each extraction. However, evaluate whether all 2,800 tokens are needed for every invoice — some instructions may only apply to specific invoice types and could be conditionally injected.',
        'C) Move the system prompt to the first call only and reference it in subsequent calls.',
        'D) Use a smaller model that requires fewer system prompt tokens.',
      ],
      correct: 1,
      explanation: 'For independent API calls, the system prompt must be present in each call — there\'s no persistent state. The optimization opportunity is reducing the per-call system prompt size by making vendor-specific instructions conditional. Option A ignores the compounding cost. Option C doesn\'t work — API calls are stateless. Option D reduces capability along with cost.',
    },
    {
      id: 20, examScenario: 5, domain: 'd5', domainLabel: 'Codebase Exploration',
      scenario: 'You\'re using Claude Code to add support for a new document format (PDF/A) to your existing document processing system. The codebase has 35 files across src/processors/, src/extractors/, src/validators/, and src/utils/.',
      question: 'What is the most efficient way to understand where to add the new format support?',
      options: [
        'A) Read all 35 files to understand the complete architecture.',
        'B) Search for existing format handlers (e.g., "pdf", "docx", "png"), read one representative handler to understand the interface pattern, then read the factory/registry that dispatches to handlers. This reveals the pattern and integration point without reading everything.',
        'C) Read only files in src/processors/ since that\'s where format handling happens.',
        'D) Create the new handler from scratch based on your understanding of PDF/A.',
      ],
      correct: 1,
      explanation: 'Finding an existing handler to understand the interface pattern, then locating the dispatch registry, reveals both the implementation pattern and the integration point. This is the standard approach for adding new capabilities to an existing codebase. Option A wastes context. Option C may miss the registry/dispatch logic in other directories. Option D risks creating a handler that doesn\'t conform to the existing architecture.',
    },
  ],
};

export default exam;
