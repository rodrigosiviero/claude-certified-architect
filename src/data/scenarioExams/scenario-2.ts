import type { ScenarioExamData } from './types';

const exam: ScenarioExamData = {
  id: 2,
  title: 'Code Generation with Claude Code',
  description: 'Optimize Claude Code workflows for code generation, review, refactoring, and CI/CD integration.',
  icon: '💻',
  questions: [
    {
      id: 1, examScenario: 2, domain: 'd3', domainLabel: 'Custom Commands & Skills',
      examTask: '3.2 Custom Commands & Skills',
      scenario: 'You want to create a custom /review slash command that runs your team\'s standard code review checklist. This command should be available to every developer when they clone or pull the repository.',
      question: 'Where should you create this command file?',
      options: [
        'A) In ~/.claude/commands/ in each developer\'s home directory for personal access.',
        'B) In .claude/commands/ within the project repository, so it\'s version-controlled and automatically available to all developers when they clone or pull the repo.',
        'C) In the CLAUDE.md file at the project root as a special ## Commands section.',
        'D) In a .claude/config.json file with a commands array defining all custom commands.',
      ],
      correct: 1,
      explanation: 'Project-scoped custom slash commands should be stored in .claude/commands/ within the repository. These commands are version-controlled and automatically available to all developers when they clone or pull. A is for personal commands that aren\'t shared. C is for project instructions, not command definitions. D describes a configuration mechanism that doesn\'t exist in Claude Code. [Task 3.2 — Custom Commands: project-scoped commands in .claude/commands/ ensure team-wide availability.]',
    },
    {
      id: 2, examScenario: 2, domain: 'd3', domainLabel: 'Plan Mode vs Direct Execution',
      examTask: '3.4 Plan Mode vs Direct Execution',
      scenario: 'You\'ve been assigned to restructure the team\'s monolithic application into microservices. This will involve changes across dozens of files and requires decisions about service boundaries and module dependencies.',
      question: 'Which approach should you take?',
      options: [
        'A) Start with direct execution and make changes incrementally, letting the implementation reveal the natural service boundaries.',
        'B) Enter plan mode to explore the codebase, understand dependencies, and design an implementation approach before making changes.',
        'C) Use direct execution with comprehensive upfront instructions detailing exactly how each service should be structured.',
        'D) Begin in direct execution mode and only switch to plan mode if you encounter unexpected complexity during implementation.',
      ],
      correct: 1,
      explanation: 'Plan mode is designed for complex tasks involving large-scale changes, multiple valid approaches, and architectural decisions — exactly what monolith-to-microservices restructuring requires. It enables safe codebase exploration and design before committing to changes. A risks costly rework when dependencies are discovered late. C assumes you already know the right structure without exploring. D ignores that the complexity is already stated in the requirements. [Task 3.4 — Plan Mode: use plan mode for complex architectural tasks that require exploration before implementation.]',
    },
    {
      id: 3, examScenario: 2, domain: 'd3', domainLabel: 'Path-Specific Rules',
      examTask: '3.3 Path-Specific Rules',
      scenario: 'Your codebase has distinct areas with different coding conventions: React components use functional style with hooks, API handlers use async/await with specific error handling, and database models follow a repository pattern. Test files are spread throughout the codebase alongside the code they test (e.g., Button.test.tsx next to Button.tsx).',
      question: 'What\'s the most maintainable way to ensure Claude applies the correct conventions when generating code?',
      options: [
        'A) Consolidate all conventions in the root CLAUDE.md file under headers for each area, relying on Claude to infer which section applies based on the file being edited.',
        'B) Create rule files in .claude/rules/ with YAML frontmatter specifying glob patterns to conditionally apply conventions based on file paths.',
        'C) Create skills in .claude/skills/ for each code type that include the relevant conventions in their SKILL.md files.',
        'D) Place a separate CLAUDE.md file in each subdirectory containing that area\'s specific conventions.',
      ],
      correct: 1,
      explanation: 'Rule files in .claude/rules/ with glob patterns (e.g., **/*.test.tsx) allow conventions to be automatically applied based on file paths regardless of directory location — essential for test files spread throughout the codebase. A relies on inference rather than explicit matching. C requires manual skill invocation or relies on Claude choosing to load them. D can\'t easily handle files spread across many directories. [Task 3.3 — Path-Specific Rules: glob-based rules are the most reliable way to apply conventions to scattered file patterns.]',
    },
    {
      id: 4, examScenario: 2, domain: 'd3', domainLabel: 'CI/CD Integration',
      examTask: '3.6 CI/CD Integration',
      scenario: 'Your pipeline script runs claude "Analyze this pull request for security issues" but the job hangs indefinitely. Logs indicate Claude Code is waiting for interactive input.',
      question: 'What\'s the correct approach to run Claude Code in an automated pipeline?',
      options: [
        'A) Add the -p flag: claude -p "Analyze this pull request for security issues" to run in non-interactive mode.',
        'B) Set the environment variable CLAUDE_HEADLESS=true before running the command.',
        'C) Redirect stdin from /dev/null: claude "Analyze this pull request for security issues" < /dev/null.',
        'D) Add the --batch flag: claude --batch "Analyze this pull request for security issues".',
      ],
      correct: 0,
      explanation: 'The -p (--print) flag is the documented way to run Claude Code in non-interactive mode. It processes the prompt, outputs to stdout, and exits without waiting for user input. B references a non-existent variable. C is a Unix workaround that doesn\'t properly address Claude Code\'s command syntax. D references a non-existent flag. [Task 3.6 — CI/CD: the -p flag is required for automated pipeline usage of Claude Code.]',
    },
    {
      id: 5, examScenario: 2, domain: 'd4', domainLabel: 'Batch Processing',
      examTask: '4.5 Batch Processing Strategies',
      scenario: 'Your team wants to reduce API costs for automated analysis. Currently, real-time Claude calls power two workflows: (1) a blocking pre-merge check that must complete before developers can merge, and (2) a technical debt report generated overnight for review the next morning. Your manager proposes switching both to the Message Batches API for its 50% cost savings.',
      question: 'How should you evaluate this proposal?',
      options: [
        'A) Use batch processing for the technical debt reports only; keep real-time calls for pre-merge checks, since batch processing has no guaranteed latency SLA and developers need results before merging.',
        'B) Switch both workflows to batch processing with status polling to check for early completion.',
        'C) Keep real-time calls for both workflows to avoid batch result ordering issues.',
        'D) Switch both to batch processing with a timeout fallback to real-time if batches take too long.',
      ],
      correct: 0,
      explanation: 'Message Batches offer 50% savings but can take up to 24 hours — unsuitable for blocking pre-merge checks but ideal for overnight reports. B relies on unpredictable completion times for blocking workflows. C reflects a misconception — batch results can be correlated using custom_id fields. D adds unnecessary complexity when the simpler solution is matching each API to its appropriate use case. [Task 4.5 — Batch Processing: match API choice to workflow latency requirements — batch for async, real-time for blocking.]',
    },
    {
      id: 6, examScenario: 2, domain: 'd4', domainLabel: 'Multi-Instance Review',
      examTask: '4.6 Multi-Instance & Multi-Pass Review',
      scenario: 'A pull request modifies 14 files across the stock tracking module. Your single-pass review produces inconsistent results: detailed feedback for some files but superficial comments for others, obvious bugs missed, and contradictory feedback — flagging a pattern as problematic in one file while approving identical code elsewhere.',
      question: 'How should you restructure the review?',
      options: [
        'A) Split into focused passes: analyze each file individually for local issues, then run a separate integration-focused pass examining cross-file data flow.',
        'B) Require developers to split large PRs into smaller submissions of 3-4 files before the automated review runs.',
        'C) Switch to a higher-tier model with a larger context window to give all 14 files adequate attention in one pass.',
        'D) Run three independent review passes on the full PR and only flag issues that appear in at least two of the three runs.',
      ],
      correct: 0,
      explanation: 'Splitting reviews into focused passes directly addresses the root cause: attention dilution when processing many files at once. File-by-file analysis ensures consistent depth, while a separate integration pass catches cross-file issues. B shifts burden to developers without improving the system. C doesn\'t solve attention quality issues with more context. D suppresses real bugs by requiring consensus. [Task 4.6 — Multi-Instance Review: focused passes with integration review prevents attention dilution across many files.]',
    },
    {
      id: 7, examScenario: 2, domain: 'd5', domainLabel: 'Codebase Exploration',
      examTask: '5.4 Large Codebase Exploration',
      scenario: 'You\'re using Claude Code to add input validation to a REST API in a monorepo with 150+ services. The validation logic should be consistent with existing patterns. You start by reading the file where you want to add validation, but Claude doesn\'t know what validation patterns the project uses.',
      question: 'What\'s the most efficient way to discover the project\'s validation conventions?',
      options: [
        'A) Read all files in the src/validators/ directory to understand the validation patterns.',
        'B) Use grep to search for validation-related patterns (e.g., schema.validate, validateInput, z.object) across the codebase, read 2-3 representative examples, then apply the discovered pattern.',
        'C) Ask Claude to infer the validation pattern based on the language and framework being used.',
        'D) Read the project\'s README.md for documentation on coding conventions.',
      ],
      correct: 1,
      explanation: 'Grep for validation patterns followed by reading representative examples discovers the actual conventions used in the codebase — efficient and accurate. A reads potentially many files without knowing which patterns are actually in use. C may produce framework-generic patterns that don\'t match the project\'s specific conventions. D rarely contains the level of detail needed for implementation patterns. [Task 5.4 — Codebase Exploration: grep-driven discovery of patterns followed by targeted reading is the most efficient exploration strategy.]',
    },
    {
      id: 8, examScenario: 2, domain: 'd2', domainLabel: 'Structured Output',
      examTask: '4.3 Structured Output (tool_use + JSON Schema)',
      scenario: 'Your code review agent must return structured feedback: file_path, line_start, line_end, severity (critical|warning|info), category (security|performance|style|logic), and suggestion. Currently, the agent returns free-text reviews that require parsing.',
      question: 'What is the most reliable approach to guarantee structured output?',
      options: [
        'A) Add a system prompt instruction: "Respond with JSON in this format: {file_path, line_start, ...}" and include the schema.',
        'B) Define the schema as a tool with required fields and enums, then use tool_choice to force the model to output via this tool, guaranteeing parseable structured output.',
        'C) Use response_format: {type: "json_object"} and include the schema in the system prompt.',
        'D) Post-process the free-text response with regex extraction to pull out structured fields.',
      ],
      correct: 1,
      explanation: 'tool_use with JSON Schema and tool_choice provides the strongest guarantee of structured output — the model is constrained to produce valid JSON matching the exact schema with correct types and enum values. A and C rely on instruction following for format compliance. D is brittle and unreliable for complex nested structures. [Task 4.3 — Structured Output: tool_use with JSON Schema is the most reliable mechanism for guaranteed structured output.]',
    },
    {
      id: 9, examScenario: 2, domain: 'd3', domainLabel: 'CLAUDE.md Hierarchy',
      examTask: '3.1 CLAUDE.md Hierarchy & Scoping',
      scenario: 'Your project has a root CLAUDE.md with general coding standards. The team uses TypeScript with strict mode for backend code but React with more relaxed settings for frontend. Developers report that Claude applies strict TypeScript patterns to React components, causing unnecessary refactoring suggestions.',
      question: 'How should you configure Claude Code to apply the correct TypeScript settings to each area?',
      options: [
        'A) Add a note in the root CLAUDE.md: "Use strict TypeScript for backend and relaxed settings for frontend."',
        'B) Create rule files in .claude/rules/ with glob patterns: one rule applying to src/backend/** with strict TypeScript conventions, and another applying to src/frontend/** with relaxed React conventions.',
        'C) Create separate CLAUDE.md files in src/backend/ and src/frontend/ directories with the appropriate TypeScript settings.',
        'D) Ask developers to specify the area in each prompt: "Using relaxed React conventions, refactor this component."',
      ],
      correct: 1,
      explanation: 'Rules with glob patterns automatically apply the correct conventions based on file paths — reliable and requiring no developer intervention. A is a single note that may not override the default behavior. C works but rules provide more reliable path-scoped application. D shifts burden to developers and is error-prone. [Task 3.1 — CLAUDE.md Hierarchy: path-specific rules are the most reliable way to apply different conventions to different code areas.]',
    },
    {
      id: 10, examScenario: 2, domain: 'd3', domainLabel: 'Iterative Refinement',
      examTask: '3.5 Iterative Refinement',
      scenario: 'You ask Claude Code to implement a rate limiter. The implementation works but has 3 issues: it doesn\'t handle concurrent requests correctly, it logs sensitive request headers, and it uses a fixed window instead of a sliding window algorithm.',
      question: 'What refinement approach would be most reliable?',
      options: [
        'A) List all three issues in a single follow-up: "Fix concurrent request handling, remove sensitive header logging, and change to a sliding window algorithm."',
        'B) Address one issue at a time: first fix concurrent request handling and verify, then fix header logging and verify, then update the algorithm and verify.',
        'C) Start over with a new prompt that includes all requirements from the beginning.',
        'D) Fix the issues yourself since Claude\'s first attempt missed multiple requirements.',
      ],
      correct: 1,
      explanation: 'One issue at a time with verification between fixes ensures each change is correct and doesn\'t introduce regressions. It\'s easier to verify isolated changes and roll back if needed. A risks partial fixes and regressions from simultaneous changes. C discards working code. D negates the productivity benefit of using Claude Code. [Task 3.5 — Iterative Refinement: addressing one issue at a time with verification is more reliable than batch fixes.]',
    },
    {
      id: 11, examScenario: 2, domain: 'd5', domainLabel: 'Provenance & Uncertainty',
      examTask: '5.6 Information Provenance & Uncertainty',
      scenario: 'Your code documentation agent generates API docs from code comments and type signatures. In 15% of cases, it adds descriptions for parameters that don\'t exist in the actual code — fabricated from the model\'s understanding of what parameters "should" be there.',
      question: 'What approach would prevent fabricated parameter descriptions?',
      options: [
        'A) Add a system prompt instruction: "Only document parameters that actually exist in the code. Do not invent parameters."',
        'B) Use tool-based code reading where the agent must extract parameter lists directly from the source code using AST parsing or grep, then generate descriptions only for the extracted parameters.',
        'C) Add a verification step where a second Claude call compares the generated docs against the source code.',
        'D) Reduce the temperature to 0 to minimize creative additions.',
      ],
      correct: 1,
      explanation: 'Tool-based code reading ensures documentation is grounded in actual code — the agent can only describe parameters it has explicitly read from the source. This prevents fabrication structurally. A relies on instruction following which the data shows fails 15% of the time. C adds cost and latency. D reduces creativity but doesn\'t prevent hallucination when the model lacks correct information. [Task 5.6 — Provenance: tool-based source code reading prevents documentation fabrication structurally.]',
    },
    {
      id: 12, examScenario: 2, domain: 'd1', domainLabel: 'Agentic Loops',
      examTask: '1.1 Agentic Loops',
      scenario: 'Your code review agent runs in an agentic loop: read_files → analyze → generate_feedback. The analyze step uses stop_reason: "tool_use" to read additional files. Occasionally, the agent enters a loop where it keeps reading more files without ever generating feedback — consuming 50K+ tokens across 15+ file reads.',
      question: 'What loop modification would prevent runaway file reading?',
      options: [
        'A) Set max_iterations to 5 to limit the total number of loop iterations.',
        'B) Implement a diminishing-returns check: track the incremental value of each file read (new issues found per file), and when 3 consecutive files produce no new findings, force a transition to the generate_feedback step.',
        'C) Remove the ability to read additional files during the analysis phase.',
        'D) Add a token budget: stop reading files when total context exceeds 20K tokens.',
      ],
      correct: 1,
      explanation: 'A diminishing-returns check detects when additional file reads provide no value and proactively transitions to output generation. A uses an arbitrary iteration limit that may cut off legitimate analysis or allow excessive reads. C removes a valuable capability. D is a reasonable safeguard but doesn\'t address the behavioral issue — the agent should learn when it has enough information. [Task 1.1 — Agentic Loops: convergence detection prevents runaway loops while preserving legitimate deep analysis.]',
    },
    {
      id: 13, examScenario: 2, domain: 'd4', domainLabel: 'Validation & Retry',
      examTask: '4.4 Validation, Retry & Feedback Loops',
      scenario: 'Your code generation agent produces unit tests that compile successfully but fail on 22% of test runs due to incorrect assertions (wrong expected values) and missing mock setup. Simply retrying produces different tests with similar failure rates.',
      question: 'What retry strategy would most effectively improve test correctness?',
      options: [
        'A) Retry with the same prompt up to 3 times and select the test that compiles and passes.',
        'B) Include the test failure output in the retry prompt: "The test \'should calculate total with tax\' failed because expected 110 but got 110.5 (tax rate was applied to the discounted price). Fix the assertion and ensure the mock returns the correct test data."',
        'C) Add the project\'s testing framework documentation to the system prompt.',
        'D) Generate 3 versions of each test and select the one that passes.',
      ],
      correct: 1,
      explanation: 'Including the specific test failure output (expected vs. actual, error message) enables Claude to fix the exact issue. This is far more effective than blind retries or generating multiple versions. A and D rely on randomness rather than informed correction. C is good prevention but doesn\'t help when failures still occur. [Task 4.4 — Validation & Retry: failure-informed retries with specific error details are more effective than blind regeneration.]',
    },
    {
      id: 14, examScenario: 2, domain: 'd5', domainLabel: 'Human Review & Confidence',
      examTask: '5.5 Human Review & Confidence Calibration',
      scenario: 'Your code generation agent proposes database migrations with a self-reported confidence score. Migrations with 90%+ confidence succeed 88% of the time. Migrations with 70-80% confidence succeed 72% of the time. A failed migration requires 2-4 hours of manual recovery.',
      question: 'What review policy is appropriate given these failure costs?',
      options: [
        'A) Allow all migrations to execute autonomously since the success rate is above 70%.',
        'B) Require human review and confirmation for all database migrations regardless of confidence, given the 2-4 hour recovery cost of failures.',
        'C) Require human review only for migrations with confidence below 80%.',
        'D) Run all migrations in a staging environment first, then promote automatically if staging succeeds.',
      ],
      correct: 1,
      explanation: 'The recovery cost (2-4 hours) justifies human review for all database migrations — even the highest confidence tier fails 12% of the time, and each failure is expensive. The principle is: blast radius should determine review requirements, not confidence scores. A accepts unacceptable risk. C allows 12% of high-confidence migrations to fail autonomously. D is a good complement but staging doesn\'t guarantee production safety. [Task 5.5 — Human Review: review requirements should be based on failure cost, not confidence scores.]',
    },
    {
      id: 15, examScenario: 2, domain: 'd2', domainLabel: 'Tool Distribution & tool_choice',
      examTask: '2.3 Tool Distribution & tool_choice',
      scenario: 'Your code generation agent has access to file_write, shell_exec, and git_commit tools. In 5% of cases, the agent calls git_commit before verifying that the code compiles — committing broken code to the repository.',
      question: 'What tool_choice configuration would prevent premature commits?',
      options: [
        'A) Set tool_choice to "auto" and add a system prompt instruction to always verify compilation before committing.',
        'B) Implement a tool gating mechanism: git_commit is only available after shell_exec with a compile command returns success. The orchestration layer hides git_commit from the available tools until compilation is verified.',
        'C) Remove git_commit from the tool catalog and require manual commits from developers.',
        'D) Set tool_choice to "required" to ensure the agent always calls tools rather than skipping steps.',
      ],
      correct: 1,
      explanation: 'Tool gating prevents premature commits by making git_commit physically unavailable until compilation succeeds. This provides deterministic enforcement that instructions cannot. A relies on probabilistic compliance. C removes automation entirely. D forces tool use but doesn\'t control which tools are used or in what order. [Task 2.3 — Tool Distribution: tool gating based on prerequisite conditions prevents premature actions deterministically.]',
    },
    {
      id: 16, examScenario: 2, domain: 'd5', domainLabel: 'Context Management',
      examTask: '5.1 Conversation Context Management',
      scenario: 'You\'re working with Claude Code on a multi-file refactoring. After editing 8 files, Claude begins losing track of earlier changes — suggesting modifications that conflict with edits it made 4 files ago. The conversation context is 32K tokens.',
      question: 'What approach would maintain consistency across the refactoring?',
      options: [
        'A) Start a new session for every 4 files to keep context fresh.',
        'B) Maintain a structured change log in the context: after each file edit, append a summary of changes (file, what changed, why) to a running list that\'s injected at the start of each agent turn.',
        'C) Increase the context window to accommodate the full refactoring.',
        'D) Use plan mode to design all changes upfront, then implement them in direct execution mode.',
      ],
      correct: 1,
      explanation: 'A structured change log injected at each turn keeps earlier changes visible regardless of context length. This is more reliable than depending on the model to track changes across a growing context. A loses all accumulated context. C delays the problem. D helps with planning but doesn\'t prevent inconsistency during implementation. [Task 5.1 — Context Management: a structured change log prevents conflicting edits in multi-file refactoring.]',
    },
    {
      id: 17, examScenario: 2, domain: 'd1', domainLabel: 'Workflow Enforcement',
      examTask: '1.4 Workflow Enforcement & Structured Handoffs',
      scenario: 'Your code generation workflow requires: generate_code → run_linter → run_tests → commit. The agent sometimes skips the linter step (step 2) and runs tests directly, allowing style violations into the codebase.',
      question: 'What enforcement mechanism would ensure all steps execute in order?',
      options: [
        'A) Add numbered step instructions to the system prompt: "Step 1: Generate code. Step 2: Run linter. Step 3: Run tests. Step 4: Commit. Complete all steps in order."',
        'B) Implement a pipeline executor that controls step execution: each step must return a success status before the next step\'s tools become available. The agent cannot access run_tests tools until run_linter reports completion.',
        'C) Add a post-processing validator that checks if the linter was run before allowing commits.',
        'D) Combine the linter and test steps into a single step that runs both.',
      ],
      correct: 1,
      explanation: 'A pipeline executor with tool gating provides deterministic step ordering — the agent physically cannot skip steps because subsequent tools aren\'t available until prerequisites complete. A relies on instruction following. C detects violations after they happen. D may work but doesn\'t enforce the order — the agent could still skip the combined step. [Task 1.4 — Workflow Enforcement: programmatic pipelines with tool gating ensure step ordering deterministically.]',
    },
    {
      id: 18, examScenario: 2, domain: 'd2', domainLabel: 'MCP Server Integration',
      examTask: '2.4 MCP Server Integration',
      scenario: 'Your Claude Code agent connects to an MCP server providing database access tools. The MCP server takes 3 seconds to initialize its connection pool. Currently, your agent connects to the MCP server at startup even when the user\'s task doesn\'t involve database operations.',
      question: 'What connection pattern would optimize startup time?',
      options: [
        'A) Connect at startup to have tools ready when needed.',
        'B) Implement lazy connection: defer MCP server connection until the agent determines database tools are needed for the current task, then connect on-demand and cache the connection for the session.',
        'C) Pre-warm the MCP connection in a background process at Claude Code startup.',
        'D) Keep a persistent MCP connection open across sessions.',
      ],
      correct: 1,
      explanation: 'Lazy on-demand connection eliminates the 3-second startup penalty for tasks that don\'t need database tools, while caching ensures no repeated connection overhead within a session. A adds unnecessary startup latency. C adds complexity for a marginal improvement. D may leak resources across sessions and isn\'t always feasible. [Task 2.4 — MCP Server Integration: lazy on-demand connection optimizes startup time while maintaining availability.]',
    },
    {
      id: 19, examScenario: 2, domain: 'd5', domainLabel: 'Error Propagation',
      examTask: '5.3 Error Propagation Strategies',
      scenario: 'Your code generation agent calls a linter tool that returns: "Error on line 42." The agent attempts to fix line 42, but the linter returns the same error — because the actual issue is on line 43 (the linter\'s line numbers are 0-indexed but the agent assumes 1-indexed).',
      question: 'What error propagation improvement would prevent this systematic misinterpretation?',
      options: [
        'A) Always add 1 to line numbers reported by the linter.',
        'B) Include the file content and the specific line in the error response: {error: "Unexpected token", file: "auth.ts", line: 42, column: 15, context: "  const user = getUsr(id);"}. This enables the agent to verify the actual problematic line.',
        'C) Use a different linter that reports more accurate line numbers.',
        'D) Add a system prompt instruction: "When fixing linter errors, check both the reported line and the adjacent lines."',
      ],
      correct: 1,
      explanation: 'Including file content and the specific line in the error response enables the agent to verify the actual issue rather than blindly trusting line numbers. The context line shows the exact code, making the error unambiguous. A assumes a fixed offset that may not always be correct. C avoids the problem rather than handling it. D is a reasonable workaround but less reliable than providing actual context. [Task 5.3 — Error Propagation: including source context in error responses prevents systematic misinterpretation.]',
    },
    {
      id: 20, examScenario: 2, domain: 'd1', domainLabel: 'Subagent Invocation',
      examTask: '1.3 Subagent Invocation & Context Isolation',
      scenario: 'Your code review system spawns a subagent for each file in a PR. The parent agent passes its full analysis guidelines (3,000 tokens) to every subagent. A PR with 22 files generates 66,000 tokens of repeated guideline context alone.',
      question: 'How can you reduce the context overhead while maintaining review quality?',
      options: [
        'A) Pass the full guidelines to the first subagent and only file-specific context to subsequent subagents.',
        'B) Condense the review guidelines into a 500-token summary for each subagent, and have the parent agent perform a final integration review with the full guidelines to catch anything the condensed version missed.',
        'C) Process all files in a single agent call instead of spawning subagents.',
        'D) Remove the review guidelines entirely and rely on Claude\'s built-in code review capabilities.',
      ],
      correct: 1,
      explanation: 'Condensed guidelines for subagents + full-guideline integration review by the parent reduces per-subagent overhead from 3,000 to 500 tokens (83% reduction) while preserving review quality through the parent\'s final check. A produces inconsistent reviews across files. C may hit context limits with 22 files. D loses project-specific review criteria. [Task 1.3 — Subagent Invocation: condensed context for subagents with full-context parent integration balances quality and efficiency.]',
    },
  ],
};

export default exam;
