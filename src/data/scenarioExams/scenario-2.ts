import type { ScenarioExamData } from './types';

const exam: ScenarioExamData = {
  id: 2,
  title: 'Code Generation with Claude Code',
  description: 'Design and optimize Claude Code workflows for code generation, review, refactoring, and CI/CD integration.',
  icon: '💻',
  questions: [
    // ─── D1 — Agentic Architecture ──────────────────────────────────
    {
      id: 1, examScenario: 2, domain: 'd1', domainLabel: 'Agentic Loops',
      scenario: 'You\'re using Claude Code to implement a new feature across 8 files. Claude enters plan mode, explores the codebase, and proposes an implementation approach. After you approve the plan, Claude begins implementation but encounters an unexpected dependency: the new feature requires changes to a shared utility that 12 other modules import. Claude pauses and asks for guidance.',
      question: 'What is the most appropriate action at this point in the agentic workflow?',
      options: [
        'A) Tell Claude to proceed with the original plan and ignore the shared utility dependency.',
        'B) Have Claude re-enter plan mode to reassess the implementation approach given the new dependency information, update the plan, and get your approval before continuing.',
        'C) Abort the implementation and start over with a completely different approach.',
        'D) Tell Claude to create a new shared utility file rather than modifying the existing one.',
      ],
      correct: 1,
      explanation: 'When significant new information emerges during implementation (a shared dependency affecting 12 modules), re-planning is the appropriate response. The plan-fix-verify agentic pattern includes this feedback loop explicitly. Option A risks breaking 12 existing modules. Option C is premature — the original plan may be sound with modifications. Option D may create unnecessary duplication rather than properly handling the shared dependency.',
    },
    {
      id: 2, examScenario: 2, domain: 'd1', domainLabel: 'Tool Use Flow',
      scenario: 'Claude Code reads a file, edits it, then runs the test suite. The test run produces 3 failing tests. Claude reads the test output, analyzes the failures, edits the source file to fix them, and runs tests again. This observe-fix-verify cycle repeats 4 times before all tests pass. Total time: 3 minutes.',
      question: 'This iterative pattern is an example of what agentic design principle?',
      options: [
        'A) MapReduce — dividing the problem into independent subtasks processed in parallel.',
        'B) Observe-Orient-Decide-Act (OODA) loop — each iteration observes the current state (test output), orients by analyzing failures, decides on a fix, and acts by editing the file.',
        'C) Chain-of-thought reasoning — generating explanations for each step before acting.',
        'D) State machine — following a predetermined sequence of steps regardless of outcomes.',
      ],
      correct: 1,
      explanation: 'The OODA loop pattern describes iterative refinement where each cycle observes the current state, interprets the situation, decides on action, and executes. This is the fundamental pattern for code-fixing agents. Option A describes parallel processing, not iterative refinement. Option C is about reasoning visibility, not the iterative action pattern. Option D is the opposite — a state machine follows predetermined paths, while the OODA loop adapts based on observations.',
    },
    {
      id: 3, examScenario: 2, domain: 'd1', domainLabel: 'Multi-Agent Orchestration',
      scenario: 'Your development pipeline uses two agents: CoderAgent writes code from specifications, and ReviewerAgent reviews the output for bugs and style compliance. The CoderAgent produces code, the ReviewerAgent identifies issues, and the code goes back to CoderAgent with feedback. This loop runs up to 3 times. After 3 iterations, 22% of code still has unresolved issues.',
      question: 'What is the most effective improvement to reduce the residual issue rate?',
      options: [
        'A) Increase the loop to 5 iterations to give more chances for convergence.',
        'B) Feed the ReviewerAgent\'s most common feedback patterns directly into the CoderAgent\'s system prompt as explicit coding guidelines, preventing the issues before they occur rather than catching them after.',
        'C) Remove the ReviewerAgent and rely solely on automated tests to catch issues.',
        'D) Run the CoderAgent and ReviewerAgent in parallel and merge their outputs.',
      ],
      correct: 1,
      explanation: 'Propagating common review feedback upstream as coding guidelines prevents issues at the source. This is the feedforward pattern — rather than iterating on fixes, prevent the common mistakes. Options A addresses the symptom with more compute. Option C removes a valuable safety layer. Option D doesn\'t make sense for a generation-review pipeline.',
    },
    {
      id: 4, examScenario: 2, domain: 'd1', domainLabel: 'Agentic Loops',
      scenario: 'You\'re using Claude Code in a CI pipeline to review pull requests. The pipeline script runs: claude "Review this pull request for bugs and security issues." The job hangs indefinitely — logs show Claude Code is waiting for interactive user input to confirm an action.',
      question: 'What flag should you add to run Claude Code in non-interactive mode for automated pipelines?',
      options: [
        'A) claude --batch "Review this pull request for bugs and security issues"',
        'B) claude -p "Review this pull request for bugs and security issues"',
        'C) claude --headless "Review this pull request for bugs and security issues"',
        'D) claude --no-input "Review this pull request for bugs and security issues"',
      ],
      correct: 1,
      explanation: 'The -p (or --print) flag runs Claude Code in non-interactive mode — it processes the prompt, outputs the result to stdout, and exits without waiting for user input. This is the documented way to use Claude Code in CI/CD pipelines. Options A, C, and D reference flags that don\'t exist in Claude Code\'s CLI.',
    },
    // ─── D2 — Prompt Engineering ─────────────────────────────────────
    {
      id: 5, examScenario: 2, domain: 'd2', domainLabel: 'System Prompt Design',
      scenario: 'You want Claude Code to follow your team\'s coding conventions when generating code. Your project uses functional React components with named exports, async/await for API calls, and a specific error handling pattern. You currently have these conventions documented in a README.md file, but Claude Code doesn\'t consistently follow them.',
      question: 'Where should you configure project-specific coding conventions for Claude Code?',
      options: [
        'A) In a CLAUDE.md file at the project root, which Claude Code automatically reads for project context and instructions.',
        'B) In each developer\'s ~/.claude/config.json file.',
        'C) As environment variables prefixed with CLAUDE_ that Claude Code reads at startup.',
        'D) In the README.md file with a special <!-- CLAUDE --> comment section.',
      ],
      correct: 0,
      explanation: 'CLAUDE.md at the project root is the documented location for project-specific instructions that Claude Code automatically reads. It\'s version-controlled and available to all developers who clone the repository. Option B is for personal settings, not shared project conventions. Option C references a mechanism that doesn\'t exist. Option D uses a convention that Claude Code doesn\'t recognize.',
    },
    {
      id: 6, examScenario: 2, domain: 'd2', domainLabel: 'Context Window Management',
      scenario: 'Claude Code is refactoring a 2,000-line legacy authentication module. As it reads and edits the file, the context window fills up. By the time Claude reaches the middleware functions at line 1,800, it has lost track of the new helper functions it created at line 200, and starts duplicating functionality.',
      question: 'What context management approach would most effectively prevent this?',
      options: [
        'A) Tell Claude Code to split the file into smaller modules first, then refactor each module independently, maintaining clear context boundaries.',
        'B) Add a system prompt instruction: "Remember all functions you create throughout the refactoring process."',
        'C) Process the file from bottom to top to keep earlier sections fresh in context.',
        'D) Increase the context window size by using a larger model.',
      ],
      correct: 0,
      explanation: 'Splitting the large file into smaller modules before refactoring creates natural context boundaries. Each module can be refactored independently with full context attention, and the interfaces between modules are explicitly defined. Option B is advisory and doesn\'t address the fundamental context limitation. Option C is a workaround that doesn\'t solve the problem for files that genuinely exceed effective context utilization. Option D delays the problem at higher cost.',
    },
    {
      id: 7, examScenario: 2, domain: 'd2', domainLabel: 'Prompt Chaining',
      scenario: 'Your code generation pipeline chains three steps: (1) SpecParser extracts requirements from user stories, (2) CodeGenerator writes the implementation, (3) TestWriter generates unit tests. CodeGenerator occasionally produces code that doesn\'t match the specification — for example, creating a REST endpoint when the spec asks for a GraphQL resolver.',
      question: 'At which stage should you intervene to prevent specification mismatches?',
      options: [
        'A) At stage 3 (TestWriter) — have tests validate that the code matches the original specification.',
        'B) At stage 2 (CodeGenerator) — include the original specification as verbatim context in the CodeGenerator\'s prompt, not just the parsed requirements, so it can cross-reference its output against the actual spec.',
        'C) At stage 1 (SpecParser) — improve the parsing to capture more detail.',
        'D) Add a stage 4 (Verifier) that compares the generated code against the original specification.',
      ],
      correct: 1,
      explanation: 'Including the original specification as verbatim context gives CodeGenerator the richest possible signal for what to build. Parsed requirements may lose nuance (like the distinction between REST and GraphQL). The original spec preserves all detail. Option A catches errors after code is written — better to prevent them. Option C is helpful but doesn\'t address the core issue of CodeGenerator not seeing the full spec. Option D adds cost and latency when the fix is simpler.',
    },
    // ─── D3 — Tool Design ────────────────────────────────────────────
    {
      id: 8, examScenario: 2, domain: 'd3', domainLabel: 'Tool Selection & Design',
      scenario: 'You create a custom /review slash command for Claude Code that runs your team\'s standard code review checklist. You want this command to be available to every developer when they clone or pull the repository.',
      question: 'Where should you create this command file?',
      options: [
        'A) In the .claude/commands/ directory in the project repository, so it\'s version-controlled and automatically available to all developers.',
        'B) In ~/.claude/commands/ in each developer\'s home directory for personal use.',
        'C) In the CLAUDE.md file at the project root under a "## Commands" section.',
        'D) In a .claude/config.json file with a commands array.',
      ],
      correct: 0,
      explanation: 'Project-scoped custom slash commands go in .claude/commands/ within the repository. These are version-controlled and automatically available to all developers when they clone or pull the repo. Option B is for personal commands that aren\'t shared. Option C is for project instructions, not command definitions. Option D references a configuration mechanism that doesn\'t exist in Claude Code.',
    },
    {
      id: 9, examScenario: 2, domain: 'd3', domainLabel: 'API Integration Patterns',
      scenario: 'Your Claude Code agent needs to interact with your company\'s internal API during code generation — for example, fetching the latest API schema before generating client code. The internal API requires authentication and returns data in a proprietary format.',
      question: 'What integration pattern would most cleanly handle this in a Claude Code workflow?',
      options: [
        'A) Hardcode the API credentials and format details directly in the CLAUDE.md file.',
        'B) Create a custom slash command that wraps the API call, handles authentication via environment variables, transforms the response to a clean format, and presents the result to Claude Code as structured context.',
        'C) Give Claude Code direct network access to the internal API and let it handle the authentication and parsing.',
        'D) Manually fetch the API schema and paste it into each Claude Code session.',
      ],
      correct: 1,
      explanation: 'A custom slash command encapsulates the integration complexity — authentication, format transformation, and error handling — behind a simple interface. Claude Code gets clean, structured data without needing to understand the API\'s proprietary format. Option A exposes credentials in a version-controlled file. Option C gives Claude Code unnecessary low-level responsibilities. Option D is manual and doesn\'t scale.',
    },
    // ─── D4 — Output Structuring ─────────────────────────────────────
    {
      id: 10, examScenario: 2, domain: 'd4', domainLabel: 'System Prompt Architecture',
      scenario: 'Your code generation agent produces Python functions. 85% of the time, the output runs correctly on first attempt. The other 15% fails due to import errors, incorrect function signatures, or missing type hints. You want to improve the first-attempt success rate.',
      question: 'What system prompt improvement would most effectively increase first-attempt correctness?',
      options: [
        'A) Add explicit instructions to always include type hints, handle all imports, and verify function signatures against any provided interface definitions before generating code.',
        'B) Increase the model temperature to encourage more diverse code solutions.',
        'C) Add a post-generation compilation step that catches syntax errors.',
        'D) Generate code in a simpler language like pseudocode first, then translate to Python.',
      ],
      correct: 0,
      explanation: 'Explicit instructions addressing the specific failure modes (missing imports, incorrect signatures, absent type hints) directly target the 15% error patterns. When the failure modes are known and specific, targeted prompt improvements are the most efficient fix. Option B adds randomness that reduces reliability. Option C catches syntax errors but not semantic issues like incorrect signatures. Option D adds unnecessary translation overhead.',
    },
    {
      id: 11, examScenario: 2, domain: 'd4', domainLabel: 'Validation & Retry Strategies',
      scenario: 'Claude Code generates a Python module with 4 functions. Your test suite has 12 tests for this module. After running the tests, 3 fail. Claude reads the test output, identifies the issue, and fixes the code. On re-run, 2 of the 3 previously passing tests now fail — the fix introduced a regression.',
      question: 'What validation strategy would most effectively prevent regression during fixes?',
      options: [
        'A) Run only the previously failing tests to verify the fix, then accept the result.',
        'B) Run the full test suite after every code change, not just the failing tests. This ensures fixes don\'t introduce regressions in previously passing tests.',
        'C) Run tests in a random order to catch order-dependent test failures.',
        'D) Have Claude generate new tests for the fix before running any tests.',
      ],
      correct: 1,
      explanation: 'Running the full test suite after every change is the standard regression prevention strategy. It catches unintended side effects of fixes. This is a fundamental principle of automated testing. Option A is exactly the mistake that allowed the regression. Option C addresses a different problem (test order dependency). Option D adds unnecessary work without preventing the regression.',
    },
    {
      id: 12, examScenario: 2, domain: 'd4', domainLabel: 'Batch Processing',
      scenario: 'Your team uses Claude Code for automated code reviews on pull requests. Currently, each PR review costs $0.08 via real-time API calls. Your manager proposes switching all reviews to the Message Batches API for 50% cost savings. However, PR reviews are a blocking pre-merge check — developers wait for the review before merging.',
      question: 'How should you evaluate this proposal?',
      options: [
        'A) Switch all PR reviews to the Message Batches API — the 50% savings justify the potential wait time.',
        'B) Keep real-time API calls for PR reviews since they are a blocking workflow where developers wait for results. Use the Message Batches API for non-blocking batch jobs like overnight code quality reports.',
        'C) Switch to batch processing with a 1-hour timeout fallback to real-time calls.',
        'D) Switch to batch processing and have developers check back later for review results.',
      ],
      correct: 1,
      explanation: 'The Message Batches API has processing times up to 24 hours with no guaranteed latency SLA. This makes it unsuitable for blocking pre-merge checks where developers wait for results. The correct approach is matching the API to the use case — real-time for blocking workflows, batch for background jobs. Option A ignores the blocking nature of PR reviews. Option C adds unnecessary complexity. Option D degrades developer experience.',
    },
    {
      id: 13, examScenario: 2, domain: 'd4', domainLabel: 'Multi-Instance Review',
      scenario: 'A pull request modifies 14 files across a stock tracking module. Your single-pass Claude Code review produces inconsistent results: detailed feedback for some files but superficial comments for others, obvious bugs missed, and contradictory feedback — flagging a pattern as problematic in one file while approving identical code in another.',
      question: 'How should you restructure the automated review?',
      options: [
        'A) Require developers to split large PRs into 3-4 file submissions before automated review.',
        'B) Split into focused passes: analyze each file individually for local issues, then run a separate integration-focused pass examining cross-file data flow and consistency.',
        'C) Switch to a higher-tier model with a larger context window.',
        'D) Run three independent review passes and only flag issues that appear in at least two.',
      ],
      correct: 1,
      explanation: 'Splitting reviews into focused passes addresses the root cause — attention dilution when processing many files simultaneously. File-by-file analysis ensures consistent depth, while a separate integration pass catches cross-file issues. Option B from the official exam. Option A shifts burden to developers. Option C misunderstands that larger context doesn\'t solve attention quality issues. Option D suppresses real findings by requiring consensus.',
    },
    // ─── D5 — Reliability ────────────────────────────────────────────
    {
      id: 14, examScenario: 2, domain: 'd5', domainLabel: 'Context Management',
      scenario: 'Claude Code is refactoring a codebase and has made changes across 10 files over 25 turns. By turn 20, it starts reverting changes it made in earlier turns — forgetting that it already migrated a utility function to a new location and recreating it in the old location.',
      question: 'What context management strategy would most effectively prevent this?',
      options: [
        'A) Maintain a structured change log that Claude Code updates after each modification, injected as persistent context in every turn, tracking what was changed, where, and why.',
        'B) Add a system prompt instruction: "Keep track of all changes you make throughout the session."',
        'C) Limit refactoring sessions to 10 turns to prevent context overflow.',
        'D) Commit changes after every file modification to create a persistent external state.',
      ],
      correct: 0,
      explanation: 'A structured change log with persistent injection ensures Claude Code has a definitive record of all modifications, regardless of conversation length. This is deterministic — it doesn\'t rely on attention to earlier turns. Option B is advisory and contradicted by the observed behavior. Option C artificially limits the scope of work. Option D creates excessive commits and still doesn\'t prevent the re-creation of already-moved code.',
    },
    {
      id: 15, examScenario: 2, domain: 'd5', domainLabel: 'Codebase Exploration',
      scenario: 'You\'ve been assigned to restructure a monolithic application into microservices. This involves changes across dozens of files and requires decisions about service boundaries and module dependencies. You\'re deciding between plan mode and direct execution.',
      question: 'Which approach should you take for this complex refactoring task?',
      options: [
        'A) Use direct execution and make changes incrementally, letting the implementation reveal natural service boundaries.',
        'B) Enter plan mode to explore the codebase, understand dependencies, and design an implementation approach before making any changes.',
        'C) Use direct execution with comprehensive upfront instructions detailing exactly how each service should be structured.',
        'D) Begin in direct execution mode and only switch to plan mode if unexpected complexity is encountered.',
      ],
      correct: 1,
      explanation: 'Plan mode is designed for complex tasks involving large-scale changes, multiple valid approaches, and architectural decisions — exactly what monolith-to-microservices restructuring requires. It enables safe codebase exploration and design before committing to changes. Option A risks costly rework when dependencies are discovered late. Option C assumes you already know the right structure without exploring. Option D ignores that the complexity is already stated in the requirements.',
    },
    {
      id: 16, examScenario: 2, domain: 'd5', domainLabel: 'Escalation & Ambiguity',
      scenario: 'Claude Code encounters an ambiguous API specification while generating client code — the spec has conflicting endpoint definitions in two different documentation files. Claude proceeds with one interpretation without asking for clarification, and the generated code works for one endpoint but fails for the other.',
      question: 'What behavior would most effectively handle specification ambiguity?',
      options: [
        'A) Claude Code should always pick the most recent specification and proceed.',
        'B) Claude Code should detect the ambiguity, present both conflicting definitions to the developer with a specific question, and wait for clarification before generating code that depends on the ambiguous specification.',
        'C) Claude Code should generate code for both interpretations and let the developer choose.',
        'D) Claude Code should generate code for the more common endpoint pattern and note the discrepancy.',
      ],
      correct: 1,
      explanation: 'When specification ambiguity could lead to incorrect code, the safest behavior is to surface the ambiguity to the developer with a specific question rather than guessing. This is especially important for API client code where incorrect endpoints cause runtime failures. Option A may pick the wrong spec. Option C doubles maintenance burden. Option D produces code that partially works, which is worse than code that clearly indicates it needs input.',
    },
    {
      id: 17, examScenario: 2, domain: 'd5', domainLabel: 'Error Propagation',
      scenario: 'During an automated code generation task, Claude Code encounters a compilation error. The error message from the compiler is: "TypeError: Cannot read property \'map\' of undefined at line 42." Claude Code\'s error handling currently logs the error and continues to the next file, leaving the broken code in place.',
      question: 'What error propagation pattern would most effectively handle compilation errors during code generation?',
      options: [
        'A) Propagate the error to the developer immediately and stop generation, waiting for manual fix.',
        'B) Return structured error context to the agentic loop including the error type, the specific line, the relevant code context, and the expected data structure, allowing Claude to reason about the fix and attempt a correction within the same session.',
        'C) Catch the error and skip the file, continuing with other files.',
        'D) Automatically revert all changes made in the current session when any compilation error occurs.',
      ],
      correct: 1,
      explanation: 'Structured error context with surrounding code allows Claude to reason about the fix — the error is specific enough (accessing .map on undefined) that Claude can likely fix it. The agentic loop\'s observe-fix-verify pattern handles exactly this case. Option A is overly cautious — many compilation errors are straightforward to fix. Option C leaves broken code in place. Option D is unnecessarily destructive, discarding all progress.',
    },
    {
      id: 18, examScenario: 2, domain: 'd5', domainLabel: 'Provenance & Attribution',
      scenario: 'Claude Code generates a security patch that modifies 5 files. Your security team requires that all automated code changes include provenance metadata — who/what generated the change, why, and what it fixes — for audit compliance.',
      question: 'What provenance approach would most effectively meet this requirement?',
      options: [
        'A) Add a comment header to each modified file: "Auto-generated by Claude Code" with the date.',
        'B) Include structured metadata in each commit or change record: the agent that made the change (Claude Code), the triggering event (security patch for CVE-2024-XXXX), the files modified, and the reasoning for each change, stored in a machine-readable format alongside the code changes.',
        'C) Store provenance information in a separate log file that tracks all Claude Code sessions.',
        'D) Rely on git blame to show which files were modified by automated processes.',
      ],
      correct: 1,
      explanation: 'Structured metadata with each change record provides complete provenance — the agent, trigger, files, and reasoning — in a machine-readable format that supports automated audit compliance. Option A is too minimal for compliance requirements. Option C separates provenance from the actual changes, making correlation harder. Option D only shows who changed what, not why or under what authority.',
    },
    {
      id: 19, examScenario: 2, domain: 'd5', domainLabel: 'Human Review & Confidence',
      scenario: 'Your CI pipeline uses Claude Code to generate security fixes. Claude generates a patch with 0.82 confidence. The patch modifies authentication middleware. Your security policy requires human review for all authentication-related changes, regardless of confidence score.',
      question: 'Should this patch proceed autonomously, and what principle applies?',
      options: [
        'A) Yes — the 0.82 confidence exceeds the 0.7 threshold for autonomous processing.',
        'B) No — the domain-based override principle applies: when changes affect high-stakes domains (authentication, payment processing, data access), domain-specific review policies override confidence thresholds regardless of the score.',
        'C) Yes, but add a post-deployment verification step to catch any issues.',
        'D) Let the confidence score decide — if it\'s above 0.8, it\'s safe to deploy.',
      ],
      correct: 1,
      explanation: 'Domain-based overrides are essential for high-stakes code paths. Authentication middleware changes have disproportionate impact — a bug could compromise all users. Confidence scores don\'t capture domain-specific risk. Option A and D ignore the domain policy. Option C accepts risk and adds a reactive rather than preventive measure.',
    },
    {
      id: 20, examScenario: 2, domain: 'd5', domainLabel: 'Codebase Exploration',
      scenario: 'Your codebase has distinct areas with different coding conventions: React components use functional style with hooks, API handlers use async/await with specific error handling, and database models follow a repository pattern. Test files are spread throughout the codebase alongside the code they test. You want Claude to automatically apply the correct conventions when editing files.',
      question: 'What\'s the most maintainable way to ensure Claude applies the correct conventions?',
      options: [
        'A) Consolidate all conventions in the root CLAUDE.md under headers for each area.',
        'B) Create rule files in .claude/rules/ with YAML frontmatter specifying glob patterns to conditionally apply conventions based on file paths (e.g., **/*.test.tsx triggers test conventions; **/api/**/*.ts triggers API handler conventions).',
        'C) Create skills in .claude/skills/ for each code type with conventions in their SKILL.md files.',
        'D) Place a separate CLAUDE.md file in each subdirectory with that area\'s conventions.',
      ],
      correct: 1,
      explanation: 'Rule files with glob patterns automatically apply conventions based on file paths, regardless of directory structure. This is essential for test files that are co-located with source files — the glob pattern **/*.test.tsx matches test files everywhere. Option A relies on inference rather than explicit matching. Option C requires manual invocation or relies on Claude choosing to load them. Option D can\'t handle files spread across many directories.',
    },
  ],
};

export default exam;
