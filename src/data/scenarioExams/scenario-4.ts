import type { ScenarioExamData } from './types';

const exam: ScenarioExamData = {
  id: 4,
  title: 'DevOps & CI/CD Pipeline Agent',
  description: 'Design and optimize Claude-powered agents for deployment automation, monitoring, incident response, and infrastructure management.',
  icon: '🛠️',
  questions: [
    // ─── D1 ──────────────────────────────────────────────────────────
    {
      id: 1, examScenario: 4, domain: 'd1', domainLabel: 'Agentic Loops',
      scenario: 'Your deployment agent uses an agentic loop to manage releases: check tests, build artifacts, deploy to staging, run smoke tests, deploy to production. On 8% of deployments, the loop exceeds 12 iterations because the smoke test fails intermittently — passing on retry but failing again on the next deployment step.',
      question: 'What is the most effective way to handle intermittent smoke test failures in the deployment loop?',
      options: [
        'A) Set max_iterations to 20 to accommodate retries.',
        'B) Implement a retry counter specific to smoke tests: allow up to 3 retries per step with a backoff period, and if the step consistently fails, break the loop with a structured error report including the failure pattern, rather than letting the loop oscillate.',
        'C) Skip smoke tests and rely on production monitoring to catch issues.',
        'D) Always deploy to production regardless of smoke test results.',
      ],
      correct: 1,
      explanation: 'A step-specific retry counter with structured failure reporting addresses the intermittent pattern directly. The key insight is distinguishing between transient failures (retryable) and systematic failures (need human intervention). Option A masks the problem with more iterations. Option C eliminates a valuable safety check. Option D risks deploying broken code to production.',
    },
    {
      id: 2, examScenario: 4, domain: 'd1', domainLabel: 'Multi-Agent Orchestration',
      scenario: 'Your DevOps system has three agents: BuildAgent (compiles code), TestAgent (runs test suites), and DeployAgent (handles deployment). Currently, all three are orchestrated by a simple sequential script. You want to add parallel execution where possible.',
      question: 'What is the safest way to parallelize this pipeline?',
      options: [
        'A) Run all three agents in parallel for every commit.',
        'B) Run BuildAgent first (compilation must succeed before testing), then run TestAgent and lint checks in parallel, then DeployAgent only after both TestAgent and lint checks pass. This maintains dependency ordering while parallelizing independent steps.',
        'C) Run TestAgent and DeployAgent in parallel since they don\'t depend on each other.',
        'D) Remove the orchestrator and let agents coordinate via shared state.',
      ],
      correct: 1,
      explanation: 'The correct parallelization respects the dependency chain: build must complete before tests can run, and tests must pass before deployment. The parallelization opportunity is running independent checks (tests + lint) concurrently after a successful build. Option A violates dependencies. Option C skips the build dependency. Option D introduces coordination complexity that an orchestrator handles more reliably.',
    },
    {
      id: 3, examScenario: 4, domain: 'd1', domainLabel: 'Tool Use Flow',
      scenario: 'Your infrastructure agent receives a complex request: "Scale up the API servers, update the load balancer config, and notify the team." In a single response, Claude calls three tools: scale_servers (takes 60s), update_config (takes 5s), and send_notification (takes 2s). Your implementation executes them sequentially, totaling 67 seconds.',
      question: 'How should you handle these tool calls to minimize total execution time?',
      options: [
        'A) Execute all three tools in parallel since they are independent operations.',
        'B) Execute scale_servers first (60s), then execute update_config and send_notification in parallel (5s), totaling 65 seconds.',
        'C) Execute send_notification first to inform the team immediately, then the other two.',
        'D) Execute update_config first since it\'s fastest, then scale_servers, then send_notification.',
      ],
      correct: 0,
      explanation: 'All three operations are independent — scaling servers doesn\'t depend on the config update, and the notification can be sent immediately. Parallel execution reduces total time from 67 seconds to 60 seconds (the longest single operation). Option B creates an unnecessary dependency. Option C notifies the team before the action is complete, potentially causing confusion if scaling fails. Option D is arbitrary and slower.',
    },
    {
      id: 4, examScenario: 4, domain: 'd1', domainLabel: 'Agentic Loops',
      scenario: 'Your incident response agent uses an agentic loop to diagnose production issues. On a critical outage, the agent spends 8 iterations reading logs, checking metrics, and querying databases without converging on a diagnosis. The outage has been ongoing for 15 minutes.',
      question: 'What is the most appropriate intervention after 8 diagnostic iterations without convergence?',
      options: [
        'A) Let the agent continue — it will eventually find the root cause.',
        'B) Inject a meta-observation: "You\'ve investigated 8 areas without finding the root cause. Based on the symptoms described, consider: [top 3 most likely causes based on initial symptoms]. Focus your next investigation on the most likely cause."',
        'C) Terminate the agent and page a human engineer immediately.',
        'D) Restart the agent with a fresh context window.',
      ],
      correct: 1,
      explanation: 'A meta-observation injection redirects the agent\'s investigation by synthesizing what it has learned and suggesting focused next steps. This leverages the 8 iterations of diagnostic data while breaking the non-convergence pattern. Option A wastes critical outage time. Option C discards 8 iterations of valuable diagnostic work. Option D loses all accumulated context.',
    },
    // ─── D2 ──────────────────────────────────────────────────────────
    {
      id: 5, examScenario: 4, domain: 'd2', domainLabel: 'System Prompt Design',
      scenario: 'Your deployment agent\'s system prompt includes detailed instructions for 3 cloud providers (AWS, GCP, Azure) totaling 3,800 tokens. Each deployment uses only one provider. When deploying to AWS, the agent occasionally uses GCP-specific CLI flags or Azure-specific authentication patterns.',
      question: 'What system prompt design would prevent cross-provider confusion?',
      options: [
        'A) Add stronger delimiters: "=== AWS INSTRUCTIONS === ... === GCP INSTRUCTIONS ===" to help Claude distinguish between them.',
        'B) Use dynamic prompt assembly — inject only the target provider\'s instructions into the system prompt at deployment time, removing the other two providers entirely.',
        'C) Add few-shot examples for each provider demonstrating correct CLI usage.',
        'D) Consolidate all instructions into provider-agnostic guidelines.',
      ],
      correct: 1,
      explanation: 'Dynamic prompt assembly eliminates cross-contamination at the source by including only the relevant provider\'s instructions. This is both more token-efficient and more reliable than including all three and hoping Claude selects the right one. Option A adds visual structure but doesn\'t prevent the attention overlap. Option C adds token overhead. Option D loses provider-specific detail that is necessary.',
    },
    {
      id: 6, examScenario: 4, domain: 'd2', domainLabel: 'Context Window Management',
      scenario: 'Your monitoring agent processes alerts by reading recent log entries. During a major incident, log volume spikes from 500 lines/minute to 5,000 lines/minute. The agent reads all logs and exceeds its context window, producing an incomplete diagnosis.',
      question: 'What context management strategy would most effectively handle log volume spikes?',
      options: [
        'A) Implement log sampling: during high-volume periods, read only error-level and warning-level logs, summarize info-level logs, and prioritize the most recent entries. This reduces context consumption while preserving the most diagnostic information.',
        'B) Read all logs regardless of volume to ensure nothing is missed.',
        'C) Read only the most recent 100 log lines during any incident.',
        'D) Use a larger context window model during incidents.',
      ],
      correct: 0,
      explanation: 'Priority-based log sampling adapts to volume spikes by focusing on the most diagnostic entries (errors, warnings) while reducing noise (info-level). This is the standard approach in observability systems. Option B is impractical during spikes — the volume exceeds context capacity. Option C is arbitrary and may miss critical older errors. Option D increases cost without solving the fundamental data volume problem.',
    },
    {
      id: 7, examScenario: 4, domain: 'd2', domainLabel: 'Prompt Chaining',
      scenario: 'Your incident response pipeline chains: AlertTriage (classifies severity) → DiagnosticAgent (investigates root cause) → RemediationAgent (implements fix). The DiagnosticAgent occasionally recommends actions that are unsafe for production (e.g., "restart the database") without flagging the risk.',
      question: 'What prompt chaining improvement would add appropriate safety checks?',
      options: [
        'A) Add a system prompt to DiagnosticAgent: "Never recommend risky actions."',
        'B) Add a RiskAssessment stage between Diagnostic and Remediation that evaluates each recommended action for production safety, flags high-risk actions, and requires explicit human approval before the RemediationAgent can execute them.',
        'C) Have the RemediationAgent evaluate the safety of the DiagnosticAgent\'s recommendations.',
        'D) Remove the RemediationAgent and have humans implement all fixes.',
      ],
      correct: 1,
      explanation: 'A dedicated risk assessment stage creates an explicit safety gate between diagnosis and action. This is the principle of separating recommendation from execution for high-stakes operations. Option A is too vague — "risky" is subjective. Option C is reasonable but conflates two concerns (remediation + safety assessment). Option D eliminates automation benefits entirely.',
    },
    // ─── D3 ──────────────────────────────────────────────────────────
    {
      id: 8, examScenario: 4, domain: 'd3', domainLabel: 'Tool Selection & Design',
      scenario: 'Your DevOps agent has two tools: deploy_service (description: "Deploys a service") and rollback_service (description: "Rolls back a service"). During a failed deployment, the agent calls deploy_service again instead of rollback_service — the minimal descriptions don\'t clearly differentiate when to use each tool.',
      question: 'What tool design change would most improve selection accuracy in failure scenarios?',
      options: [
        'A) Merge both tools into a single manage_deployment tool with an action parameter.',
        'B) Expand descriptions with explicit use cases: deploy_service → "Deploys a new version of a service. Use when initiating a new deployment or updating an existing service. NOT for reverting failed deployments." rollback_service → "Rolls back a service to its previous stable version. Use when a deployment fails or causes issues. This is the correct action when deployment produces errors or degraded performance."',
        'C) Add few-shot examples showing rollback scenarios.',
        'D) Implement a routing classifier that detects failure scenarios and pre-selects rollback.',
      ],
      correct: 1,
      explanation: 'Detailed descriptions with explicit boundaries and negative examples directly address the selection confusion. The "NOT for reverting failed deployments" and "correct action when deployment produces errors" language gives Claude clear decision criteria. Option A creates a poorly scoped tool. Option C adds examples but doesn\'t fix the fundamental description gap. Option D over-engineers what better descriptions solve.',
    },
    {
      id: 9, examScenario: 4, domain: 'd3', domainLabel: 'Tool Error Handling',
      scenario: 'Your deployment tool fails with a cryptic error: "Error: resource quota exceeded." The tool returns this as a plain string. Claude interprets this as a transient error and retries 3 times, wasting 9 minutes, when the actual issue is that the team\'s cloud account needs a quota increase — a non-retryable error.',
      question: 'What error handling pattern would prevent wasted retries on non-retryable errors?',
      options: [
        'A) Implement structured error responses with a retryable field: {is_error: true, error_type: "quota_exceeded", retryable: false, resolution: "Request a quota increase from the cloud provider console"}. Claude can then skip retries and provide actionable guidance.',
        'B) Disable retries entirely and report all errors to humans.',
        'C) Increase the retry count to 5 to exhaust all possibilities.',
        'D) Add a system prompt instruction: "Don\'t retry on quota errors."',
      ],
      correct: 0,
      explanation: 'Structured error responses with explicit retryability classification allow Claude to make informed decisions — skip retries on non-retryable errors and provide resolution guidance. This is the standard pattern for API error handling in agent systems. Option B is overly cautious. Option C wastes more time. Option D requires Claude to recognize quota errors from plain text, which it failed to do.',
    },
    // ─── D4 ──────────────────────────────────────────────────────────
    {
      id: 10, examScenario: 4, domain: 'd4', domainLabel: 'System Prompt Architecture',
      scenario: 'Your infrastructure agent manages resources across AWS, GCP, and Azure. The system prompt includes authentication setup, resource naming conventions, tagging policies, and compliance requirements for each provider — totaling 8,200 tokens. Only one provider is used per session.',
      question: 'Is this system prompt size a concern, and what optimization should you prioritize?',
      options: [
        'A) 8,200 tokens is fine — context windows are large enough.',
        'B) Yes, it\'s a concern. Use dynamic prompt assembly to inject only the target provider\'s instructions (estimated 2,800 tokens), cutting system prompt size by 66% and eliminating cross-provider confusion.',
        'C) Move all provider instructions to a tool-accessible reference.',
        'D) Split into three separate agents, one per provider.',
      ],
      correct: 1,
      explanation: '8,200 tokens consumed on every API call for instructions that are 66% irrelevant (two unused providers) is a significant waste. Dynamic prompt assembly injects only what\'s needed, saving tokens and preventing cross-provider confusion. Option A ignores the compounding cost across many API calls per session. Option C loses the always-present instructions that ensure compliance. Option D creates unnecessary agent proliferation.',
    },
    {
      id: 11, examScenario: 4, domain: 'd4', domainLabel: 'Validation & Retry Strategies',
      scenario: 'Your infrastructure-as-code agent generates Terraform configurations. Validation shows that 5% of generated configs have incorrect resource references — e.g., referencing a subnet that doesn\'t exist in the specified VPC. A terraform plan catches these errors, but the retry logic feeds the plan output back without highlighting the specific error.',
      question: 'What retry strategy would most effectively fix resource reference errors?',
      options: [
        'A) Increase retries to 5 attempts.',
        'B) Before generating Terraform config, have Claude first query the infrastructure state (existing VPCs, subnets, resources) via a tool, then generate config that references only verified resources. Prevention is more effective than retry.',
        'C) Add a system prompt instruction: "Always verify resource references before generating Terraform config."',
        'D) Parse the terraform plan output and extract the specific error line numbers for targeted retry.',
      ],
      correct: 1,
      explanation: 'Querying infrastructure state before generating config prevents resource reference errors at the source — Claude knows what resources exist and can only reference valid ones. This is more effective than catching errors after generation. Option A retries a fundamentally flawed approach. Option C is advisory and unreliable. Option D is a reasonable complement but less effective than prevention.',
    },
    {
      id: 12, examScenario: 4, domain: 'd4', domainLabel: 'Batch Processing',
      scenario: 'Your security scanning agent checks 200 microservices for vulnerabilities. Each check is an independent Claude API call costing $0.005, totaling $1.00 per full scan. Scans run every 6 hours (4x/day). Your manager wants to reduce the $4/day cost.',
      question: 'What is the most cost-effective approach while maintaining scan coverage?',
      options: [
        'A) Use the Message Batches API for all 200 checks per scan. Security scans don\'t require real-time results — findings can be reported asynchronously.',
        'B) Scan only the 50 most critical services and skip the rest.',
        'C) Reduce scan frequency to once per day.',
        'D) Run all 200 checks in a single API call by concatenating service descriptions.',
      ],
      correct: 0,
      explanation: 'Security scanning is inherently a batch operation — results are reviewed asynchronously, not in real-time. The Message Batches API\'s 50% cost savings are fully appropriate, reducing cost from $4/day to $2/day without sacrificing coverage. Option B reduces security coverage. Option C reduces scan frequency, increasing vulnerability exposure window. Option D would degrade per-service analysis quality due to attention dilution.',
    },
    {
      id: 13, examScenario: 4, domain: 'd4', domainLabel: 'Multi-Instance Review',
      scenario: 'Your security review agent uses dual-instance review for infrastructure changes. Two Claude instances independently review each change. When they disagree (one approves, one flags an issue), the change goes to a human reviewer. This happens 30% of the time, creating a bottleneck.',
      question: 'What would most effectively reduce unnecessary disagreement-based escalations?',
      options: [
        'A) Accept the 30% escalation rate as the cost of thorough review.',
        'B) Align the reviewers\' criteria: create a shared checklist of specific security patterns to check, with explicit severity ratings. When both reviewers use the same structured criteria, disagreement on clear-cut issues decreases, and remaining disagreements reflect genuine ambiguity warranting human review.',
        'C) Use a single reviewer and accept the reduced safety coverage.',
        'D) Use a tiebreaker (third instance) instead of human review.',
      ],
      correct: 1,
      explanation: 'Much reviewer disagreement stems from different evaluation criteria rather than genuine ambiguity. A shared structured checklist ensures both reviewers evaluate against the same standards. Remaining disagreements are more likely to reflect genuinely ambiguous cases that truly warrant human review. Option A accepts a solvable problem. Option C reduces safety. Option D automates away the human judgment that disagreements specifically require.',
    },
    // ─── D5 ──────────────────────────────────────────────────────────
    {
      id: 14, examScenario: 4, domain: 'd5', domainLabel: 'Context Management',
      scenario: 'Your incident response agent maintains context across a multi-hour incident. After 45 minutes and 60 turns of investigation, the agent begins suggesting diagnostic steps that were already completed in turns 5-15, wasting critical time during an ongoing outage.',
      question: 'What context management pattern would prevent re-investigation of completed steps?',
      options: [
        'A) Maintain a structured investigation log that is injected as persistent context in every turn, recording: step attempted, result, and conclusion. The agent can reference this log rather than relying on conversation memory for completed steps.',
        'B) Add a system prompt instruction: "Keep track of all diagnostic steps you\'ve performed."',
        'C) Start a new conversation every 20 turns.',
        'D) Increase the model\'s context window to handle longer conversations.',
      ],
      correct: 0,
      explanation: 'A structured investigation log with persistent injection provides a definitive record of completed steps that doesn\'t rely on the model\'s attention to earlier conversation turns. During high-pressure incidents, deterministic tracking is essential. Option B is advisory and contradicted by the data. Option C loses investigation continuity. Option D delays the problem without solving it.',
    },
    {
      id: 15, examScenario: 4, domain: 'd5', domainLabel: 'Escalation & Ambiguity',
      scenario: 'Your monitoring agent detects an anomaly: API response times increased from 200ms to 800ms. The agent\'s diagnostic confidence is 0.65. The affected service handles payment processing for 10,000 active users.',
      question: 'Should this be escalated to a human engineer immediately?',
      options: [
        'A) No — 0.65 confidence is above the 0.5 threshold for autonomous handling.',
        'B) Yes — the combination of moderate diagnostic confidence (0.65) AND high business impact (payment processing for 10K users) warrants immediate human notification. The agent can continue investigating in parallel, but human awareness should not wait for a definitive diagnosis.',
        'C) No — let the agent continue investigating and only escalate if the situation worsens.',
        'D) Yes, and stop all automated investigation to avoid interfering with human diagnosis.',
      ],
      correct: 1,
      explanation: 'The escalation decision should consider both confidence and impact. Payment processing for 10K users is high-impact, and a 4x latency increase could indicate imminent failure. Early human awareness enables preparation while the agent continues investigating. Option A ignores the business impact. Option C risks the situation degrading before humans are alerted. Option D wastes the agent\'s ongoing diagnostic work.',
    },
    {
      id: 16, examScenario: 4, domain: 'd5', domainLabel: 'Error Propagation',
      scenario: 'Your deployment pipeline fails at the database migration step. The migration tool returns: "Migration failed: column \'user_email\' already exists." The pipeline agent catches this error but reports it as "Deployment failed due to an unspecified database issue" to the operations team, obscuring the actionable root cause.',
      question: 'What error propagation pattern would provide the most useful information to operators?',
      options: [
        'A) Propagate the raw SQL error directly to the operations team.',
        'B) Implement structured error enrichment: catch the specific error type (schema conflict), add context (migration was attempting to add an existing column), suggest resolution (this is a non-destructive error — the migration can be marked as already applied), and include the original error for technical reference.',
        'C) Automatically retry the migration 3 times.',
        'D) Roll back the entire deployment and report a generic failure.',
      ],
      correct: 1,
      explanation: 'Structured error enrichment adds actionable context — the specific issue, why it occurred, and how to resolve it. The "already exists" error is actually benign and can be resolved without a rollback. Option A exposes raw details without guidance. Option C retries a non-retryable error. Option D takes the most destructive action based on a non-destructive error.',
    },
    {
      id: 17, examScenario: 4, domain: 'd5', domainLabel: 'Human Review & Confidence',
      scenario: 'Your automated remediation agent can restart services, scale resources, and update configurations. It detects high memory usage (92%) on a database server and proposes restarting the service with 0.88 confidence. The database serves as the primary data store for a financial application.',
      question: 'Should the agent execute the restart autonomously?',
      options: [
        'A) Yes — 0.88 confidence is high, and the action is appropriate for the symptoms.',
        'B) No — restarting a production database serving a financial application is a high-impact action. Even with high confidence, domain-based review policies should require human approval for database restarts in financial systems, where even brief downtime has significant consequences.',
        'C) Yes, but create a backup first and then restart.',
        'D) Yes, but only during off-hours.',
      ],
      correct: 1,
      explanation: 'High-impact actions on critical infrastructure require human approval regardless of confidence scores. A database restart causes temporary unavailability, which in a financial system means potential transaction failures. The confidence score measures the model\'s certainty about the diagnosis, not the appropriateness of the action given business constraints. Options C and D add reasonable mitigations but still automate a decision that should involve human judgment for financial systems.',
    },
    {
      id: 18, examScenario: 4, domain: 'd5', domainLabel: 'Provenance & Attribution',
      scenario: 'Your infrastructure agent makes 6 changes during an incident remediation: scaling up servers, updating a load balancer rule, restarting a service, modifying a health check, adjusting a timeout, and updating a DNS record. The incident is resolved, but during the post-mortem, the team can\'t determine which change actually fixed the issue.',
      question: 'What provenance practice should have been followed during remediation?',
      options: [
        'A) Record each change as it\'s made with: timestamp, the triggering symptom or metric, the specific change, the expected outcome, and the observed result after the change. This creates a causal chain for post-mortem analysis.',
        'B) Only make one change at a time and wait for results before making the next.',
        'C) Document all changes in a single summary after the incident is resolved.',
        'D) Let each agent log its own changes independently without coordination.',
      ],
      correct: 0,
      explanation: 'Real-time change attribution with causal context (trigger → action → expected result → observed result) enables precise post-mortem analysis. This is the standard practice in Site Reliability Engineering. Option B is ideal but impractical during critical incidents where parallel actions may be necessary. Option C loses real-time causal context. Option D creates fragmented logs without a coherent timeline.',
    },
    {
      id: 19, examScenario: 4, domain: 'd5', domainLabel: 'Context Management',
      scenario: 'Your monitoring agent processes 10,000 metric data points per minute. It needs to identify anomalies and correlate them with recent deployments. Raw metrics would consume 200,000 tokens per minute — far exceeding any context window.',
      question: 'What data processing pattern would most effectively handle this volume?',
      options: [
        'A) Process all metrics directly — let Claude find patterns in the raw data.',
        'B) Implement multi-stage data reduction: aggregate metrics into statistical summaries (mean, p95, p99), flag only metrics that deviate from baseline by >2 standard deviations, and present Claude with a focused anomaly report plus deployment timeline for correlation analysis.',
        'C) Randomly sample 1% of metrics and analyze those.',
        'D) Use a non-AI statistical anomaly detection system and only send alerts to Claude.',
      ],
      correct: 1,
      explanation: 'Multi-stage data reduction — statistical aggregation, deviation flagging, and focused anomaly reporting — reduces the data to a manageable volume while preserving the most diagnostic information. Claude receives high-signal context for correlation analysis. Option A is impractical given the volume. Option C risks missing anomalies in the 99% of unanalyzed data. Option D eliminates Claude\'s correlation and reasoning capability.',
    },
    {
      id: 20, examScenario: 4, domain: 'd5', domainLabel: 'Codebase Exploration',
      scenario: 'Your infrastructure is defined in Terraform across 30 files. An error occurs during deployment: "Error: Invalid for_each argument — required keys are missing." You want Claude Code to find the source of the error.',
      question: 'What is the most efficient exploration strategy?',
      options: [
        'A) Read all 30 Terraform files to build a complete understanding.',
        'B) Search for files containing "for_each" and the resource type mentioned in the error, read those files, and trace the data source to find where the missing keys should be defined.',
        'C) Read only the main.tf file.',
        'D) Ask the DevOps engineer to identify the problematic file.',
      ],
      correct: 1,
      explanation: 'Targeted search for the error-specific keywords (for_each, the resource type) followed by dependency tracing efficiently locates the issue. This is dramatically faster than reading all files and more targeted than reading only main.tf. Option A wastes context on irrelevant files. Option C may miss the error if it\'s in a different file. Option D shifts work to humans.',
    },
  ],
};

export default exam;
