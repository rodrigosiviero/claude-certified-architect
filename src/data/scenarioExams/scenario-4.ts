import type { ScenarioExamData } from './types';

const exam: ScenarioExamData = {
  id: 4,
  title: 'DevOps & CI/CD Pipeline Agent',
  description: 'Design and optimize automated deployment, monitoring, and incident response workflows using agentic patterns.',
  icon: '⚙️',
  questions: [
    { id: 1, examScenario: 4, domain: 'd1', domainLabel: 'Agentic Loops', examTask: '1.1 Agentic Loops', scenario: 'Your deployment agent runs an agentic loop: build → test → stage → smoke_test → deploy. On 5% of deployments, the smoke test passes but the production deployment fails with a configuration error. The agent retries the entire loop from build, wasting 8 minutes on each retry.', question: 'What loop optimization would most effectively handle configuration-specific deployment failures?', options: ['A) Retry the full loop up to 5 times before reporting failure.', 'B) Implement targeted retry: when a deployment fails at a specific stage (e.g., configuration error during deploy), retry only from that stage with the error context, rather than restarting the entire pipeline from build.', 'C) Skip the failing stage and continue deployment with default configuration.', 'D) Add a pre-deployment configuration validation step that catches errors before the loop starts.'], correct: 1, explanation: 'Targeted retry from the failing stage avoids wasting time on completed steps. A configuration error during deployment doesn\'t invalidate the build, tests, or staging — only the configuration needs fixing. A wastes 40+ minutes on unnecessary re-processing. C skips a safety check. D is good prevention but doesn\'t help when errors still occur during deployment. [Task 1.1 — Agentic Loops: targeted retry from failure points is more efficient than full pipeline restart.]' },
    { id: 2, examScenario: 4, domain: 'd1', domainLabel: 'Multi-Agent Orchestration', examTask: '1.2 Multi-Agent Orchestration', scenario: 'Your deployment system has BuildAgent, SecurityAgent, TestAgent, and DeployAgent coordinated by a PipelineOrchestrator. BuildAgent completes in 3 minutes, SecurityAgent in 5 minutes, TestAgent in 4 minutes. Currently they run sequentially after build: Security → Test → Deploy, totaling 12 minutes.', question: 'What orchestration change would minimize pipeline duration?', options: ['A) Run all four agents in parallel to minimize total time.', 'B) After BuildAgent completes, run SecurityAgent and TestAgent in parallel (total time = max(5,4) = 5 minutes), then DeployAgent. Total: Build(3) + max(Security,Test)(5) + Deploy(1) = 9 minutes.', 'C) Run SecurityAgent and TestAgent before BuildAgent to catch issues earlier.', 'D) Merge SecurityAgent and TestAgent into a single combined agent.'], correct: 1, explanation: 'Security scanning and test suite execution are independent after a successful build — they can run in parallel. Pipeline duration drops from 12 to 9 minutes. A is impossible — security and tests depend on build output. C violates dependencies. D loses specialization without enabling parallelism. [Task 1.2 — Multi-Agent Orchestration: parallelize independent stages while maintaining dependency order.]' },
    { id: 3, examScenario: 4, domain: 'd2', domainLabel: 'Structured Error Responses', examTask: '2.2 Structured Error Responses', scenario: 'Your deployment agent\'s deploy_to_production tool returns: "Error: Container health check failed — timeout after 30 seconds." The agent retries the deployment 3 times with the same result. Investigation shows the container starts successfully but takes 45 seconds to pass health checks — the timeout is too short, not the deployment.', question: 'What error response design would have identified the root cause without 3 retries?', options: ['A) Return structured error context: {error_type: "HEALTH_CHECK_TIMEOUT", timeout_seconds: 30, container_status_at_timeout: "starting", last_health_check_output: "...", actual_startup_time_estimate: "45s"}.', 'B) Increase the health check timeout to 60 seconds by default.', 'C) Add a system prompt instruction to investigate health check failures before retrying.', 'D) Skip health checks during deployment and verify post-deployment.'], correct: 0, explanation: 'Structured error context with diagnostic details (timeout value, actual container state) enables intelligent root cause analysis. The agent can distinguish "container failed to start" from "container starting but health check timeout too short" — fundamentally different issues requiring different responses. B is a blunt workaround. C relies on the agent recognizing the pattern without data. D removes a safety check. [Task 2.2 — Structured Error Responses: diagnostic context in error responses enables intelligent root cause analysis.]' },
    { id: 4, examScenario: 4, domain: 'd4', domainLabel: 'System Prompt Architecture', examTask: '4.1 Prompts with Explicit Criteria', scenario: 'Your deployment agent needs to enforce a strict deployment checklist with 23 items. When the system prompt exceeds 3,500 tokens, the agent occasionally skips checklist items.', question: 'What architectural approach would most reliably enforce the full checklist?', options: ['A) Add stronger language: "You MUST complete ALL 23 checklist items before proceeding."', 'B) Implement the checklist programmatically as a state machine: each item becomes a required tool call with a validation gate that must pass before the next step executes, enforced by the deployment orchestration code.', 'C) Split the checklist into smaller groups and process them sequentially across multiple prompts.', 'D) Repeat the full checklist at the end of deployment to verify completion.'], correct: 1, explanation: 'Programmatic enforcement via a state machine provides deterministic guarantees that system prompt instructions cannot. Each item becomes a required step with a validation gate — deployment cannot proceed until the current step passes. A relies on probabilistic compliance. C adds complexity without solving the fundamental issue. D is reactive rather than preventive. [Task 4.1 — Explicit Criteria: programmatic enforcement provides deterministic guarantees for critical operational procedures.]' },
    { id: 5, examScenario: 4, domain: 'd4', domainLabel: 'Validation & Retry', examTask: '4.4 Validation, Retry & Feedback Loops', scenario: 'Your deployment validation checks 15 metrics (error rate, latency, CPU, memory) after each deployment. All 15 must pass within a 5-minute window. 20% of deployments initially fail validation due to transient metric spikes that resolve within 10 minutes.', question: 'What validation strategy would reduce false-positive deployment failures?', options: ['A) Extend the validation window to 30 minutes for all deployments.', 'B) Implement two-phase validation: strict immediate check (critical metrics like error rate >5%) and gradual stabilization (non-critical metrics get a 15-minute stabilization window before evaluation).', 'C) Remove non-critical metrics from the validation to reduce false positives.', 'D) Accept a 20% false-positive rate as the cost of thorough validation.'], correct: 1, explanation: 'Two-phase validation separates critical failure indicators from transient settling effects. Error rates need immediate validation; CPU and memory metrics need time to stabilize. A delays all deployments unnecessarily. C reduces safety coverage. D accepts a solvable problem. [Task 4.4 — Validation & Retry: two-phase validation distinguishes critical failures from transient settling effects.]' },
    { id: 6, examScenario: 4, domain: 'd4', domainLabel: 'Batch Processing', examTask: '4.5 Batch Processing Strategies', scenario: 'Your team deploys 30 microservices independently. Each deployment requires a pre-deployment Claude analysis ($0.005). During a coordinated release, all 30 services need analysis. Currently you process them sequentially with real-time API calls.', question: 'What is the most cost-effective approach for batch deployment analysis?', options: ['A) Continue sequential real-time processing since $0.15 total is minimal.', 'B) Use the Message Batches API for all 30 pre-deployment analyses. Since these are preparation steps (not blocking — developers can review when ready), batch processing time is acceptable and saves 50%.', 'C) Analyze only the 10 most critical services and skip the rest.', 'D) Combine all 30 analyses into a single API call.'], correct: 1, explanation: 'Pre-deployment analysis is batchable — it\'s a preparation step, not a blocking gate. Message Batches processes all 30 at 50% savings with results ready before deployment begins. A pays full price unnecessarily. C reduces coverage. D degrades per-service analysis quality. [Task 4.5 — Batch Processing: non-blocking preparation steps are ideal candidates for batch API usage.]' },
    { id: 7, examScenario: 4, domain: 'd4', domainLabel: 'Multi-Instance Review', examTask: '4.6 Multi-Instance & Multi-Pass Review', scenario: 'Your deployment review uses a single Claude instance to assess risk. It has a 15% false-positive rate (flagging safe deployments) and 5% false-negative rate (approving risky ones). You\'re considering adding a second review instance.', question: 'What multi-instance pattern would minimize both error types?', options: ['A) Run two instances and approve only if both approve (minimizes false negatives but increases false positives).', 'B) Run two instances and approve if either approves (minimizes false positives but increases false negatives).', 'C) Run two instances with different perspectives: one optimized for detecting risks (conservative) and one for identifying safe patterns (liberal). If they agree, use their consensus. If they disagree, escalate to human review.', 'D) A single instance with a better prompt is always superior to multi-instance review.'], correct: 2, explanation: 'Complementary perspectives cover both failure modes. Agreement = high confidence in either direction. Disagreement = genuine ambiguity requiring human judgment. A is overly conservative. B is overly permissive. D may improve single-instance performance but doesn\'t provide the redundancy that catches edge cases. [Task 4.6 — Multi-Instance Review: complementary perspectives with disagreement escalation balance both error types.]' },
    { id: 8, examScenario: 4, domain: 'd5', domainLabel: 'Context Management', examTask: '5.1 Conversation Context Management', scenario: 'Your deployment agent maintains state across a multi-service deployment. After deploying services A, B, C (with detailed logs and health checks), it starts deploying service D. Context from A-C consumes 80% of the window, leaving limited space for D\'s reasoning.', question: 'What context strategy would ensure adequate reasoning space for each service?', options: ['A) Process all services in a single context window to maintain full visibility.', 'B) Implement state compression between services: after each is deployed and verified, compress its details into a structured summary (service name, status, health, key metrics) and discard raw logs, freeing context for subsequent deployments.', 'C) Deploy each service in a completely separate session with no shared context.', 'D) Use a larger context window model for multi-service deployments.'], correct: 1, explanation: 'State compression preserves essential information (which services deployed successfully, their health) while freeing context for the next deployment. Raw logs from service A aren\'t needed when deploying service D — only the outcome matters. A risks context exhaustion. C loses coordination context. D delays the problem at higher cost. [Task 5.1 — Context Management: compression between deployment stages preserves essential state while freeing reasoning space.]' },
    { id: 9, examScenario: 4, domain: 'd5', domainLabel: 'Escalation & Ambiguity', examTask: '5.2 Escalation & Ambiguity Resolution', scenario: 'Your deployment agent detects an anomaly during a canary deployment: error rate increased from 0.1% to 0.4%. Baseline variation is up to 0.3%. The agent\'s confidence that this is deployment-caused is 0.62.', question: 'What is the most appropriate action?', options: ['A) Automatically roll back — the error rate increase is concerning.', 'B) Continue the canary with increased monitoring and set an automatic rollback trigger at 0.5%. The 0.4% rate is above normal variation but below a clear failure threshold — extended observation provides more data.', 'C) Ignore the anomaly — 0.4% is still a low error rate.', 'D) Immediately escalate to the entire engineering team.'], correct: 1, explanation: 'The 0.4% rate is ambiguous — above normal variation (0.3%) but not clearly a deployment-caused failure. Enhanced monitoring with a defined rollback trigger gathers more data before irreversible action. A is premature — the increase might stabilize. C is risky — it might be a real issue. D causes unnecessary alarm for an ambiguous signal. [Task 5.2 — Escalation & Ambiguity: ambiguous signals warrant enhanced monitoring with defined thresholds, not immediate action.]' },
    { id: 10, examScenario: 4, domain: 'd5', domainLabel: 'Error Propagation', examTask: '5.3 Error Propagation Strategies', scenario: 'A canary deployment fails due to a memory leak. The agent initiates rollback. During rollback, the previous version also fails because the configuration was updated for the new version and is incompatible with the old version.', question: 'What error propagation pattern would have prevented the failed rollback?', options: ['A) Always create a configuration backup before deployment.', 'B) Implement atomic deployment with configuration snapshots: before any change, snapshot the current configuration state. If rollback is needed, restore the exact snapshot alongside the previous version. Deployment and configuration are a single atomic unit.', 'C) Never update configuration during deployments.', 'D) Roll back only the application code, not the configuration.'], correct: 1, explanation: 'Atomic deployment with configuration snapshots treats the entire deployment state (code + configuration + infrastructure) as a single rollback unit. This prevents partial rollbacks where code reverts but configuration doesn\'t. A is good practice but doesn\'t ensure automatic restoration. C is unrealistic. D is exactly the mistake that caused the failure. [Task 5.3 — Error Propagation: atomic deployment with configuration snapshots prevents partial rollback failures.]' },
    { id: 11, examScenario: 4, domain: 'd5', domainLabel: 'Provenance & Attribution', examTask: '5.6 Information Provenance & Uncertainty', scenario: 'Over the past month, your deployment agent made 200 deployments. A production issue is traced to a configuration change made 3 weeks ago. You need to determine which deployment introduced it and why.', question: 'What provenance information should be recorded for each deployment?', options: ['A) Deployment timestamp and version number only.', 'B) For each deployment: timestamp, deployer (agent vs. human), triggering event, all configuration changes with before/after values, risk assessment, approval chain, and post-deployment metrics — creating a complete audit trail.', 'C) Git commit hash only — everything else is in version control.', 'D) Only record deployments that fail.'], correct: 1, explanation: 'A complete deployment record with configuration changes (before/after) and decision chain (trigger, risk assessment, approval) enables root cause investigation weeks later. Git shows code changes but not deployment reasoning. A is insufficient. C misses configuration and reasoning context. D loses information about successful changes that may cause issues later. [Task 5.6 — Provenance: comprehensive deployment records with configuration deltas and decision context enable retrospective investigation.]' },
    { id: 12, examScenario: 4, domain: 'd5', domainLabel: 'Human Review & Confidence', examTask: '5.5 Human Review & Confidence Calibration', scenario: 'Your deployment agent proposes a database schema migration with 0.91 confidence. It\'s an additive change with a default value. The database serves 2 million active users.', question: 'Should this migration proceed automatically?', options: ['A) Yes — 0.91 confidence and low risk assessment justifies automatic execution.', 'B) No — database schema changes on systems serving 2M users should require human review regardless of confidence. The blast radius of a migration error (data corruption, downtime) is too large for fully automated execution.', 'C) Yes, but run it during off-peak hours.', 'D) Yes, but create a backup first.'], correct: 1, explanation: 'Database migrations on high-traffic systems are inherently high-stakes. Even "additive, non-breaking" changes can have unexpected consequences (table locking, cascading effects). Blast radius determines review requirements, not confidence scores. C and D add mitigations but still automate a decision warranting human judgment for 2M-user systems. [Task 5.5 — Human Review: blast radius should determine review requirements, not confidence scores.]' },
    { id: 13, examScenario: 4, domain: 'd1', domainLabel: 'Tool Use Flow', examTask: '1.1 Agentic Loops', scenario: 'During deployment, Claude returns stop_reason: "end_turn" with "The deployment is complete!" However, your monitoring dashboard shows containers still starting up.', question: 'What does this indicate about the agent\'s tool use?', options: ['A) Trust Claude\'s response — the deployment is complete.', 'B) Claude may have concluded prematurely. The agentic loop should verify deployment status via a monitoring tool (e.g., check_deployment_status) to verify the actual state before concluding the loop.', 'C) Add a system prompt instruction to verify deployment completion before reporting success.', 'D) Increase max_iterations to give Claude more time to complete.'], correct: 1, explanation: 'Claude\'s end_turn is a statement of belief, not a verified fact. The loop should validate actual state via monitoring tools before concluding. This is the verify-before-concluding pattern. A trusts an unverified claim. C is advisory without programmatic enforcement. D doesn\'t address premature conclusion. [Task 1.1 — Agentic Loops: verify actual state via tools rather than trusting the model\'s claims of completion.]' },
    { id: 14, examScenario: 4, domain: 'd1', domainLabel: 'Agentic Loops', examTask: '1.1 Agentic Loops', scenario: 'Your rollback agent has been running for 5 minutes across 12 iterations during a critical incident. The team is considering manual intervention.', question: 'At what point should human intervention override the automated agent?', options: ['A) Never — let the agent run until it resolves the issue.', 'B) Implement configurable thresholds: after 5 minutes or 10 iterations without resolution, automatically escalate to human engineers with the full investigation log, allowing the agent to continue in parallel.', 'C) After 3 iterations, always escalate to humans.', 'D) Only intervene if the agent explicitly requests help.'], correct: 1, explanation: 'Configurable thresholds with automatic escalation ensure human awareness during critical incidents. The agent can continue while humans are alerted, combining automated speed with human judgment. A risks extended outages. C is too aggressive — many issues resolve in 4-5 iterations. D assumes the agent will recognize when it\'s stuck. [Task 1.1 — Agentic Loops: configurable time and iteration thresholds with human escalation balance automation with safety.]' },
    { id: 15, examScenario: 4, domain: 'd2', domainLabel: 'System Prompt Design', examTask: '2.1 Effective Tool Interfaces', scenario: 'Your deployment agent has rollback procedures for 5 strategies. When performing a canary rollback, it occasionally uses blue-green commands, creating confusion.', question: 'What prompt design would prevent strategy cross-contamination?', options: ['A) Add stronger delimiters between strategy sections in the system prompt.', 'B) Use dynamic prompt assembly — inject only the active strategy\'s rollback procedure into the system prompt, removing the other four entirely.', 'C) Add few-shot examples for each strategy to reinforce correct usage.', 'D) Include all five strategies with: "Use only the section matching your current strategy."'], correct: 1, explanation: 'Dynamic prompt assembly eliminates cross-contamination by including only the relevant strategy. It\'s also more token-efficient. A adds visual structure without preventing attention overlap. C adds token overhead. D relies on Claude reliably selecting the correct section — which the data shows it doesn\'t. [Task 2.1 — Tool Interfaces: dynamic prompt assembly prevents cross-contamination between similar procedures.]' },
    { id: 16, examScenario: 4, domain: 'd2', domainLabel: 'Prompt Chaining', examTask: '2.3 Tool Distribution & tool_choice', scenario: 'Your pipeline chains: RiskAssessment → DeploymentPlanner → ExecutionAgent. The ExecutionAgent occasionally encounters issues not in the risk assessment, causing failures.', question: 'What chaining improvement would reduce unexpected issues?', options: ['A) Have the ExecutionAgent handle issues reactively as they arise.', 'B) Feed the RiskAssessment\'s findings directly into the ExecutionAgent\'s context alongside the deployment plan, so it has both the plan and the identified risks. Add a fallback: "If you encounter an issue not in the risk assessment, pause and report rather than proceeding."', 'C) Add more detail to the RiskAssessment prompt to make it more thorough.', 'D) Merge all three agents into one to eliminate handoff issues.'], correct: 1, explanation: 'Providing the ExecutionAgent with both the plan AND the risk assessment gives full context for decision-making. The fallback instruction ensures unexpected issues trigger a pause. A is reactive. C is reasonable but doesn\'t help the ExecutionAgent respond to findings. D loses specialization benefits. [Task 2.3 — Tool Distribution: downstream agents need both the plan and the upstream risk context for informed execution.]' },
    { id: 17, examScenario: 4, domain: 'd3', domainLabel: 'CI/CD Integration', examTask: '3.6 CI/CD Integration', scenario: 'Your CI pipeline runs claude "Analyze this PR for deployment readiness" but the job hangs because Claude Code enters interactive mode when it encounters a large diff.', question: 'What is the correct fix?', options: ['A) Use the -p flag: claude -p "Analyze this PR for deployment readiness" for non-interactive execution.', 'B) Set CLAUDE_CI=true before the command.', 'C) Pipe /dev/null to stdin: claude "Analyze this PR" < /dev/null.', 'D) Use the --non-interactive flag.'], correct: 0, explanation: 'The -p (--print) flag is the documented way to run Claude Code non-interactively. It processes the prompt, outputs to stdout, and exits. B references a non-existent variable. C is a Unix workaround. D references a non-existent flag. [Task 3.6 — CI/CD: the -p flag is essential for non-interactive pipeline usage.]' },
    { id: 18, examScenario: 4, domain: 'd2', domainLabel: 'API Integration Patterns', examTask: '2.4 MCP Server Integration', scenario: 'Your deployment agent integrates with a cloud provider API with a rate limit of 100 requests/minute. During large deployments creating 80+ resources, you hit the rate limit 25% of the time.', question: 'What integration pattern would handle rate limits reliably?', options: ['A) Implement request queuing with token-bucket rate limiting in the tool execution layer, so API calls wait for available capacity rather than failing, combined with exponential backoff for any remaining rate limit responses.', 'B) Slow down all deployments to stay well under the rate limit.', 'C) Request a rate limit increase from the cloud provider.', 'D) Batch all API calls into fewer, larger requests.'], correct: 0, explanation: 'Request queuing with token-bucket rate limiting ensures API calls never fail due to rate limits — they wait for capacity. Combined with exponential backoff, this provides reliable execution. B unnecessarily slows all deployments. C is outside your control. D may not be feasible depending on the API. [Task 2.4 — MCP Server Integration: client-side rate limiting with queuing prevents API failures during burst operations.]' },
    { id: 19, examScenario: 4, domain: 'd5', domainLabel: 'Context Management', examTask: '5.1 Conversation Context Management', scenario: 'Your deployment agent maintains deployment history. After 50 deployments, history consumes 30,000 tokens. The agent references outdated patterns from 30+ deployments ago, occasionally applying deprecated configurations.', question: 'What context pattern would keep history relevant?', options: ['A) Keep full history for completeness.', 'B) Implement history windowing: maintain the last 10 deployments in full detail, compress older ones to summaries (key metrics only), and remove deployments older than 30 days.', 'C) Delete all history after each deployment.', 'D) Store history in a database and query it via tool when needed.'], correct: 1, explanation: 'History windowing with compression keeps recent context while preventing stale patterns from contaminating decisions. C loses valuable pattern information. D is a good complement but adds latency for context retrieval. A allows stale context to accumulate and influence decisions. [Task 5.1 — Context Management: windowed history with compression balances recency with pattern recognition.]' },
    { id: 20, examScenario: 4, domain: 'd5', domainLabel: 'Codebase Exploration', examTask: '5.4 Large Codebase Exploration', scenario: 'You\'re using Claude Code to update deployment scripts for a new container orchestration platform. Current scripts are in deploy/ with 12 Bash scripts and 3 Python utilities.', question: 'What exploration strategy would most efficiently prepare Claude?', options: ['A) Read all 15 files to build a complete understanding.', 'B) Read the main deployment script (entry point) to understand the flow, then follow function calls to discover the architecture. Read Python utilities only if referenced by the main script.', 'C) Read only the README.md in the deploy/ directory.', 'D) Start writing new scripts from scratch without reading existing ones.'], correct: 1, explanation: 'Entry-point-first exploration discovers the actual deployment flow and only reads participating files. This is dramatically faster than reading everything. A wastes context on potentially unused scripts. C may miss implementation details. D risks duplicating functionality and missing important logic. [Task 5.4 — Codebase Exploration: entry-point-first traversal is the most efficient exploration strategy.]' },
  ],
};

export default exam;
