import type { ScenarioExamData } from './types';

const exam: ScenarioExamData = {
  id: 6,
  title: 'Deployment Pipeline Automation',
  description: 'Design and optimize automated deployment workflows using Claude agents for build, test, security scanning, and production release management.',
  icon: '🚀',
  questions: [
    // ─── D1 ──────────────────────────────────────────────────────────
    {
      id: 1, examScenario: 6, domain: 'd1', domainLabel: 'Agentic Loops',
      scenario: 'Your deployment agent runs in an agentic loop: build → test → stage → smoke_test → deploy. On 5% of deployments, the smoke test passes but the production deployment fails with a configuration error. The agent retries the entire loop from build, wasting 8 minutes on each retry.',
      question: 'What loop optimization would most effectively handle configuration-specific deployment failures?',
      options: [
        'A) Retry the full loop 5 times before reporting failure.',
        'B) Implement targeted retry: when a deployment fails at a specific stage (e.g., configuration error during deploy), retry only from that stage with the error context, rather than restarting the entire pipeline from build.',
        'C) Skip the failing stage and continue deployment.',
        'D) Always deploy to production without smoke tests to avoid the loop.',
      ],
      correct: 1,
      explanation: 'Targeted retry from the failing stage avoids wasting time on already-completed steps. A configuration error during deployment doesn\'t invalidate the build, tests, or staging — only the configuration needs fixing. Option A wastes 40+ minutes on unnecessary re-processing. Option C skips a safety check. Option D removes a critical quality gate.',
    },
    {
      id: 2, examScenario: 6, domain: 'd1', domainLabel: 'Multi-Agent Orchestration',
      scenario: 'Your deployment system has: BuildAgent, SecurityAgent, TestAgent, and DeployAgent, coordinated by a PipelineOrchestrator. BuildAgent completes in 3 minutes, SecurityAgent in 5 minutes, and TestAgent in 4 minutes. Currently, they run sequentially after build: Security → Test → Deploy, totaling 12 minutes.',
      question: 'What orchestration change would minimize pipeline duration?',
      options: [
        'A) Run all four agents in parallel.',
        'B) After BuildAgent completes, run SecurityAgent and TestAgent in parallel (max duration = 5 minutes), then DeployAgent. Total: Build(3) + max(Security, Test)(5) + Deploy(1) = 9 minutes.',
        'C) Run SecurityAgent and TestAgent before BuildAgent.',
        'D) Merge SecurityAgent and TestAgent into a single agent.',
      ],
      correct: 1,
      explanation: 'Security scanning and test suite execution are independent after a successful build — they can run in parallel. The pipeline duration drops from 12 to 9 minutes by parallelizing the two longest post-build stages. Option A is impossible — security and tests depend on the build output. Option C violates dependencies. Option D loses specialization and doesn\'t enable parallelism.',
    },
    {
      id: 3, examScenario: 6, domain: 'd1', domainLabel: 'Tool Use Flow',
      scenario: 'During deployment, Claude returns stop_reason: "end_turn" with a text response saying "The deployment is complete!" However, your monitoring dashboard shows the deployment is still in progress — the containers are still starting up.',
      question: 'What does this indicate about the agent\'s tool use, and how should it be handled?',
      options: [
        'A) Trust Claude\'s response — the deployment is complete.',
        'B) Claude may have stopped prematurely. The agentic loop should check deployment status via a monitoring tool (e.g., check_deployment_status) to verify the actual state, rather than relying on Claude\'s statement. If the deployment is still in progress, the loop should continue.',
        'C) Add a system prompt instruction: "Always verify deployment completion before reporting success."',
        'D) Increase max_iterations to give Claude more time.',
      ],
      correct: 1,
      explanation: 'Claude\'s end_turn response is a statement of belief, not a verified fact. The agentic loop should validate the actual deployment state via monitoring tools before concluding. This is the verify-before-concluding pattern. Option A trusts an unverified claim. Option C is advisory and doesn\'t add programmatic verification. Option D doesn\'t address the premature conclusion issue.',
    },
    {
      id: 4, examScenario: 6, domain: 'd1', domainLabel: 'Agentic Loops',
      scenario: 'Your rollback agent uses an agentic loop to diagnose and fix deployment issues. During a critical incident, the agent has been running for 5 minutes across 12 iterations, trying different rollback strategies. Each iteration takes 25 seconds. The team is considering manually intervening.',
      question: 'At what point should human intervention override the automated agent?',
      options: [
        'A) Never — let the agent run until it resolves the issue.',
        'B) Implement a time-based and iteration-based override: after a configurable threshold (e.g., 5 minutes or 10 iterations without resolution), automatically escalate to human engineers with the full investigation log, allowing the agent to continue in parallel but enabling human intervention.',
        'C) After 3 iterations, always escalate to humans.',
        'D) Only intervene if the agent explicitly requests help.',
      ],
      correct: 1,
      explanation: 'Configurable time and iteration thresholds with automatic escalation ensure human awareness during critical incidents. The agent can continue investigating while humans are alerted, combining automated speed with human judgment. Option A risks extended outages. Option C is too aggressive — many issues resolve in 4-5 iterations. Option D assumes the agent will recognize when it\'s stuck, which isn\'t guaranteed.',
    },
    // ─── D2 ──────────────────────────────────────────────────────────
    {
      id: 5, examScenario: 6, domain: 'd2', domainLabel: 'System Prompt Design',
      scenario: 'Your deployment agent\'s system prompt includes rollback procedures for 5 different deployment strategies: blue-green, canary, rolling, feature flags, and A/B testing. When performing a canary rollback, the agent occasionally uses blue-green terminology and commands, creating confusion.',
      question: 'What system prompt design would prevent strategy cross-contamination?',
      options: [
        'A) Add stronger delimiters between strategy sections.',
        'B) Use dynamic prompt assembly — inject only the rollback procedure for the active deployment strategy into the system prompt, removing the other four strategies entirely.',
        'C) Add few-shot examples for each strategy.',
        'D) Include all five strategies but add: "Use only the section matching your current strategy."',
      ],
      correct: 1,
      explanation: 'Dynamic prompt assembly eliminates cross-contamination by including only the relevant strategy\'s rollback procedure. This is both more token-efficient and eliminates the possibility of using the wrong strategy\'s commands. Option A adds visual structure without preventing attention overlap. Option C adds token overhead. Option D relies on Claude reliably selecting the correct section — which the data shows it doesn\'t.',
    },
    {
      id: 6, examScenario: 6, domain: 'd2', domainLabel: 'Context Window Management',
      scenario: 'Your deployment agent processes a release with 47 modified files. The diff is 35,000 tokens. Combined with the system prompt (4,000 tokens) and deployment configuration (3,000 tokens), the agent has limited context remaining for reasoning about the deployment strategy and potential issues.',
      question: 'What context management approach would balance comprehensive review with adequate reasoning space?',
      options: [
        'A) Process all 47 files in a single pass — the model has enough context.',
        'B) Prioritize the diff by risk: categorize files as high-risk (config changes, database migrations, API changes), medium-risk (business logic), and low-risk (tests, docs). Inject only high-risk files in full, summarize medium-risk changes, and list low-risk file names. This preserves reasoning context for the most impactful changes.',
        'C) Skip the diff and deploy based on test results alone.',
        'D) Split the deployment into multiple smaller deployments.',
      ],
      correct: 1,
      explanation: 'Risk-based context prioritization ensures the most impactful changes receive full attention while preserving reasoning space. Not all 47 files need equal scrutiny — config changes and database migrations carry more deployment risk than documentation updates. Option A may leave insufficient reasoning context. Option C loses important deployment context. Option D may not be feasible for a cohesive release.',
    },
    {
      id: 7, examScenario: 6, domain: 'd2', domainLabel: 'Prompt Chaining',
      scenario: 'Your deployment pipeline chains: RiskAssessment → DeploymentPlanner → ExecutionAgent. The RiskAssessment identifies potential issues, DeploymentPlanner creates a mitigation plan, and ExecutionAgent implements it. The ExecutionAgent occasionally encounters issues not identified in the risk assessment, causing deployment failures.',
      question: 'What prompt chaining improvement would reduce unexpected deployment issues?',
      options: [
        'A) Have the ExecutionAgent handle issues as they arise without upstream planning.',
        'B) Feed the RiskAssessment\'s findings directly into the ExecutionAgent\'s context alongside the deployment plan, so it has both the planned approach and the identified risks. Add a fallback instruction: "If you encounter an issue not in the risk assessment, pause and report rather than proceeding."',
        'C) Add more detail to the RiskAssessment prompt.',
        'D) Merge all three agents into one.',
      ],
      correct: 1,
      explanation: 'Providing the ExecutionAgent with both the deployment plan AND the risk assessment gives it full context for decision-making. The fallback instruction ensures unexpected issues trigger a pause rather than blind continuation. Option A is reactive rather than proactive. Option C is reasonable but doesn\'t help the ExecutionAgent respond to the findings. Option D loses the benefits of specialized agents.',
    },
    // ─── D3 ──────────────────────────────────────────────────────────
    {
      id: 8, examScenario: 6, domain: 'd3', domainLabel: 'API Integration Patterns',
      scenario: 'Your deployment agent integrates with your cloud provider\'s API for deployment operations. The API has a rate limit of 100 requests/minute. During large deployments that create 80+ resources, you hit the rate limit 25% of the time, causing deployment failures.',
      question: 'What integration pattern would most reliably handle API rate limits during deployments?',
      options: [
        'A) Implement request queuing with token-bucket rate limiting in the tool execution layer, so API calls wait for available capacity rather than failing. Combined with exponential backoff for rate limit responses.',
        'B) Slow down all deployments to stay well under the rate limit.',
        'C) Request a rate limit increase from the cloud provider.',
        'D) Batch all API calls into fewer, larger requests.',
      ],
      correct: 0,
      explanation: 'Request queuing with token-bucket rate limiting ensures API calls never fail due to rate limits — they wait for available capacity. Combined with exponential backoff for any remaining rate limit responses, this provides reliable deployment execution. Option B unnecessarily slows all deployments. Option C is outside your control. Option D may not be feasible depending on the API\'s capabilities.',
    },
    {
      id: 9, examScenario: 6, domain: 'd3', domainLabel: 'Tool Error Handling',
      scenario: 'Your deployment tool returns: "Error: Container health check failed — timeout after 30 seconds." The agent retries the deployment 3 times with the same result. Investigation shows the container actually starts successfully but takes 45 seconds to pass health checks — the timeout is too short, not the deployment.',
      question: 'What error analysis pattern would have identified the root cause without 3 retries?',
      options: [
        'A) Always retry 3 times before investigating.',
        'B) Return structured error context including: error type, timeout value, actual container state at timeout, and logs from the health check period. This enables Claude to reason: "The container is starting but the 30-second timeout is too short. Increase the timeout to 60 seconds" rather than retrying the same failed action.',
        'C) Increase all timeouts to 5 minutes by default.',
        'D) Skip health checks during deployment.',
      ],
      correct: 1,
      explanation: 'Structured error context with diagnostic details (timeout value, actual container state) enables intelligent root cause analysis. The agent can distinguish between "container failed to start" and "container started but health check timed out" — fundamentally different issues requiring different responses. Option A wastes time on known failures. Option C is a blunt workaround. Option D removes a safety check.',
    },
    // ─── D4 ──────────────────────────────────────────────────────────
    {
      id: 10, examScenario: 6, domain: 'd4', domainLabel: 'System Prompt Architecture',
      scenario: 'Your deployment agent needs to enforce a strict deployment checklist: pre-deployment verification, deployment steps, post-deployment validation, and rollback procedures. The checklist has 23 items. When the system prompt exceeds 3,500 tokens, the agent occasionally skips checklist items.',
      question: 'What architectural approach would most reliably enforce the full checklist?',
      options: [
        'A) Add stronger language: "You MUST complete ALL checklist items."',
        'B) Implement the checklist programmatically as a state machine or ordered sequence enforced by the deployment orchestration code, not just the system prompt. Each checklist item becomes a required tool call with a validation gate that must pass before the next step executes.',
        'C) Split the checklist into smaller groups and process them sequentially.',
        'D) Repeat the full checklist at the end of the deployment to verify completion.',
      ],
      correct: 1,
      explanation: 'Programmatic enforcement via a state machine provides deterministic guarantees that system prompt instructions cannot. Each checklist item becomes a required step with a validation gate — the deployment cannot proceed to the next step until the current one passes. This is the standard approach for critical operational procedures. Option A relies on probabilistic compliance. Option C doesn\'t solve the underlying issue. Option D is reactive rather than preventive.',
    },
    {
      id: 11, examScenario: 6, domain: 'd4', domainLabel: 'Validation & Retry Strategies',
      scenario: 'Your deployment validation checks 15 metrics (error rate, latency, CPU, memory, etc.) after each deployment. Currently, all 15 must pass within a 5-minute window. 20% of deployments initially fail validation due to transient metric spikes that resolve within 10 minutes.',
      question: 'What validation strategy would reduce false-positive deployment failures?',
      options: [
        'A) Extend the validation window to 30 minutes for all deployments.',
        'B) Implement a two-phase validation: a strict immediate check (critical metrics like error rate >5%) and a gradual stabilization check (non-critical metrics have a 15-minute stabilization window before evaluation). This distinguishes between real failures and transient settling effects.',
        'C) Remove non-critical metrics from the validation.',
        'D) Accept a 20% false-positive rate as the cost of thorough validation.',
      ],
      correct: 1,
      explanation: 'Two-phase validation separates critical failure indicators from transient settling effects. Error rates and crash counts need immediate validation; CPU and memory metrics need time to stabilize after a deployment. Option A delays all deployments unnecessarily. Option C reduces safety coverage. Option D accepts a solvable problem.',
    },
    {
      id: 12, examScenario: 6, domain: 'd4', domainLabel: 'Batch Processing',
      scenario: 'Your team deploys 30 microservices independently. Each deployment requires a pre-deployment Claude analysis (cost: $0.005). During a coordinated release, all 30 services need analysis. Currently, you process them sequentially with real-time API calls.',
      question: 'What is the most cost-effective approach for batch deployment analysis?',
      options: [
        'A) Continue sequential real-time processing.',
        'B) Use the Message Batches API for all 30 pre-deployment analyses. Since these are pre-deployment checks (not blocking — developers can review results when ready), the batch processing time is acceptable. This saves 50% on analysis costs.',
        'C) Analyze only the 10 most critical services.',
        'D) Combine all 30 service analyses into a single API call.',
      ],
      correct: 1,
      explanation: 'Pre-deployment analysis is inherently batchable — it\'s a preparation step, not a blocking gate. The Message Batches API processes all 30 analyses at 50% cost savings. Results are ready for review before deployment begins. Option A pays full price unnecessarily. Option C reduces coverage. Option D degrades per-service analysis quality.',
    },
    {
      id: 13, examScenario: 6, domain: 'd4', domainLabel: 'Multi-Instance Review',
      scenario: 'Your deployment review uses a single Claude instance to assess deployment risk. It has a 15% false-positive rate (flagging safe deployments as risky) and a 5% false-negative rate (approving risky deployments). You\'re considering adding a second review instance.',
      question: 'What multi-instance pattern would minimize both false positives and false negatives?',
      options: [
        'A) Run two instances and approve only if both approve (minimizes false negatives but increases false positives).',
        'B) Run two instances and approve if either approves (minimizes false positives but increases false negatives).',
        'C) Run two instances with different perspectives: one optimized for detecting risks (conservative) and one optimized for identifying safe patterns (liberal). If they agree, use their consensus. If they disagree, escalate to human review. This catches the cases where perspective matters.',
        'D) A single instance with a better prompt is always superior to multi-instance review.',
      ],
      correct: 2,
      explanation: 'Complementary perspectives (conservative risk detection + liberal safety detection) cover both failure modes. When they agree, confidence is high in either direction. Disagreements flag genuine ambiguity that benefits from human judgment. Option A is overly conservative. Option B is overly permissive. Option D may improve single-instance performance but doesn\'t provide the redundancy that catches edge cases.',
    },
    // ─── D5 ──────────────────────────────────────────────────────────
    {
      id: 14, examScenario: 6, domain: 'd5', domainLabel: 'Context Management',
      scenario: 'Your deployment agent maintains state across a multi-service deployment. After deploying services A, B, and C (with detailed logs and health checks), it starts deploying service D. The context from services A-C is consuming 80% of the context window, leaving limited space for service D\'s deployment reasoning.',
      question: 'What context management strategy would ensure adequate reasoning space for each service deployment?',
      options: [
        'A) Process all services in a single context window.',
        'B) Implement state compression between service deployments: after each service is deployed and verified, compress its deployment details into a structured summary (service name, status, health, key metrics) and discard the raw logs. This preserves critical state while freeing context for subsequent deployments.',
        'C) Deploy each service in a completely separate session.',
        'D) Use a larger context window model.',
      ],
      correct: 1,
      explanation: 'State compression between deployments preserves essential information (which services deployed successfully, their health status) while freeing context for the next deployment. Raw logs from service A are no longer needed when deploying service D — only the outcome matters. Option A risks context exhaustion. Option C loses deployment coordination context. Option D delays the problem at higher cost.',
    },
    {
      id: 15, examScenario: 6, domain: 'd5', domainLabel: 'Escalation & Ambiguity',
      scenario: 'Your deployment agent detects an anomaly during a canary deployment: error rate increased from 0.1% to 0.4% in the canary group. The baseline error rate is 0.1% with normal variation up to 0.3%. The agent\'s confidence that this is a deployment-caused issue is 0.62.',
      question: 'What is the most appropriate action given these metrics?',
      options: [
        'A) Automatically roll back — the error rate increase is concerning.',
        'B) Continue the canary deployment but increase monitoring frequency and set an automatic rollback trigger at 0.5% error rate. The 0.4% rate is above normal variation but below a clear failure threshold. Extended observation with a defined rollback trigger provides more data before deciding.',
        'C) Ignore the anomaly — 0.4% is still a low error rate.',
        'D) Immediately escalate to the entire engineering team.',
      ],
      correct: 1,
      explanation: 'The 0.4% error rate is in the ambiguous zone — above normal variation (0.3%) but not clearly a deployment-caused failure. The most appropriate response is enhanced monitoring with a defined rollback trigger, gathering more data before taking irreversible action. Option A is premature — the increase might stabilize. Option C is risky — it might be a real issue. Option D causes unnecessary alarm for an ambiguous signal.',
    },
    {
      id: 16, examScenario: 6, domain: 'd5', domainLabel: 'Error Propagation',
      scenario: 'A canary deployment fails: the new version has a memory leak causing containers to restart. The deployment agent detects this and initiates rollback. During rollback, the previous version\'s deployment also fails because the configuration was updated for the new version and is incompatible with the old version.',
      question: 'What error propagation pattern would have prevented the failed rollback?',
      options: [
        'A) Always create a configuration backup before deployment.',
        'B) Implement atomic deployment with configuration snapshots: before any deployment change, snapshot the current configuration state. If rollback is needed, restore the exact configuration snapshot alongside the previous version. The deployment and its configuration changes should be treated as a single atomic unit.',
        'C) Never update configuration during deployments.',
        'D) Roll back only the application code, not the configuration.',
      ],
      correct: 1,
      explanation: 'Atomic deployment with configuration snapshots treats the entire deployment state (application code + configuration + infrastructure) as a single unit that can be rolled back together. This prevents partial rollbacks where code reverts but configuration doesn\'t. Option A is a good practice but doesn\'t ensure automatic restoration. Option C is unrealistic — configuration changes are often necessary. Option D is exactly the mistake that caused the failed rollback.',
    },
    {
      id: 17, examScenario: 6, domain: 'd5', domainLabel: 'Provenance & Attribution',
      scenario: 'Over the past month, your deployment agent made 200 deployments. A production issue is traced to a configuration change made 3 weeks ago. You need to determine which deployment introduced the change and why.',
      question: 'What provenance information should be recorded for each deployment to support this investigation?',
      options: [
        'A) Deployment timestamp and version number only.',
        'B) For each deployment: timestamp, deployer (agent vs. human), triggering event (commit, schedule, manual), all configuration changes with before/after values, risk assessment, approval chain, and post-deployment metrics. This creates a complete audit trail for any investigation.',
        'C) Git commit hash only — everything else is in version control.',
        'D) Only record deployments that fail.',
      ],
      correct: 1,
      explanation: 'A complete deployment record with configuration change details (before/after values) and the decision chain (trigger, risk assessment, approval) enables root cause investigation weeks or months later. Git history shows code changes but not the reasoning behind deployment decisions. Option A is insufficient — it doesn\'t show what changed. Option C misses configuration and reasoning context. Option D loses information about successful changes that may cause issues later.',
    },
    {
      id: 18, examScenario: 6, domain: 'd5', domainLabel: 'Human Review & Confidence',
      scenario: 'Your deployment agent proposes a database schema migration with 0.91 confidence. The migration adds a new column with a default value. The agent\'s risk assessment shows: "Low risk — additive change, non-breaking." The database serves 2 million active users.',
      question: 'Should this migration proceed automatically despite the high confidence?',
      options: [
        'A) Yes — 0.91 confidence and low risk assessment justifies automatic execution.',
        'B) No — database schema changes on systems serving 2M users should require human review regardless of confidence or risk assessment. The blast radius of a migration error (data corruption, downtime) is too large for fully automated execution, even with high confidence.',
        'C) Yes, but run it during off-peak hours.',
        'D) Yes, but create a backup first.',
      ],
      correct: 1,
      explanation: 'Database schema migrations on high-traffic systems are inherently high-stakes operations. Even "additive, non-breaking" changes can have unexpected consequences (locking the table during migration, triggering cascading effects in application code). The principle is: blast radius should determine review requirements, not confidence scores. Options C and D add mitigations but still automate a decision that warrants human judgment for 2M-user systems.',
    },
    {
      id: 19, examScenario: 6, domain: 'd5', domainLabel: 'Context Management',
      scenario: 'Your deployment agent maintains a deployment history for context. After 50 deployments, the history consumes 30,000 tokens. The agent references outdated deployment patterns from 30+ deployments ago that are no longer relevant, occasionally applying deprecated configurations.',
      question: 'What context management pattern would keep deployment history relevant?',
      options: [
        'A) Keep the full history for completeness.',
        'B) Implement history windowing: maintain the last 10 deployments in full detail, keep a compressed summary of older deployments (key metrics only), and remove deployments older than 30 days. This keeps relevant recent context while preventing stale patterns from influencing current decisions.',
        'C) Delete all history after each deployment.',
        'D) Store history in a database and query it via tool when needed.',
      ],
      correct: 1,
      explanation: 'History windowing with compression keeps the most relevant context (recent deployments) while preventing stale patterns from contaminating current decisions. The 10-deployment window balances recency with pattern recognition. Option A allows stale context to accumulate. Option C loses valuable deployment pattern information. Option D is a good complement but adds latency for context retrieval.',
    },
    {
      id: 20, examScenario: 6, domain: 'd5', domainLabel: 'Codebase Exploration',
      scenario: 'You\'re using Claude Code to update your deployment scripts to support a new container orchestration platform. Your current scripts are in deploy/ with 12 Bash scripts and 3 Python utilities. You want Claude to understand the current architecture before making changes.',
      question: 'What exploration strategy would most efficiently prepare Claude for the update?',
      options: [
        'A) Read all 15 files in deploy/ to build a complete understanding.',
        'B) Read the main deployment script (likely deploy.sh or similar) to understand the entry point and flow, then follow function calls to understand the architecture. Read the Python utilities only if they\'re referenced by the main script.',
        'C) Read only the README.md in the deploy/ directory.',
        'D) Start writing new scripts from scratch without reading existing ones.',
      ],
      correct: 1,
      explanation: 'Starting from the entry point and following the call chain is the most efficient exploration strategy — it discovers the actual deployment flow and only reads files that participate in it. This is dramatically faster than reading everything. Option A wastes context on potentially unused scripts. Option C may miss implementation details. Option D risks duplicating functionality and missing important existing logic.',
    },
  ],
};

export default exam;
