import type { ScenarioExamData } from './types';

const exam: ScenarioExamData = {
  id: 6,
  title: 'Healthcare Data Analysis',
  description: 'Design a multi-agent system for processing medical records, ensuring compliance with HIPAA requirements, accuracy in clinical data extraction, and safe escalation protocols.',
  icon: '🏥',
  questions: [
    { id: 1, examScenario: 6, domain: 'd5', domainLabel: 'Context Management', examTask: '5.1 Conversation Context Management', scenario: 'A healthcare agent processes patient records spanning 40+ pages of clinical notes. After processing page 25, the agent loses track of the patient\'s allergy list documented on page 3. On page 28, it recommends a medication that conflicts with the documented allergy.', question: 'What context management approach would prevent this dangerous oversight?', options: ['A) Re-read the full patient record before each medication recommendation.', 'B) Extract critical safety information (allergies, conditions, current medications) into a persistent "patient context block" that\'s injected at the start of every processing step, ensuring safety data is always visible.', 'C) Limit processing to 10 pages at a time with manual review between batches.', 'D) Add a system prompt instruction to remember all allergies.'], correct: 1, explanation: 'A persistent patient context block with critical safety information ensures allergies are always visible regardless of document position. A wastes tokens on repeated full reads. C adds excessive manual overhead. D relies on instruction following for safety-critical data — unacceptable in healthcare. [Task 5.1 — Context Management: persistent safety context blocks prevent information loss in safety-critical document processing.]' },
    { id: 2, examScenario: 6, domain: 'd5', domainLabel: 'Escalation & Ambiguity', examTask: '5.2 Escalation & Ambiguity Handling', scenario: 'Your clinical data extraction agent encounters a lab result with ambiguous units (the value could be mg/dL or μg/mL depending on the test type). The difference changes the interpretation from "normal" to "critically abnormal." The agent guesses "normal" based on statistical frequency.', question: 'What is the correct handling for this ambiguity?', options: ['A) Flag the result as ambiguous with both interpretations, escalate to a human reviewer with the specific ambiguity documented, and mark the record as "pending verification" — never guess on clinical interpretations.', 'B) Default to the more common interpretation (mg/dL) since it\'s statistically more likely.', 'C) Use context clues from surrounding text to infer the most likely unit.', 'D) Query a medical database for the most common unit for this test type.'], correct: 0, explanation: 'In healthcare, ambiguity with clinical impact must always be escalated — never guessed. Both interpretations must be presented with the escalation. B gambles with patient safety. C and D make educated guesses, which are inappropriate when the consequence is misclassifying a critical value as normal. [Task 5.2 — Escalation: clinical ambiguity with safety implications must always be escalated, never statistically guessed.]' },
    { id: 3, examScenario: 6, domain: 'd4', domainLabel: 'Structured Output', examTask: '4.3 Structured Output (tool_use + JSON Schema)', scenario: 'Your agent extracts medication lists from clinical notes. Free-text extraction produces inconsistent formats: "metformin 500mg twice daily", "Metformin 500 mg BID", "metformin 500 milligrams 2x/day". Downstream systems require structured data.', question: 'What approach would produce consistent, structured medication data?', options: ['A) Add formatting instructions listing the required output format for medications.', 'B) Define a medication_schema tool with structured fields (medication_name, dosage_value, dosage_unit, frequency, route) using JSON Schema with enums for units and routes, enforced via tool_choice.', 'C) Post-process free-text output with regex patterns.', 'D) Use a second Claude call to parse free-text medication into structured data.'], correct: 1, explanation: 'JSON Schema with enums for units and routes, enforced via tool_choice, guarantees consistent structured output at the source. A relies on instruction following. C is fragile and doesn\'t handle variations reliably. D adds cost and latency for what should be a single-pass operation. [Task 4.3 — Structured Output: JSON Schema with enums enforced via tool_choice produces consistent structured medication data.]' },
    { id: 4, examScenario: 6, domain: 'd1', domainLabel: 'Agentic Loops', examTask: '1.1 Agentic Loops', scenario: 'Your patient intake agent uses this loop: collect_symptoms → lookup_conditions → recommend_tests → generate_report. For 18% of cases, the recommended tests are irrelevant because the symptom collection step misses key details that the agent doesn\'t follow up on.', question: 'What loop modification would improve test recommendations?', options: ['A) Add a clarification sub-loop after symptom collection: analyze collected symptoms, identify gaps (missing duration, severity, triggers), ask targeted follow-up questions, then proceed to condition lookup only when symptom completeness exceeds a threshold.', 'B) Increase the number of recommended tests to cover more possibilities.', 'C) Skip the lookup_conditions step and go directly to recommending a broad panel of tests.', 'D) Have the patient fill out a comprehensive form before interacting with the agent.'], correct: 0, explanation: 'A clarification sub-loop with targeted follow-up questions addresses the root cause — incomplete symptom data. This ensures the condition lookup operates on sufficient information. B and C are over-testing approaches that increase costs and patient burden. D shifts effort to the patient rather than improving the agent. [Task 1.1 — Agentic Loops: a clarification sub-loop after initial collection ensures sufficient data before downstream processing.]' },
    { id: 5, examScenario: 6, domain: 'd1', domainLabel: 'Multi-Agent Orchestration', examTask: '1.2 Multi-Agent Orchestration', scenario: 'Your healthcare system has: IntakeAgent (collects symptoms), DiagnosisAgent (suggests differential diagnosis), TreatmentAgent (recommends treatment), and ComplianceAgent (checks for drug interactions and contraindications). Currently all run sequentially in 12 seconds.', question: 'What orchestration change would improve efficiency while maintaining safety?', options: ['A) Run all four agents in parallel for maximum speed.', 'B) Run IntakeAgent first, then run DiagnosisAgent and ComplianceAgent\'s history-check in parallel (both need intake data but not each other). Finally run TreatmentAgent with both diagnosis results and compliance clearance.', 'C) Remove ComplianceAgent and add compliance checks to TreatmentAgent.', 'D) Run TreatmentAgent and ComplianceAgent in parallel since they\'re independent.'], correct: 1, explanation: 'Parallelizing diagnosis and compliance history-check after intake reduces total time while maintaining the safety requirement that treatment runs only after both diagnosis and compliance clearance. A violates dependencies. C removes a critical safety layer. D is dangerous — treatment without compliance clearance could recommend contraindicated drugs. [Task 1.2 — Multi-Agent Orchestration: parallelizing independent agents after shared dependencies completes reduces time while maintaining safety.]' },
    { id: 6, examScenario: 6, domain: 'd3', domainLabel: 'CLAUDE.md Hierarchy', examTask: '3.1 CLAUDE.md Hierarchy & Context Loading', scenario: 'Your healthcare agent processes data for multiple hospital departments (Emergency, Cardiology, Oncology). Each department has different terminology, protocols, and extraction rules. Currently the agent uses a single configuration that\'s too generic for all departments, achieving 65% extraction accuracy.', question: 'What CLAUDE.md structure would improve department-specific accuracy?', options: ['A) Create a single comprehensive CLAUDE.md with all department rules.', 'B) Use a hierarchical structure: root CLAUDE.md with shared healthcare rules (HIPAA, safety protocols), and department-specific CLAUDE.md files (cardiology/, oncology/, emergency/) with specialized terminology and extraction rules, loaded based on document classification.', 'C) Create separate agents for each department.', 'D) Add department-specific examples to the system prompt.'], correct: 1, explanation: 'Hierarchical CLAUDE.md with shared root rules and department-specific overrides provides both consistent safety protocols and specialized extraction accuracy. A creates an unwieldy file with conflicting rules. C duplicates infrastructure and maintenance. D doesn\'t scale with prompt length as departments increase. [Task 3.1 — CLAUDE.md Hierarchy: root + department-specific files provide shared safety protocols with specialized extraction accuracy.]' },
    { id: 7, examScenario: 6, domain: 'd5', domainLabel: 'Provenance & Attribution', examTask: '5.6 Information Provenance & Uncertainty', scenario: 'Your clinical summary agent produces patient summaries. Auditors found that 12% of summaries contain details not present in the source records — the agent infers information from medical knowledge (e.g., adding typical side effects of a medication the patient takes).', question: 'What provenance approach would prevent inferred content from appearing in summaries?', options: ['A) Add a system prompt instruction: "Only include information from the source document."', 'B) Require every claim in the summary to include a source citation (document section, page, line). Claims without citations are automatically flagged and removed. Run a verification pass comparing cited sources against the original.', 'C) Use a lower temperature to reduce creative additions.', 'D) Have a second agent review the summary for accuracy.'], correct: 1, explanation: 'Mandatory source citations with automated verification structurally prevent inference — every claim must be traceable to the source document. A relies on instruction following for compliance-critical output. C doesn\'t prevent inference when the model lacks source information. D adds cost and doesn\'t prevent the initial inference. [Task 5.6 — Provenance: mandatory source citations with verification structurally prevent inferred content in clinical summaries.]' },
    { id: 8, examScenario: 6, domain: 'd2', domainLabel: 'Effective Tool Interfaces', examTask: '2.1 Effective Tool Interfaces', scenario: 'Your healthcare agent has lookup_drug and lookup_condition tools. The agent sometimes uses lookup_drug when asked about a condition ("What is hypertension?") and lookup_condition when asked about a drug ("What is metformin used for?"). This happens 22% of the time.', question: 'What tool interface change would reduce misselection?', options: ['A) Merge both tools into a single lookup_medical tool.', 'B) Update tool descriptions with explicit scope boundaries: lookup_drug ("For pharmacological queries: drug names, dosages, interactions, side effects, contraindications — input should be a medication name"), lookup_condition ("For clinical queries: diseases, conditions, symptoms, diagnostic criteria, treatment guidelines — input should be a condition name").', 'C) Add a routing step that classifies the query type before tool selection.', 'D) Remove one tool and handle both query types in a single tool.'], correct: 1, explanation: 'Explicit scope boundaries in tool descriptions enable correct selection at the decision point. The model sees "input should be a medication name" vs "input should be a condition name" when choosing. A loses specialization. C adds infrastructure when a description fix suffices. D removes capability. [Task 2.1 — Tool Interfaces: explicit scope boundaries with input guidance in tool descriptions reduce misselection without adding architectural complexity.]' },
    { id: 9, examScenario: 6, domain: 'd4', domainLabel: 'Validation & Retry', examTask: '4.4 Validation, Retry & Feedback Loops', scenario: 'Your clinical data extraction agent produces results that fail validation 14% of the time — wrong units on lab values, impossible vital signs (e.g., heart rate of 340), and missing required fields. Currently you regenerate the entire extraction.', question: 'What retry approach is most efficient for clinical data?', options: ['A) Retry the full extraction with a different model.', 'B) Include specific validation errors in the retry: "heart_rate: 340 is outside physiological range (40-200 bpm). lab_value glucose: 95 — missing unit, expected mg/dL or mmol/L. Please correct these fields." Also flag corrected fields for human verification since this is clinical data.', 'C) Discard documents that fail validation and flag for manual entry.', 'D) Add more detailed extraction instructions to prevent errors.'], correct: 1, explanation: 'Specific error feedback enables targeted corrections while flagging for human verification ensures clinical accuracy. A is wasteful and doesn\'t address the specific errors. C discards data unnecessarily. D is good prevention but doesn\'t handle errors when they occur. [Task 4.4 — Validation & Retry: specific error context in retries with human verification of corrections balances efficiency and clinical safety.]' },
    { id: 10, examScenario: 6, domain: 'd1', domainLabel: 'Task Decomposition', examTask: '1.6 Task Decomposition Strategies', scenario: 'You need to process 500 patient records for a clinical study. Each record has demographics, medical history, lab results, and treatment plans. Processing sequentially takes 8 hours. Records are independent — processing order doesn\'t matter.', question: 'What decomposition would be most efficient?', options: ['A) Process all records in a single agent session.', 'B) Decompose into 10 parallel batches of 50 records each, with a consistent extraction schema applied to all batches and a final aggregation step that validates consistency across batches.', 'C) Decompose by record section: process all demographics, then all histories, then all lab results.', 'D) Process records sequentially but with a faster model.'], correct: 1, explanation: 'Parallel batches with a consistent schema and aggregation step reduces processing time by ~10x while ensuring consistency. A takes 8 hours sequentially. C breaks intra-record context (lab results lose connection to demographics). D reduces quality for speed. [Task 1.6 — Task Decomposition: parallel batches with shared schema and aggregation optimizes throughput for independent records.]' },
    { id: 11, examScenario: 6, domain: 'd2', domainLabel: 'Tool Distribution & tool_choice', examTask: '2.3 Tool Distribution & tool_choice', scenario: 'Your clinical agent has tools: check_interactions, verify_dosage, and lookup_guideline. For new prescriptions, it must check interactions first, then verify dosage, then check guidelines. Currently the agent sometimes skips interactions and goes straight to dosage verification.', question: 'What tool_choice configuration enforces the required order?', options: ['A) Set tool_choice to "auto" with a system prompt specifying the required order.', 'B) Use a stateful orchestration approach: first turn forces check_interactions via tool_choice, second turn forces verify_dosage, third turn forces lookup_guideline. Each turn only enables the required tool.', 'C) Combine all three checks into a single verify_prescription tool.', 'D) Set tool_choice to "required" to ensure at least one tool is called each turn.'], correct: 1, explanation: 'Stateful tool_choice forces each step in order by only enabling the required tool per turn. This makes skipping interactions impossible — the model can only call check_interactions on turn 1. A relies on instruction following for safety-critical ordering. C loses modularity and the ability to update individual checks. D forces any tool, not the correct one. [Task 2.3 — Tool Distribution: stateful tool_choice with per-turn tool enabling enforces critical safety sequences deterministically.]' },
    { id: 12, examScenario: 6, domain: 'd4', domainLabel: 'Prompts with Explicit Criteria', examTask: '4.1 Prompts with Explicit Criteria', scenario: 'Your clinical coding agent assigns ICD-10 codes to diagnoses. Accuracy is 70%. The prompt says: "Assign the appropriate ICD-10 code." Most errors come from confusion between similar codes (e.g., E11.9 "Type 2 diabetes without complications" vs E11.65 "Type 2 diabetes with hyperglycemia").', question: 'What prompt modification would most effectively reduce coding errors?', options: ['A) Add few-shot examples for every ICD-10 code the agent might encounter.', 'B) Provide explicit selection criteria for commonly confused code pairs: "Assign E11.65 ONLY when the clinical notes explicitly state \'hyperglycemia\' or report fasting glucose > 125 mg/dL. If glucose levels are normal or unstated, use E11.9."', 'C) Use a larger model with better medical knowledge.', 'D) Have the agent list multiple possible codes and select the most common one.'], correct: 1, explanation: 'Explicit criteria with specific triggers for commonly confused codes directly addresses the boundary ambiguity. The agent needs clear rules like "E11.65 ONLY when hyperglycemia is explicitly stated." A doesn\'t scale and doesn\'t address boundary issues. C may improve but doesn\'t guarantee correct boundary decisions. D introduces statistical guessing into clinical coding. [Task 4.1 — Explicit Criteria: specific triggers for commonly confused code pairs reduce clinical coding errors more than volume-based examples.]' },
    { id: 13, examScenario: 6, domain: 'd3', domainLabel: 'Iterative Refinement', examTask: '3.5 Iterative Refinement', scenario: 'Your clinical note summarizer achieves 82% accuracy on typed notes but 55% on dictated notes (with speech recognition artifacts like "patient has a history of MI" transcribed as "patient has a history of em eye"). You want to improve dictation accuracy.', question: 'What refinement approach targets this without affecting typed note performance?', options: ['A) Add examples of common speech recognition errors to the general prompt.', 'B) Create a dictation-specific preprocessing prompt that expands common medical abbreviations and corrects known speech recognition patterns before summarization. Iteratively refine against a test set of dictated notes.', 'C) Retrain the speech recognition model.', 'D) Have physicians type their notes instead of dictating.'], correct: 1, explanation: 'A dictation-specific preprocessing layer targets the root cause (speech artifacts) without changing the working summarizer for typed notes. Iterative refinement against dictated notes improves the preprocessing over time. A dilutes the general prompt. C is outside the agent\'s scope. D is impractical. [Task 3.5 — Iterative Refinement: a targeted preprocessing layer for specific input types addresses weaknesses without affecting general performance.]' },
    { id: 14, examScenario: 6, domain: 'd5', domainLabel: 'Human Review & Confidence', examTask: '5.5 Human Review & Confidence Calibration', scenario: 'Your clinical decision support agent recommends treatments with a confidence score. When confidence is 90%+, accuracy is 92%. At 70-89%, accuracy is 75%. At below 70%, accuracy is 50%. A wrong recommendation could have serious patient safety implications.', question: 'What review threshold is appropriate for clinical recommendations?', options: ['A) Allow autonomous processing at 70%+ confidence since 75% accuracy is acceptable.', 'B) Require physician review for all recommendations below 90% confidence. The 92% accuracy at high confidence is acceptable for decision support (with physician confirmation), but 75% and 50% are not safe enough for autonomous operation in clinical settings.', 'C) Require physician review for all recommendations regardless of confidence.', 'D) Set the threshold at 80% confidence as a balance.'], correct: 1, explanation: 'For clinical decision support, only the 92% accuracy tier (90%+ confidence) is appropriate for autonomous recommendations, and even then with physician confirmation. The 75% tier is too risky for patient safety. A is dangerous — 75% accuracy means 1 in 4 recommendations is wrong. C eliminates the efficiency benefit. D accepts 75-92% accuracy range which is inconsistent. [Task 5.5 — Human Review: clinical applications require higher confidence thresholds than general applications due to patient safety implications.]' },
    { id: 15, examScenario: 6, domain: 'd1', domainLabel: 'Workflow Enforcement', examTask: '1.4 Workflow Enforcement & Structured Handoffs', scenario: 'Your healthcare pipeline requires: patient_verification → record_retrieval → data_extraction → clinical_review → output. The agent sometimes skips patient_verification and proceeds with data from the wrong patient record — a critical safety failure.', question: 'What enforcement mechanism would prevent patient mismatch?', options: ['A) Add a system prompt instruction: "Always verify patient identity before accessing records."', 'B) Implement mandatory verification where the record_retrieval tool requires a verification_token output by patient_verification — making it impossible to retrieve records without completing verification.', 'C) Add a post-processing check that compares the patient name in the output with the requested patient.', 'D) Display the patient name prominently in the system prompt.'], correct: 1, explanation: 'Token-based enforcement where record_retrieval requires a verification_token makes skipping patient_verification impossible — the retrieval tool cannot be called without the token. A relies on instruction following for a critical safety step. C catches errors after they occur rather than preventing them. D doesn\'t enforce the verification step. [Task 1.4 — Workflow Enforcement: token-based tool dependencies prevent critical safety steps from being skipped.]' },
    { id: 16, examScenario: 6, domain: 'd1', domainLabel: 'Subagent Invocation', examTask: '1.3 Subagent Invocation & Context Isolation', scenario: 'Your healthcare system spawns a subagent per patient record. Patient A\'s subagent occasionally leaks data into Patient B\'s context (e.g., mentioning Patient A\'s medications when processing Patient B). Cross-contamination occurs in 3% of cases — a HIPAA violation.', question: 'What context isolation would prevent cross-patient contamination?', options: ['A) Add instructions: "Do not reference any data from other patients."', 'B) Ensure each subagent receives only its patient\'s data in a completely isolated context — no shared conversation history, no access to other subagents\' outputs, with the parent orchestrator maintaining strict data boundaries between subagent invocations.', 'C) Process patients sequentially to prevent any overlap.', 'D) Use separate model instances for each patient.'], correct: 1, explanation: 'Complete context isolation — no shared history, no cross-subagent data access — structurally prevents HIPAA violations. A relies on instruction following for compliance. C prevents parallelism unnecessarily. D is impractical and doesn\'t address the root cause (shared context, not shared model). [Task 1.3 — Subagent Invocation: complete context isolation between patient subagents structurally prevents HIPAA violations.]' },
    { id: 17, examScenario: 6, domain: 'd2', domainLabel: 'MCP Server Integration', examTask: '2.4 MCP Server Integration', scenario: 'Your healthcare agent connects to an MCP-based lab results server. The server returns results in HL7 format, which your agent needs to parse. Occasionally the HL7 parser fails on non-standard formatting, and the agent proceeds with incomplete data.', question: 'What integration pattern would handle HL7 parsing failures?', options: ['A) Add robust HL7 parsing with structured error reporting: return parsed results with a completeness flag and list of unparseable segments. When completeness < 100%, flag for manual review rather than proceeding with partial data.', 'B) Skip unparseable segments and process available data.', 'C) Retry the HL7 parser with different configurations until it succeeds.', 'D) Convert HL7 to JSON before agent processing.'], correct: 0, explanation: 'Structured error reporting with completeness flagging ensures incomplete data is flagged for human review rather than silently processed. B processes incomplete clinical data — dangerous. C may not converge and delays results. D adds a preprocessing step but doesn\'t handle conversion failures. [Task 2.4 — MCP Server Integration: completeness-aware parsing with human review for partial results ensures clinical data integrity.]' },
    { id: 18, examScenario: 6, domain: 'd4', domainLabel: 'Few-Shot Prompting', examTask: '4.2 Few-Shot Prompting', scenario: 'Your medical entity extraction agent uses zero-shot prompting and achieves 68% accuracy on rare conditions. Adding 5 generic examples per entity type improves to 74%. But rare conditions (prevalence < 1%) still have only 55% accuracy.', question: 'What few-shot strategy would most efficiently improve rare condition extraction?', options: ['A) Add 20 more generic examples per entity type.', 'B) Replace generic examples with 3 targeted examples specifically for rare conditions, showing the exact extraction patterns for commonly missed rare diseases — fewer total examples but focused on the failure cases.', 'C) Use chain-of-thought prompting for all entity extraction.', 'D) Switch to a medical-specialized model.'], correct: 1, explanation: 'Targeted examples for failure cases (rare conditions) address the specific weakness without bloating the prompt with generic examples. 3 focused examples outperform 20 generic ones. A adds token overhead for marginal gain on rare conditions. C changes the reasoning approach without providing specific rare condition patterns. D may help but doesn\'t address the few-shot strategy. [Task 4.2 — Few-Shot Prompting: targeted examples for failure modes outperform volume-based approaches for rare conditions.]' },
    { id: 19, examScenario: 6, domain: 'd3', domainLabel: 'CI/CD Integration', examTask: '3.6 CI/CD Integration & Prompt Registry', scenario: 'Your clinical extraction prompt is updated weekly by the ML team. Last week\'s update changed a key instruction and reduced extraction accuracy from 85% to 72%. The regression was caught 3 days later by a manual audit.', question: 'What CI/CD practice would catch prompt regressions faster?', options: ['A) Require manual testing before every prompt update.', 'B) Implement automated regression testing: maintain a golden dataset of clinical documents with verified extractions. Run the new prompt against this dataset on every update and block deployment if accuracy drops more than 2% from the baseline.', 'C) Roll back to the previous prompt when accuracy drops.', 'D) Have two agents process the same data and compare results.'], correct: 1, explanation: 'Automated regression testing with a golden dataset catches accuracy drops immediately on update, blocking deployment before the regression reaches production. A is slow and inconsistent. C is reactive, not preventive. D doubles processing cost and doesn\'t establish a quality baseline. [Task 3.6 — CI/CD Integration: automated regression testing against golden datasets catches prompt regressions before deployment.]' },
    { id: 20, examScenario: 6, domain: 'd1', domainLabel: 'Session State', examTask: '1.7 Session State, Persistence & Resumption', scenario: 'Your healthcare agent processes a 200-patient batch. After processing 140 patients, the session crashes. On restart, it begins from patient 1 — wasting the 140 completed extractions and delaying results by hours.', question: 'What session persistence would enable efficient resumption?', options: ['A) Store all 140 results in memory and reprocess on restart.', 'B) Implement checkpoint persistence: save completed patient IDs and results after every 20 patients. On restart, load the checkpoint, skip processed patients, and resume from patient 141.', 'C) Process patients in smaller batches of 20 to minimize loss on crash.', 'D) Require manual intervention to specify which patient to resume from.'], correct: 1, explanation: 'Periodic checkpoint persistence saves progress incrementally, enabling resumption from the last checkpoint (patient 141) rather than from the beginning. A wastes the completed work if not persisted. C reduces batch size but doesn\'t solve the resumption problem. D shifts operational burden to humans. [Task 1.7 — Session State: periodic checkpoints enable efficient resumption of batch healthcare processing after crashes.]' },
  ],
};

export default exam;
