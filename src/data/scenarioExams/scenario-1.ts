import type { ScenarioExamData } from './types';

const exam: ScenarioExamData = {
  id: 1,
  title: 'Customer Support Resolution Agent',
  description: 'Design and troubleshoot an autonomous Claude agent that handles customer support tickets — refunds, order lookups, account updates, and escalations.',
  icon: '🎧',
  questions: [
    // ─── D1 — Agentic Architecture ──────────────────────────────────
    {
      id: 1, examScenario: 1, domain: 'd1', domainLabel: 'Agentic Loops',
      scenario: 'Your support agent handles 500 tickets/day using an agentic loop. During peak hours, 2% of conversations loop indefinitely — Claude calls lookup_order with the same order ID 6-8 times without concluding. Each redundant call adds 300ms latency.',
      question: 'What is the most effective architectural fix for this looping behavior?',
      options: [
        'A) Add a system prompt instruction: "Never call the same tool twice in one conversation."',
        'B) Implement tool-call deduplication in the agentic loop: track repeated tool calls with identical parameters, and on the second occurrence inject a message to Claude with the previous result and an instruction to proceed.',
        'C) Set max_iterations to 5 and return whatever partial response Claude has at that point.',
        'D) Replace the agentic loop with a fixed state machine that enforces a linear tool-calling sequence.',
      ],
      correct: 1,
      explanation: 'Tool-call deduplication directly addresses the root cause — Claude getting stuck in a loop calling the same tool with the same parameters. Injecting the previous result with a meta-instruction breaks the cycle deterministically. Option A is advisory and unreliable for preventing loops. Option C is a blunt instrument that terminates productive conversations prematurely. Option D eliminates the flexibility that makes agentic loops valuable for handling diverse support requests.',
    },
    {
      id: 2, examScenario: 1, domain: 'd1', domainLabel: 'Tool Use Flow',
      scenario: 'A customer requests a refund for order #ORD-8842. In a single API response, Claude returns stop_reason: "tool_use" with three tool_use blocks: lookup_order, check_refund_eligibility, and get_customer_history. Your current implementation executes tool calls sequentially and returns results one at a time.',
      question: 'How should you handle multiple tool_use blocks in a single Claude response?',
      options: [
        'A) Execute only the first tool (lookup_order) and discard the other two to minimize latency.',
        'B) Execute ALL three tools concurrently, collect ALL results, append them together as tool_result messages, and send the combined results back to Claude for the next reasoning iteration.',
        'C) Execute them sequentially, sending each result as a separate message to Claude before executing the next tool.',
        'D) Return an error to Claude requesting it to call only one tool per turn.',
      ],
      correct: 1,
      explanation: 'The API specification requires that ALL tool_use blocks in a response be executed. Results should be batched and appended as tool_result messages before the next agentic loop iteration. This allows Claude to reason about the complete set of data. Option A discards information Claude explicitly requested. Option C introduces unnecessary sequential latency. Option D fights against the API\'s native multi-tool capability.',
    },
    {
      id: 3, examScenario: 1, domain: 'd1', domainLabel: 'Multi-Agent Orchestration',
      scenario: 'Your support system uses a Coordinator agent that classifies requests and routes to specialist agents: RefundAgent, OrderAgent, AccountAgent, and EscalationAgent. Metrics show that simple "where is my order" queries take 4.2 seconds because they pass through the Coordinator, then RefundAgent, then OrderAgent sequentially before reaching the correct agent.',
      question: 'What architectural change would most reduce latency for these queries?',
      options: [
        'A) Route all queries to all four agents in parallel and merge their responses.',
        'B) Have the Coordinator classify the query intent first and invoke only the relevant specialist agent directly, eliminating unnecessary intermediate handoffs.',
        'C) Merge all four specialist agents into a single generalist agent to eliminate routing overhead.',
        'D) Remove the Coordinator and let users select which specialist agent to talk to.',
      ],
      correct: 1,
      explanation: 'Intent classification followed by direct routing to the relevant specialist eliminates the sequential handoff pattern. The Coordinator\'s job is classification and routing — once it identifies "order tracking," it should invoke OrderAgent directly. Option A wastes compute on irrelevant agents and creates response merging complexity. Option C loses specialist depth. Option D shifts architectural complexity to the user.',
    },
    {
      id: 4, examScenario: 1, domain: 'd1', domainLabel: 'Agentic Loops',
      scenario: 'Production logs show that your support agent\'s agentic loop averages 3.1 iterations per conversation. However, 8% of conversations hit the max_iterations limit of 15. Analysis reveals these are complex multi-step requests where Claude needs to: verify customer identity, look up order history, check refund eligibility, process the refund, and send confirmation — each requiring a separate tool call iteration.',
      question: 'What is the most appropriate response to the max_iterations boundary?',
      options: [
        'A) Increase max_iterations to 25 to accommodate complex requests.',
        'B) On the final allowed iteration, inject a message instructing Claude to summarize the current state and provide the user with what has been accomplished so far, plus any remaining steps that need manual follow-up.',
        'C) Return a generic error message: "Sorry, I couldn\'t complete your request. Please try again."',
        'D) Automatically restart the agentic loop with a fresh context window.',
      ],
      correct: 1,
      explanation: 'Graceful degradation on the final iteration preserves the work already completed and gives the user actionable information. This is more helpful than a generic error (Option C) and more reliable than increasing limits (Option A, which merely delays the problem). Option D loses all progress and may loop indefinitely.',
    },
    // ─── D2 — Prompt Engineering ─────────────────────────────────────
    {
      id: 5, examScenario: 1, domain: 'd2', domainLabel: 'System Prompt Design',
      scenario: 'Your support agent\'s system prompt contains the refund policy as a 600-word prose description. Despite this, the agent applies incorrect refund amounts in 12% of cases — for example, offering full refunds on items marked "final sale" or applying a 10% restocking fee when the policy specifies 15%.',
      question: 'What change would most effectively improve policy compliance?',
      options: [
        'A) Add stronger language to the system prompt: "CRITICAL: Never deviate from the refund policy. This is MANDATORY."',
        'B) Replace the prose policy description with a structured reference table (condition → fee percentage) that Claude can parse deterministically, or move the policy to a tool-retrieved document that Claude queries when handling refunds.',
        'C) Double the system prompt length with more detailed explanations of each policy rule.',
        'D) Add a post-processing step that validates refund amounts against the policy before sending to the customer.',
      ],
      correct: 1,
      explanation: 'Structured data (tables, schemas) is parsed more reliably than prose for rule enforcement. When policy rules have specific conditions and numeric outcomes, a structured format reduces interpretation errors. Option A uses emphasis that doesn\'t improve comprehension. Option C increases token cost without changing the format problem. Option D is a reasonable safety net but doesn\'t fix the root cause of incorrect amounts.',
    },
    {
      id: 6, examScenario: 1, domain: 'd2', domainLabel: 'Context Window Management',
      scenario: 'A customer sends a 15-message conversation about a complex return involving multiple items, store credit, and shipping issues. By message 12, the agent begins losing track of which items were approved for return and which are still pending. The conversation is consuming 95,000 tokens of a 128K context window.',
      question: 'What context management strategy would most effectively maintain accuracy?',
      options: [
        'A) Implement periodic state summarization: every 5 turns, extract the current return status (approved items, pending items, credit amounts) into a structured summary that replaces the raw conversation history for those turns.',
        'B) Add a system prompt instruction: "Always track the status of each return item carefully throughout the conversation."',
        'C) Limit conversations to 10 turns to prevent context window overflow.',
        'D) Store all return item statuses in a database accessed via tool, and instruct the agent to verify current status via tool call rather than relying on conversation memory.',
      ],
      correct: 3,
      explanation: 'For transactional data where accuracy is critical (which items are approved, credit amounts), external storage with tool retrieval provides deterministic accuracy. The agent retrieves exact current state rather than relying on increasingly unreliable attention to earlier conversation turns. Option A helps but summaries can lose detail. Option B is advisory and contradicted by the data. Option C artificially limits functionality.',
    },
    {
      id: 7, examScenario: 1, domain: 'd2', domainLabel: 'Prompt Chaining',
      scenario: 'Your support pipeline chains three steps: (1) IntentClassifier categorizes the request, (2) ResponseGenerator drafts the response using category-specific guidelines, (3) QualityChecker validates tone and policy compliance. The QualityChecker rejects 25% of responses, sending them back to ResponseGenerator with feedback. These rejections add an average of 2.3 extra API calls per rejected response.',
      question: 'What change would most effectively reduce the rejection rate?',
      options: [
        'A) Remove the QualityChecker and accept that some responses may not meet standards.',
        'B) Feed the QualityChecker\'s common rejection criteria (tone guidelines, policy rules) back into the ResponseGenerator\'s system prompt as explicit constraints, reducing the initial rejection rate by preventing violations before they occur.',
        'C) Run the pipeline twice for every request and select the version that passes quality checks.',
        'D) Consolidate ResponseGenerator and QualityChecker into a single Claude call that generates and validates simultaneously.',
      ],
      correct: 1,
      explanation: 'Propagating quality criteria upstream (the feedforward pattern) prevents violations at the source rather than catching them downstream. This is more efficient than retrying and doesn\'t sacrifice quality. Option A eliminates quality control. Option C doubles cost. Option D loses the benefit of independent review — the same model is unlikely to catch its own errors reliably.',
    },
    // ─── D3 — Tool Design ────────────────────────────────────────────
    {
      id: 8, examScenario: 1, domain: 'd3', domainLabel: 'Tool Selection & Design',
      scenario: 'Your agent has two tools: lookup_order (description: "Retrieves order details") and search_products (description: "Searches product catalog"). Claude calls search_products 18% of the time when users ask "where is my order" — the minimal descriptions don\'t differentiate them sufficiently for order-tracking queries.',
      question: 'What is the most effective first step to improve tool selection accuracy?',
      options: [
        'A) Add 5-8 few-shot examples to the system prompt showing correct tool usage for order-tracking queries.',
        'B) Expand each tool\'s description to include specific use cases, example queries it handles, and explicit boundaries: lookup_order → "Retrieves order status, tracking info, and delivery details using order IDs (ORD-12345). Use for order tracking, delivery questions, and order modifications." search_products → "Searches the product catalog for items by name, category, or attributes. Use for product availability, comparisons, and recommendations. NOT for tracking existing orders."',
        'C) Implement a routing classifier that parses user input and pre-selects the tool based on detected keywords.',
        'D) Merge both tools into a single search tool that handles both orders and products.',
      ],
      correct: 1,
      explanation: 'Tool descriptions are the primary mechanism Claude uses for tool selection. When descriptions are minimal, the model lacks context to differentiate between similar tools. Expanding descriptions with use cases and explicit boundaries is a low-effort, high-leverage fix that directly addresses the root cause. Option A adds token overhead without fixing the underlying issue. Option C over-engineers what Claude can handle natively. Option D creates a poorly scoped tool.',
    },
    {
      id: 9, examScenario: 1, domain: 'd3', domainLabel: 'Tool Error Handling',
      scenario: 'Your process_refund tool fails 3% of the time due to payment gateway timeouts. When it fails, the tool returns a generic "Error" string. Claude then tells the customer "Your refund has been processed successfully" in 40% of failure cases — the generic error message doesn\'t clearly communicate the failure to Claude.',
      question: 'What error handling pattern would most effectively prevent incorrect success messages?',
      options: [
        'A) Return is_error: true with a structured error response including the failure reason, whether the refund may have partially processed, and an explicit instruction: "The refund failed. Inform the customer that the refund could not be processed at this time and that they will be contacted once it is resolved."',
        'B) Add a system prompt instruction: "Always verify the refund tool result before confirming success to the customer."',
        'C) Retry the refund automatically 3 times before reporting any result to Claude.',
        'D) Remove the refund tool and handle all refunds manually.',
      ],
      correct: 0,
      explanation: 'Structured error responses with is_error: true flag and explicit instructions give Claude clear, unambiguous information about the failure. The key is providing enough context for Claude to respond appropriately. Option B relies on probabilistic self-correction. Option C risks double-refunding and adds latency. Option D eliminates automation benefits.',
    },
    {
      id: 10, examScenario: 1, domain: 'd3', domainLabel: 'API Integration Patterns',
      scenario: 'Your CRM integration tool returns customer data in 2.5 seconds on average. Your order lookup tool returns in 400ms. When both tools are called in the same agentic loop iteration, your implementation executes them sequentially, resulting in 2.9 seconds total wait time.',
      question: 'What architectural pattern would minimize tool execution latency?',
      options: [
        'A) Execute independent tool calls concurrently — when Claude requests both get_customer and lookup_order in the same turn, run them in parallel, reducing total wait time to the slowest call (2.5 seconds).',
        'B) Pre-fetch all customer data at the start of every conversation to eliminate the CRM call during active processing.',
        'C) Cache CRM results indefinitely to avoid repeated calls.',
        'D) Consolidate both tools into a single get_customer_with_orders tool that queries both systems.',
      ],
      correct: 0,
      explanation: 'Parallel execution of independent tool calls is a fundamental optimization — it reduces total wait time from the sum of all calls to the duration of the slowest single call. This is the standard pattern for independent tool calls. Option B wastes resources on data that may never be needed. Option C serves stale data. Option D creates a coupled tool that violates single-responsibility design.',
    },
    // ─── D4 — Output Structuring ─────────────────────────────────────
    {
      id: 11, examScenario: 1, domain: 'd4', domainLabel: 'System Prompt Architecture',
      scenario: 'Your support agent needs to produce structured responses that your front-end renders as cards. The system prompt includes a 300-token output format specification. Despite this, the agent returns plain text responses without the required JSON structure 15% of the time, particularly after long conversations where the format instructions are far from the current turn.',
      question: 'What is the most effective way to enforce consistent JSON output formatting?',
      options: [
        'A) Add stronger language: "You MUST ALWAYS return JSON. This is NON-NEGOTIABLE."',
        'B) Use JSON mode (or structured output features) to programmatically enforce JSON output format at the API level, rather than relying solely on natural language instructions in the system prompt.',
        'C) Repeat the output format specification every 5 turns as a reminder.',
        'D) Add a post-processing layer that wraps plain text responses in the required JSON structure.',
      ],
      correct: 1,
      explanation: 'Programmatic enforcement via JSON mode provides deterministic guarantees — the output will always be valid JSON. Natural language format instructions in the system prompt are advisory and degrade as conversation length increases. Option A uses emphasis that doesn\'t change capability. Option C adds significant token overhead. Option D produces malformed JSON by wrapping arbitrary text in a JSON envelope.',
    },
    {
      id: 12, examScenario: 1, domain: 'd4', domainLabel: 'Validation & Retry Strategies',
      scenario: 'Your agent generates order cancellation confirmations. A validation check catches that 7% of cancellation responses include incorrect estimated refund dates. When the error is fed back to Claude for retry, 65% of retries produce the correct date, but the remaining 35% still get it wrong — Claude consistently misinterprets "3-5 business days" as a specific calendar date.',
      question: 'What strategy would most effectively resolve this persistent retry failure?',
      options: [
        'A) Increase retries from 1 to 3 attempts to improve the success rate.',
        'B) Fix the root cause: provide Claude with a tool that calculates the estimated refund date based on the current date and business rules, eliminating the need for date interpretation entirely.',
        'C) Add a system prompt instruction: "When calculating refund dates, add 3-5 business days to today\'s date, excluding weekends."',
        'D) Accept the 7% error rate and add manual review for all cancellation responses.',
      ],
      correct: 1,
      explanation: 'When retries consistently fail on a specific task (date calculation), the solution is to remove the task from Claude\'s reasoning and delegate it to a deterministic tool. Date calculation is a computational task that tools handle perfectly and LLMs handle unreliably. Option A applies more attempts at a fundamentally flawed approach. Option C is advisory and already contradicted by the data. Option D accepts preventable errors.',
    },
    {
      id: 13, examScenario: 1, domain: 'd4', domainLabel: 'Batch Processing',
      scenario: 'Your support analytics team wants to classify 5,000 historical support tickets by category and sentiment. Each classification costs $0.003 via a real-time API call, totaling $15 for the batch. The Message Batches API offers 50% cost savings with processing times up to 24 hours.',
      question: 'What is the most appropriate approach for this one-time batch classification?',
      options: [
        'A) Use the Message Batches API for the full 5,000 tickets, accepting the 24-hour processing time since this is a one-time historical analysis, not a real-time requirement.',
        'B) Process all tickets via real-time API calls to get results faster.',
        'C) Process 100 tickets via real-time calls as a sample, then use the Message Batches API for the remaining 4,900.',
        'D) Use a single API call with all 5,000 tickets concatenated to minimize total calls.',
      ],
      correct: 0,
      explanation: 'Historical ticket classification has no real-time latency requirement — the results are for analysis, not for routing active conversations. The Message Batches API\'s 50% cost savings are fully appropriate here. Option B pays double for speed that isn\'t needed. Option C adds unnecessary complexity. Option D would exceed context limits and produce terrible accuracy.',
    },
    {
      id: 14, examScenario: 1, domain: 'd4', domainLabel: 'Multi-Instance Review',
      scenario: 'Your support agent processes refund decisions using a multi-instance review pattern: three independent Claude calls analyze the same refund request, and you take the majority decision. You observe that when the decision is ambiguous (e.g., partial refund for a damaged item with unclear cause), all three instances tend to make the same error — approving a full refund when policy specifies partial.',
      question: 'What does this correlated failure pattern indicate, and what would be more effective?',
      options: [
        'A) Increase to 5 instances to improve majority voting reliability.',
        'B) The correlated failures indicate a systematic issue in the prompt, not random variance. Fix the root cause: improve the system prompt with explicit partial refund criteria, specific examples of damage-based vs. defect-based refunds, and a decision tree for refund amounts.',
        'C) Use different temperature settings for each instance to encourage diverse reasoning.',
        'D) Replace majority voting with requiring all three instances to agree before proceeding.',
      ],
      correct: 1,
      explanation: 'Multi-instance review with majority voting only catches random errors, not systematic biases shared across instances. When all instances make the same mistake, the problem is in the shared prompt, not the review mechanism. Fix the prompt\'s refund criteria. Option A wastes compute on correlated failures. Option C adds randomness without fixing the systematic error. Option D would deadlock on ambiguous cases.',
    },
    // ─── D5 — Reliability ────────────────────────────────────────────
    {
      id: 15, examScenario: 1, domain: 'd5', domainLabel: 'Context Management',
      scenario: 'A customer has been chatting with your support agent for 25 minutes across 30 turns, discussing multiple issues. The agent resolved the first issue (password reset) but now, when discussing a billing problem, it occasionally references the password reset context inappropriately — suggesting billing issues might be "related to your recent password change."',
      question: 'What context management strategy would most effectively prevent cross-topic contamination?',
      options: [
        'A) Implement topic segmentation: detect topic boundaries in the conversation, and when the topic shifts, compress the previous topic into a brief resolution summary while preserving only the current topic\'s full context.',
        'B) Add a system prompt instruction: "Keep different support topics separate and don\'t mix context between unrelated issues."',
        'C) Start a new conversation every time the user changes topics.',
        'D) Reduce the context window to force the agent to focus only on recent messages.',
      ],
      correct: 0,
      explanation: 'Topic segmentation with targeted summarization preserves the fact that previous issues were resolved while eliminating detailed context that can contaminate reasoning about the new topic. Option B is advisory and doesn\'t prevent the attention mechanism from accessing earlier context. Option C loses conversation continuity. Option D loses important current-topic context along with old-topic context.',
    },
    {
      id: 16, examScenario: 1, domain: 'd5', domainLabel: 'Escalation & Ambiguity',
      scenario: 'Your agent achieves 68% first-contact resolution, well below the 85% target. Logs show it escalates 30% of straightforward cases (standard returns with clear policy coverage) while autonomously handling complex situations that require policy exceptions (e.g., approving a return outside the 30-day window for a long-term customer).',
      question: 'What change would most effectively improve escalation calibration?',
      options: [
        'A) Add explicit escalation criteria to the system prompt with concrete examples: "Escalate when: policy exception required, customer is VIP tier 3+, refund amount exceeds $500. Do NOT escalate when: standard return within policy, replacement request with stock available."',
        'B) Have the agent self-report confidence (1-10) and automatically escalate when confidence falls below 5.',
        'C) Deploy a separate classifier model trained on historical tickets to predict which requests need escalation.',
        'D) Lower the escalation threshold so fewer cases get escalated.',
      ],
      correct: 0,
      explanation: 'Explicit escalation criteria with concrete examples directly addresses the root cause — unclear decision boundaries between what the agent should handle and what requires human judgment. This is the proportionate first response before adding infrastructure. Option B fails because LLM self-reported confidence is poorly calibrated (the agent is already incorrectly confident on complex cases). Option C is over-engineering when prompt optimization hasn\'t been tried. Option D addresses the wrong direction — the agent should escalate more on complex cases, not less overall.',
    },
    {
      id: 17, examScenario: 1, domain: 'd5', domainLabel: 'Error Propagation',
      scenario: 'Your support system has three layers: FrontDeskAgent → SpecialistAgent → EscalationAgent. When the payment gateway times out, the SpecialistAgent catches the error but returns a generic "processing failed" to the FrontDeskAgent, which tells the customer "something went wrong." In 60% of timeout cases, the payment actually succeeded — the gateway just didn\'t respond in time.',
      question: 'What error propagation pattern would provide the most useful information at each layer?',
      options: [
        'A) Propagate the raw timeout exception directly to the customer with full technical details.',
        'B) Implement structured error chaining: the SpecialistAgent returns {type: "gateway_timeout", retryable: true, payment_status: "unknown", context: "Charge attempted, result unknown"} to FrontDeskAgent, which can then tell the customer: "Your payment is processing — we\'ll confirm within 30 minutes" and queue a reconciliation check.',
        'C) Have the SpecialistAgent automatically retry the payment 3 times before reporting failure.',
        'D) Catch all errors at the FrontDeskAgent level and always respond with "Please try again later."',
      ],
      correct: 1,
      explanation: 'Structured error chaining preserves critical context (payment status is unknown, not failed) at each layer. The FrontDeskAgent can give an accurate response that doesn\'t mislead the customer about a payment that likely succeeded. Option A exposes confusing technical details. Option C risks double-charging the 60% that already succeeded. Option D provides a generic response that doesn\'t reflect the actual situation.',
    },
    {
      id: 18, examScenario: 1, domain: 'd5', domainLabel: 'Human Review & Confidence',
      scenario: 'Your refund processing agent uses confidence scoring to decide between autonomous action and human review. The current threshold is 0.7 confidence for autonomous processing. Analysis shows: 15% of autonomous refunds above this threshold are incorrect (wrong amount or policy violation), while 40% of escalated refunds would have been handled correctly.',
      question: 'What does this data suggest about the confidence threshold, and what adjustment should you consider?',
      options: [
        'A) The threshold is correctly placed — a 15% error rate is acceptable for automated refunds.',
        'B) The 15% error rate on autonomous refunds suggests the confidence score is miscalibrated for this task. Rather than adjusting the threshold alone, improve the decision criteria in the system prompt with explicit policy rules and examples, then recalibrate the threshold based on new accuracy data.',
        'C) Raise the threshold to 0.9 to eliminate all errors, accepting that more refunds will need human review.',
        'D) Remove confidence scoring entirely and process all refunds autonomously.',
      ],
      correct: 1,
      explanation: 'A 15% error rate on high-stakes financial decisions is significant. The root issue is likely that the confidence score doesn\'t reflect true accuracy — the model is overconfident on cases where it shouldn\'t be. Improving the decision criteria (better policy rules, explicit examples) addresses the underlying miscalibration before adjusting thresholds. Option A accepts a high error rate. Option C may over-correct and increase unnecessary escalations. Option D removes safety controls entirely.',
    },
    {
      id: 19, examScenario: 1, domain: 'd5', domainLabel: 'Provenance & Attribution',
      scenario: 'Your support agent generates resolution summaries that are stored in the customer\'s case history. Operations team members report that summaries often contain specific claims ("customer was offered a 20% discount") that don\'t match what actually happened in the conversation. When they try to trace where the claim came from, the summary provides no source attribution.',
      question: 'What provenance pattern would most effectively ensure summary accuracy?',
      options: [
        'A) Implement a verification step: before finalizing the summary, cross-reference each claim in the summary against the actual conversation transcript. Flag any claims that can\'t be directly traced to specific messages.',
        'B) Add a system prompt instruction: "Only include facts that were explicitly stated in the conversation."',
        'C) Have a human review all summaries before they are saved to case history.',
        'D) Store the full conversation transcript instead of generating summaries.',
      ],
      correct: 0,
      explanation: 'A verification pass that cross-references summary claims against the source transcript catches hallucinated details before they enter the case history. This is especially important for records that affect future customer interactions. Option B is advisory and doesn\'t prevent confident hallucinations. Option C doesn\'t scale for high-volume support. Option D defeats the purpose of summarization and makes case history harder to scan.',
    },
    {
      id: 20, examScenario: 1, domain: 'd5', domainLabel: 'Codebase Exploration',
      scenario: 'You\'re using Claude Code to fix a bug in your customer support codebase. The bug report says: "Refund amounts are rounded incorrectly for international currencies." The codebase has 45 files. You want Claude Code to efficiently locate the relevant code without reading every file.',
      question: 'What is the most efficient codebase exploration strategy?',
      options: [
        'A) Start by reading all files in the project to build a complete understanding of the codebase before attempting the fix.',
        'B) Use search tools to find files containing "refund" and "currency" and "round", then read those files and follow import chains to understand the relevant code paths.',
        'C) Read only the README.md and infer the code structure from documentation.',
        'D) Ask the developer to point you to the exact file before starting.',
      ],
      correct: 1,
      explanation: 'Targeted search followed by dependency traversal is the efficient exploration pattern — it discovers relevant files quickly and expands understanding only along relevant code paths. This is dramatically faster than reading all files. Option A wastes time and context on irrelevant code. Option C provides overview without the detail needed for debugging. Option D shifts work to the developer that the agent should handle autonomously.',
    },
  ],
};

export default exam;
