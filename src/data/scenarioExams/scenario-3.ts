import type { ScenarioExamData } from './types';

const exam: ScenarioExamData = {
  id: 3,
  title: 'Multi-Agent Research System',
  description: 'Design and optimize a multi-agent architecture for research, analysis, and synthesis of complex topics.',
  icon: '🔬',
  questions: [
    // ─── D1 — Agentic Architecture ──────────────────────────────────
    {
      id: 1, examScenario: 3, domain: 'd1', domainLabel: 'Multi-Agent Orchestration',
      scenario: 'After running your research system on "impact of AI on creative industries," each subagent completes successfully: the web search agent finds articles, the document analysis agent summarizes papers, and the synthesis agent produces coherent output. However, the final report covers only visual arts, missing music, writing, and film. The coordinator\'s logs show it decomposed the topic into: "AI in digital art," "AI in graphic design," and "AI in photography."',
      question: 'What is the most likely root cause of the incomplete coverage?',
      options: [
        'A) The synthesis agent lacks instructions for identifying coverage gaps in its inputs.',
        'B) The coordinator agent\'s task decomposition is too narrow, producing subtasks that cover only one domain (visual arts) instead of spanning all relevant creative industries.',
        'C) The web search agent\'s queries are not comprehensive enough to find articles about music and film.',
        'D) The document analysis agent filters out non-visual-arts sources due to restrictive relevance criteria.',
      ],
      correct: 1,
      explanation: 'The coordinator\'s logs reveal the root cause directly: it decomposed "creative industries" into only visual arts subtasks, omitting music, writing, and film. The subagents executed correctly within their assigned scope — the problem is what they were assigned. Options A, C, and D blame downstream agents working correctly within their given tasks.',
    },
    {
      id: 2, examScenario: 3, domain: 'd1', domainLabel: 'Agentic Loops',
      scenario: 'Your research agent uses an agentic loop with 3 tools: search_web, read_document, and synthesize_findings. On a complex research topic, the agent calls search_web 8 times with slightly different queries, accumulating 45,000 tokens of search results. By the time it calls synthesize_findings, only 15,000 tokens remain for synthesis, producing shallow output.',
      question: 'What loop management strategy would most effectively balance research depth with synthesis quality?',
      options: [
        'A) Set a hard limit of 3 search calls per research session.',
        'B) Implement a context budget: allocate 40% of the context window to research (search + reading) and 60% to synthesis. When the research budget is reached, force transition to synthesis mode regardless of whether additional searches are possible.',
        'C) Remove the search_web tool and provide all relevant documents upfront.',
        'D) Let the agent continue searching until it decides it has enough information.',
      ],
      correct: 1,
      explanation: 'A context budget ensures the synthesis phase always has adequate context for producing quality output. The 40/60 split prioritizes the final deliverable (synthesis) while giving research sufficient room. Option A is arbitrary and may cut off genuinely needed searches. Option C eliminates the agent\'s ability to search dynamically. Option D risks consuming all context on research with nothing left for synthesis.',
    },
    {
      id: 3, examScenario: 3, domain: 'd1', domainLabel: 'Multi-Agent Orchestration',
      scenario: 'Your research pipeline has four agents: TopicDecomposer → SearchAgent → AnalysisAgent → SynthesisAgent. Each agent runs as a separate Claude API call. The pipeline processes a research request in 45 seconds total. Profiling shows: TopicDecomposer (3s), SearchAgent (20s — calls external search API), AnalysisAgent (12s), SynthesisAgent (10s). All four run sequentially.',
      question: 'What architectural change could reduce total pipeline latency?',
      options: [
        'A) Run all four agents in parallel and merge their outputs.',
        'B) After TopicDecomposer produces subtopics, run multiple SearchAgent instances in parallel — one per subtopic — then pass all results to AnalysisAgent. This reduces the search phase from sequential to parallel.',
        'C) Remove the TopicDecomposer and have SearchAgent handle decomposition internally.',
        'D) Merge AnalysisAgent and SynthesisAgent into a single agent to eliminate one sequential step.',
      ],
      correct: 1,
      explanation: 'Parallelizing the search phase by spawning one SearchAgent per subtopic is the most impactful optimization — the 20-second search phase dominates total latency. If TopicDecomposer produces 4 subtopics and searches run in parallel, the search phase drops from 20s to ~5s (one search duration). Option A doesn\'t make sense for a sequential pipeline. Option C removes the decomposition step that enables parallel searches. Option D saves less time than parallelizing the bottleneck.',
    },
    {
      id: 4, examScenario: 3, domain: 'd1', domainLabel: 'Tool Use Flow',
      scenario: 'Your research agent receives a tool_result from search_web containing 10 search results, each with title, URL, and snippet. Claude\'s next response contains three tool_use blocks: read_document for URLs 1, 3, and 7. Your implementation executes all three reads and returns results in a different order than requested.',
      question: 'How should tool results be ordered when returning multiple tool results?',
      options: [
        'A) Order doesn\'t matter — Claude will figure out which result corresponds to which request.',
        'B) Return results in the same order as the tool_use blocks in Claude\'s request, matching each tool_result to its corresponding tool_use by tool_use_id to avoid confusion.',
        'C) Return results ordered by document length, shortest first.',
        'D) Return only the most relevant result and discard the others.',
      ],
      correct: 1,
      explanation: 'Each tool_use block has a unique tool_use_id. Results must be matched to their requests by this ID, ensuring Claude correctly associates each document content with the correct URL. Mismatched results cause the agent to attribute content to wrong sources. Option A is incorrect — Claude uses IDs to match results. Option C is arbitrary and unhelpful. Option D discards requested information.',
    },
    // ─── D2 — Prompt Engineering ─────────────────────────────────────
    {
      id: 5, examScenario: 3, domain: 'd2', domainLabel: 'System Prompt Design',
      scenario: 'Your synthesis agent\'s system prompt instructs: "Synthesize research findings into a comprehensive report." The agent produces reports that are comprehensive but include claims not supported by the provided research data — it fills gaps with its training knowledge without distinguishing between sourced claims and generated inferences.',
      question: 'What system prompt change would most effectively prevent unsupported claims?',
      options: [
        'A) Add: "Only include claims that are directly supported by the provided research data. If information is missing, explicitly state that rather than inferring."',
        'B) Increase the system prompt\'s emphasis: "NEVER include information not in the research data. This is CRITICAL."',
        'C) Remove the word "comprehensive" from the instruction to discourage gap-filling.',
        'D) Add few-shot examples showing reports with explicit "data not available" statements.',
      ],
      correct: 0,
      explanation: 'Explicit instructions to distinguish between sourced claims and missing information directly address the gap-filling behavior. Providing a clear alternative action ("explicitly state that information is missing") gives Claude a constructive behavior to substitute. Option B uses emphasis without providing an alternative action. Option C changes scope but doesn\'t prevent the core issue. Option D is complementary but less direct than explicit instructions.',
    },
    {
      id: 6, examScenario: 3, domain: 'd2', domainLabel: 'Context Window Management',
      scenario: 'Your research agent\'s context window contains: system prompt (2,000 tokens), coordinator instructions (1,500 tokens), search results from 5 sources (25,000 tokens), and analysis from the AnalysisAgent (8,000 tokens). The SynthesisAgent needs to produce a detailed report but only has 91,500 tokens remaining of a 128K window.',
      question: 'Is 91,500 tokens sufficient for synthesis, and what should you monitor?',
      options: [
        'A) Yes, 91,500 tokens is more than enough. No monitoring needed.',
        'B) Yes, but monitor the synthesis output length. If the SynthesisAgent\'s response is truncated, it means the output exceeded the remaining context, and you should implement output length limits or split the synthesis into sections.',
        'C) No, 91,500 tokens is insufficient. Compress the search results before passing to synthesis.',
        'D) No, increase the model to 200K context to ensure adequate room.',
      ],
      correct: 1,
      explanation: '91,500 tokens is generally sufficient for synthesis, but output truncation is the risk to monitor. If the synthesis response itself is very long (detailed report with many sections), it could be cut off. Implementing output length limits or sectioned synthesis prevents this. Option A is overly optimistic without monitoring. Option C is premature — 91K tokens is substantial. Option D increases cost unnecessarily.',
    },
    {
      id: 7, examScenario: 3, domain: 'd2', domainLabel: 'Prompt Chaining',
      scenario: 'Your research pipeline chains: SearchAgent → AnalysisAgent → SynthesisAgent. The AnalysisAgent receives raw search results and produces structured analysis with categorized findings. The SynthesisAgent receives this analysis and produces the final report. You notice the SynthesisAgent frequently re-analyzes data that the AnalysisAgent already processed, wasting tokens on duplicate work.',
      question: 'What prompt chaining optimization would eliminate this duplication?',
      options: [
        'A) Merge the Analysis and Synthesis agents into a single agent.',
        'B) Structure the AnalysisAgent\'s output as a definitive intermediate format — a pre-digested brief with categorized findings, confidence levels, and key themes — and instruct the SynthesisAgent to build on this brief directly rather than re-analyzing the raw data.',
        'C) Remove the AnalysisAgent and have SynthesisAgent process raw search results directly.',
        'D) Send only the AnalysisAgent\'s conclusions to SynthesisAgent, excluding supporting evidence.',
      ],
      correct: 1,
      explanation: 'A well-defined intermediate format with clear boundaries prevents downstream agents from redoing upstream work. The SynthesisAgent should build on the AnalysisAgent\'s structured output, not re-process raw data. Option A loses specialization benefits. Option C removes the analysis layer that adds value. Option D loses the evidence that supports conclusions.',
    },
    // ─── D3 — Tool Design ────────────────────────────────────────────
    {
      id: 8, examScenario: 3, domain: 'd3', domainLabel: 'Tool Error Handling',
      scenario: 'The web search subagent times out while researching a complex topic. You need to design how this failure information flows back to the coordinator agent.',
      question: 'Which error propagation approach best enables intelligent recovery?',
      options: [
        'A) Return structured error context to the coordinator including the failure type, the attempted query, any partial results obtained before the timeout, and suggested alternative approaches the coordinator could try.',
        'B) Implement automatic retry logic with exponential backoff within the subagent, returning a generic "search unavailable" status only after all retries are exhausted.',
        'C) Catch the timeout within the subagent and return an empty result set marked as successful.',
        'D) Propagate the timeout exception directly to a top-level handler that terminates the entire research workflow.',
      ],
      correct: 0,
      explanation: 'Structured error context gives the coordinator the information to make intelligent recovery decisions — whether to retry with a modified query, try an alternative approach, or proceed with partial results. Option B hides valuable context behind a generic status. Option C suppresses the error entirely, preventing any recovery. Option D terminates the workflow unnecessarily when recovery is possible.',
    },
    {
      id: 9, examScenario: 3, domain: 'd3', domainLabel: 'API Integration Patterns',
      scenario: 'Your research agent integrates with three external APIs: academic paper search (returns JSON), news aggregator (returns XML), and social media search (returns GraphQL response). Each API has different authentication, rate limiting, and response formats.',
      question: 'What integration pattern would most cleanly handle this API diversity?',
      options: [
        'A) Have Claude parse each API\'s raw response format directly.',
        'B) Implement an adapter layer for each API that handles authentication, rate limiting, and transforms each response into a unified internal format before passing results to Claude. This isolates Claude from API-specific details.',
        'C) Consolidate all three APIs behind a single gateway that handles routing internally.',
        'D) Use only the JSON API and ignore the XML and GraphQL sources.',
      ],
      correct: 1,
      explanation: 'The adapter pattern isolates Claude from external API diversity. Each adapter handles its API\'s authentication, format transformation, and rate limiting, presenting a consistent interface to the agent. When an API changes, only its adapter needs updating. Option A wastes context tokens on format parsing. Option C adds infrastructure complexity. Option D sacrifices data sources unnecessarily.',
    },
    // ─── D4 — Output Structuring ─────────────────────────────────────
    {
      id: 10, examScenario: 3, domain: 'd4', domainLabel: 'Structured Output Patterns',
      scenario: 'Your research synthesis agent must produce reports with consistent sections: Executive Summary, Methodology, Findings (categorized by theme), and Recommendations. You define a JSON schema, but the agent occasionally omits sections or produces them in varying order.',
      question: 'What approach would most effectively enforce consistent report structure?',
      options: [
        'A) Use JSON mode with a strict schema that defines required fields and their order, programmatically enforcing the structure rather than relying on natural language instructions.',
        'B) Add a system prompt instruction listing the required sections in order.',
        'C) Add few-shot examples showing correctly structured reports.',
        'D) Post-process the output to reorder and fill missing sections.',
      ],
      correct: 0,
      explanation: 'JSON mode with a strict schema provides deterministic structural enforcement — the output will always conform to the defined schema with required fields. Natural language instructions (Option B) and examples (Option C) are advisory and don\'t guarantee compliance. Option D adds a fragile post-processing step that may misinterpret content.',
    },
    {
      id: 11, examScenario: 3, domain: 'd4', domainLabel: 'Validation & Retry Strategies',
      scenario: 'Your research agent produces factual claims in its reports. A validation pass cross-checks claims against source documents and finds that 12% of claims are not directly supported by the provided sources. When the validation results are fed back for retry, the agent sometimes removes the claim entirely rather than adding proper attribution or hedging language.',
      question: 'What retry strategy would produce the best outcome for unsupported claims?',
      options: [
        'A) Accept the removal — unsupported claims shouldn\'t be in the report.',
        'B) Provide explicit retry instructions: "For each unsupported claim, do NOT remove it. Instead, add a citation tag if the information exists in the sources, or add hedging language like \'according to industry reports\' if the claim is reasonable but unverifiable from the provided data."',
        'C) Increase the number of source documents to reduce unsupported claims.',
        'D) Remove the validation pass and accept some inaccuracy.',
      ],
      correct: 1,
      explanation: 'Explicit retry instructions give Claude a constructive alternative to removal. Many unsupported claims may be accurate but require proper attribution or hedging rather than deletion. Option A loses potentially valuable content. Option C addresses the source problem but doesn\'t fix the retry behavior. Option D eliminates quality control.',
    },
    {
      id: 12, examScenario: 3, domain: 'd4', domainLabel: 'Batch Processing',
      scenario: 'Your research system processes 200 research queries per day. Each query involves 4 sequential Claude API calls (decompose, search, analyze, synthesize). Total daily cost is $24. You want to reduce costs using the Message Batches API.',
      question: 'For which stage(s) would the Message Batches API be appropriate?',
      options: [
        'A) All four stages — batch everything for maximum savings.',
        'B) Only the initial decomposition stage, since it has no dependencies on external API calls and results can wait up to 24 hours before starting the rest of the pipeline.',
        'C) Only the search stage, since it\'s the longest-running.',
        'D) The Message Batches API is not suitable for any stage of a sequential pipeline.',
      ],
      correct: 1,
      explanation: 'The decomposition stage has no real-time dependencies — it takes a research query and produces subtopics. This can be batched overnight for next-day processing. The subsequent stages depend on decomposition results, so they can\'t be batched independently. Option A breaks the sequential dependency chain. Option C targets the wrong stage — the search stage requires external API calls that can\'t be batched via the Message Batches API. Option D is overly restrictive.',
    },
    {
      id: 13, examScenario: 3, domain: 'd4', domainLabel: 'Multi-Instance Review',
      scenario: 'Your research system uses three parallel analysis agents to process the same source material, then takes the consensus findings. You observe that all three agents miss the same key insight from a complex academic paper — the insight is buried in a footnote on page 14.',
      question: 'What does this correlated failure reveal about the multi-instance approach?',
      options: [
        'A) The multi-instance approach is flawed and should be abandoned.',
        'B) Multi-instance review catches random failures but not systematic attention gaps. The fix is not more instances but better attention distribution — for long documents, implement chunked processing where different instances focus on different sections, or add explicit instructions to review footnotes, appendices, and supplementary materials.',
        'C) Use 5 instances instead of 3 to improve coverage.',
        'D) Use different models for each instance to ensure independent reasoning.',
      ],
      correct: 1,
      explanation: 'Multi-instance redundancy only helps with random variance, not systematic attention gaps. When the same model with the same prompt reads the same document, it will have the same attention patterns. The fix is structural — ensuring comprehensive coverage through section-focused processing or explicit attention to commonly-skipped sections. Option A overreacts — the pattern still catches random errors. Option C wastes compute on correlated failures. Option D may help marginally but doesn\'t address the fundamental attention gap.',
    },
    // ─── D5 — Reliability ────────────────────────────────────────────
    {
      id: 14, examScenario: 3, domain: 'd5', domainLabel: 'Context Management',
      scenario: 'Your multi-agent research system processes a query through: Coordinator → SearchAgent → AnalysisAgent → SynthesisAgent. Each agent is a separate API call. The SynthesisAgent\'s output shows it has no awareness of the original research question — it synthesizes the analysis results without referencing the user\'s specific information needs.',
      question: 'What context management change would fix this?',
      options: [
        'A) Make the SynthesisAgent re-read all search results directly.',
        'B) Include the original research question and the Coordinator\'s decomposition as persistent context in every downstream agent\'s prompt, ensuring all agents understand the original goal.',
        'C) Have the SynthesisAgent ask the user to repeat their question.',
        'D) Add the original question to the SynthesisAgent\'s system prompt only.',
      ],
      correct: 1,
      explanation: 'Passing the original research question and decomposition through the entire pipeline ensures every agent understands the user\'s intent. This is the context propagation pattern — critical information should flow forward through the pipeline, not just the intermediate results. Option A is wasteful — the search results don\'t contain the user\'s question. Option C is a poor user experience. Option D is insufficient — the decomposition context also matters.',
    },
    {
      id: 15, examScenario: 3, domain: 'd5', domainLabel: 'Escalation & Ambiguity',
      scenario: 'Your research system receives a vague query: "Tell me about AI." The Coordinator decomposes this into 5 subtopics and dispatches research. The resulting report is 15 pages long but doesn\'t address what the user actually wanted — they were looking for a specific AI tool comparison.',
      question: 'What architectural improvement would most effectively handle vague queries?',
      options: [
        'A) Always produce comprehensive reports to cover all possible interpretations.',
        'B) Implement a clarification step: when the Coordinator detects ambiguity (broad topic, no specific angle), it returns a clarifying question to the user with 3-4 specific angles before decomposing into research subtasks.',
        'C) Use a larger model that can infer the user\'s intent from context.',
        'D) Add a feedback loop where the user rates the report and the system adjusts.',
      ],
      correct: 1,
      explanation: 'A clarification step before decomposition resolves ambiguity at the source. Presenting specific angles ("Are you interested in: AI tools comparison, AI industry trends, AI research papers, or AI ethics?") is more efficient than producing a comprehensive report that may miss the mark. Option A wastes resources and doesn\'t guarantee relevance. Option C doesn\'t reliably fix vagueness. Option D is reactive — better to get it right the first time.',
    },
    {
      id: 16, examScenario: 3, domain: 'd5', domainLabel: 'Error Propagation',
      scenario: 'During testing, the synthesis agent frequently needs to verify specific claims while combining findings. Currently, when verification is needed, the synthesis agent returns control to the coordinator, which invokes the web search agent, then re-invokes synthesis with results. This adds 2-3 round trips per task and increases latency by 40%. Evaluation shows 85% of verifications are simple fact-checks (dates, names, statistics) while 15% require deeper investigation.',
      question: 'What\'s the most effective approach to reduce overhead while maintaining reliability?',
      options: [
        'A) Give the synthesis agent a scoped verify_fact tool for simple lookups, while complex verifications continue delegating to the web search agent through the coordinator.',
        'B) Have the synthesis agent accumulate all verification needs and return them as a batch to the coordinator at the end of its pass.',
        'C) Give the synthesis agent access to all web search tools so it can handle any verification directly.',
        'D) Have the web search agent proactively cache extra context during initial research, anticipating what synthesis might need.',
      ],
      correct: 0,
      explanation: 'A scoped verify_fact tool applies the principle of least privilege — the synthesis agent gets exactly what it needs for the 85% common case (simple fact verification) while preserving the existing coordination pattern for the 15% complex cases. Option B creates blocking dependencies. Option C over-provisions the synthesis agent, violating separation of concerns. Option D relies on speculative caching that can\'t reliably predict verification needs.',
    },
    {
      id: 17, examScenario: 3, domain: 'd5', domainLabel: 'Provenance & Attribution',
      scenario: 'Your research system produces a report that includes the claim: "The global AI market is expected to reach $1.8 trillion by 2030." A stakeholder asks for the source, but neither the report nor the agent\'s logs indicate which of the 15 search results this figure came from.',
      question: 'What provenance pattern should have been implemented during synthesis?',
      options: [
        'A) Add inline citations to each factual claim: "The global AI market is expected to reach $1.8 trillion by 2030 [Source 7]" with a numbered bibliography mapping to the original search results.',
        'B) List all 15 sources at the end of the report and let readers determine which supports which claim.',
        'C) Include a confidence score for each claim.',
        'D) Only include claims that appear in multiple sources.',
      ],
      correct: 0,
      explanation: 'Inline citations with a bibliography provide direct provenance — stakeholders can immediately verify any claim by following its citation. This is essential for research reports used in decision-making. Option B provides sources without the claim-source mapping. Option C indicates reliability but doesn\'t help verification. Option D eliminates potentially valuable single-source findings.',
    },
    {
      id: 18, examScenario: 3, domain: 'd5', domainLabel: 'Human Review & Confidence',
      scenario: 'Your automated research system produces market analysis reports. A report includes a recommendation to exit the Chinese market based on AI-generated analysis. The system\'s confidence score is 0.78. The recommendation would affect $50M in annual revenue.',
      question: 'Should this recommendation be published without review?',
      options: [
        'A) Yes — 0.78 exceeds the 0.7 threshold for autonomous publication.',
        'B) No — the recommendation has high financial impact ($50M). Impact-based review policies should override confidence thresholds for decisions above a financial materiality threshold, regardless of confidence score.',
        'C) Yes, but add a disclaimer that the analysis was AI-generated.',
        'D) Lower the confidence threshold to 0.6 to allow more autonomous publications.',
      ],
      correct: 1,
      explanation: 'High-impact decisions require human review regardless of confidence scores. The $50M revenue impact far exceeds any reasonable threshold for automated decision-making. Confidence scores measure the model\'s certainty, not the correctness of the output. Option A ignores the financial stakes. Option C adds a disclaimer but still acts on potentially flawed analysis. Option D lowers standards in the wrong direction.',
    },
    {
      id: 19, examScenario: 3, domain: 'd5', domainLabel: 'Context Management',
      scenario: 'Your multi-agent system processes a 40-page research document through three sequential agents: Summarizer (produces 4,000-token summary), Analyst (produces 6,000-token analysis), and ReportWriter (produces final 3,000-token report). The original document contains critical nuance in its methodology section that is lost during the Summarizer\'s compression, leading the Analyst to draw incorrect conclusions.',
      question: 'What context management pattern would preserve critical nuance through the pipeline?',
      options: [
        'A) Pass the full 40-page document to every agent, accepting the token cost.',
        'B) Instead of pure sequential compression, use a hybrid approach: the Summarizer produces a structured summary with key sections preserved verbatim (especially methodology), and flags important nuances as explicit annotations that downstream agents receive alongside the summary.',
        'C) Have the Analyst read the original document directly, bypassing the Summarizer.',
        'D) Make all three agents read the original document independently.',
      ],
      correct: 1,
      explanation: 'A structured summary with verbatim preservation of critical sections (methodology) and explicit annotations for nuances balances compression efficiency with information fidelity. The Summarizer\'s job expands from pure compression to intelligent information triage. Option A is wasteful — the full document is too large for each agent. Option C eliminates the Summarizer\'s value. Option D triples token consumption.',
    },
    {
      id: 20, examScenario: 3, domain: 'd5', domainLabel: 'Codebase Exploration',
      scenario: 'You\'re using Claude Code to build a new research agent. You want to leverage existing utility functions from your codebase rather than writing everything from scratch. The codebase has 200+ files across multiple directories.',
      question: 'What is the most efficient approach to discover reusable components?',
      options: [
        'A) Read all 200+ files to build a complete map of available utilities.',
        'B) Use search tools to find files with relevant keywords (e.g., "API client", "search", "document parser"), read the matching files and their type definitions, then follow import chains to understand the available interfaces.',
        'C) Read only the README.md for a project overview.',
        'D) Ask a senior developer to provide a list of reusable utilities.',
      ],
      correct: 1,
      explanation: 'Targeted search followed by dependency traversal efficiently discovers relevant utilities without reading the entire codebase. This is the standard codebase exploration pattern — search for keywords, read matching files, follow imports to understand interfaces. Option A wastes context and time on irrelevant files. Option C provides overview without discoverable detail. Option D shifts work to humans that the tool can handle.',
    },
  ],
};

export default exam;
